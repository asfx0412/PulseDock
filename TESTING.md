# PulseDock 测试说明

## macOS 当前自动化

`./scripts/test.sh` 是当前 Command Line Tools 环境的权威离线门禁。它覆盖：

- 格式化、网络范围、活跃时长和天气展示；
- 浮窗命中分类（控件/滚动条与 ScrollView 文档背景）；
- Codex 主/次额度周期、`rateLimitsByLimitId`、最低剩余额度周期、错误 envelope、Token 活动、分块 JSON 和无尾换行；
- GLM 多周期、弱化编号、名称优先和百分比夹紧；
- Radio Browser 的 HTTPS/codec/失效台/重复/内网 URL 过滤；
- Radio Browser 合法空结果、目录 offset 分页，以及音频默认静音、CC0 来源唯一性、HTTP 在创建 player 前拦截、stop 幂等；
- 会话级音频中转只接受公共 HTTPS、拒绝 loopback/私网；声音搜索嵌入 `NSHostingView` 后可构造并以程序方式成为 first responder。该离线断言**不能证明**从非活跃 accessory NSPanel 首击进入中文输入法时的真实 field editor/marked-text 稳定性；
- SSH/GPU 重复行、跨卡 PID、N/A、越界显存、重复索引/job、环境变量/URL/路径脱敏；同/异本地子网的局域网范围判断；
- GLM 新旧结构、数字字符串、业务错误/HTML 响应；事件账本、Clash TCP/固定 Unix Socket 地址规则与统一凭据保险库非交互策略。

`scripts/build.sh` 会先运行上述套件，再编译、签名、打包，并校验版本、plist/strings、arm64、ZIP 完整性、解包后签名和 5 份本地化资源。

仓库保留 XCTest 测试，但这台机器仅安装 Command Line Tools，SDK 不含 `XCTest` 模块；同等格式化断言已纳入 `scripts/test.sh`。安装完整 Xcode 后可额外执行 `swift test`。

## 测试层级

1. 解析与模型测试：额度、Clash、诊断、事件账本、SSH/GPU CSV、重复 PID、脱敏规则。
2. 窗口策略测试：紧凑态不抢焦点、展开态可成为 key/main window、禁止动态切换 `.nonactivatingPanel`。
3. 真实只读冒烟测试：对用户已配置的 SSH 别名执行固定只读探测，不修改远端文件或环境。
4. 发布测试：完整 Release 编译、临时签名验证、Info.plist 校验、ZIP 解压校验。
5. 人工交互清单：双击、拖动、输入/复制粘贴、菜单栏、Dock、时间线首次打开、设备展开与刷新，以及连续定位取消/超时恢复。
6. 本地会话测试：Cursor 只读 access token 查询必须只输出状态，不输出 token、邮箱或完整响应。
7. 钥匙串策略测试：初始化和后台任务不得读取 Keychain；秘密字段不得逐字符写入；所有 Security.framework 操作必须禁止认证 UI。
8. 崩溃回归：SSH/GPU 样本必须覆盖重复 GPU UUID、跨卡重复 PID、重复进程元数据和畸形 CSV；外部数据不得直接使用会因重复键终止进程的字典初始化器。
9. Codex 协议测试：主/次窗口、`rateLimitsByLimitId`、无次级窗口、Token 活动可用/不可用、时间戳秒/毫秒和畸形响应。
10. 媒体目录测试：重复 UUID/URL、HTTP 流、未知 codec、失效台、截断 JSON 和空结果；未点击播放前不得创建播放器。
11. 远程网络策略验收：局域网目标地址与当前本地 IPv4/掩码匹配才探测；范围不匹配或 VPN 未连接时预期暂停且不告警；公网设备继续探测；Wi-Fi→Wi-Fi 后立即重判，探测期间切网时迟到结果不得覆盖暂停状态；系统观察到路径变化后 90 秒宽限、前两次失败不告警、第三次失败开始事件、恢复只通知一次。该项仍包含人工/真实网络回归，不得仅凭离线解析测试声称通过。
12. 界面响应测试：用主线程 ping、`sample`/signpost 和真实 AppKit 事件测量点击延迟；覆盖非 key 首击、声音播放、定时、分页、导航、三台 SSH 探测、温度采样和 30 秒活动存盘。互斥单双击手势不得作为即时操作入口；只验证 action 可调用不等于验证首次点击及时送达。

## 测试成本与模型使用原则

- 编译器、静态脚本、确定性 fixture 和真实只读冒烟测试优先直接由工具执行；不能用语言模型“阅读后觉得没问题”替代可重复测试。
- 在支持任务委派的开发环境中，日志归类、文档一致性、测试清单核对和重复 fixture 审阅等低风险、边界明确的工作可交给更快、更低成本的模型；主开发代理负责架构、安全边界、真实缺陷修复和最终发布判断。
- 不为每次讨论或仅更新待办重复执行完整 Release 构建。完整测试只在进入开发验收、修改相关核心链路或准备发布时执行；普通需求对齐只更新规范和验收标准。
- 弱模型的结论不得单独作为发版证据。最终门禁仍以脚本输出、编译结果、真实设备证据和人工交互记录为准。

## 自动化命令

```sh
./scripts/test.sh
```

真实 SSH 冒烟测试需明确传入已授权别名：

```sh
./scripts/test-remote.sh a100
```

Cursor 真实只读冒烟测试（需要本机已登录 Cursor）：

```sh
./scripts/test-cursor.sh
```

真实测试不会写入远端，不会输出私钥、密码、Token 或完整未脱敏命令。Cursor 测试只读取 `cursorAuth/accessToken` 并在内存中完成一次请求，不读取 refresh token。

## 发版门槛

- 任一自动化测试失败：不得生成新版本号安装包。
- Release 编译出现错误：不得发布；新增编译警告必须解释或修复。
- 真实 SSH 不可用不等于构建失败，但必须显示可复制的连接阶段与原因。
- 无法自动覆盖的 macOS 视觉/菜单栏行为必须列入人工检查，不得声称已经自动验证。
- Mihomo 真实端口检测仅对用户本机回环地址执行；自动化测试只验证地址限制与失败分类，不绑定本地端口。
- 钥匙串人工回归必须先完全退出旧版，再启动新版；连续观察启动、切页和至少一个五分钟后台周期，系统密码框出现一次即判定失败。
- 声音人工回归必须覆盖默认静音、快速切源、暂停/恢复/停止、定时结束、Wi‑Fi 切换和应用退出；同一时刻只能存在一条音频流。
- 6.11 中文目录回归：选择“大陆中文”时每张卡国家必须为 China/CN；港澳台和海外中文只能出现在各自明确类别。港澳台、全球中文在 `chinese` 首批不足六条时必须继续回填 `mandarin/cantonese` 和受限后续批次；工作音乐搜索“华语”等关键词不得混入 FM、新闻、谈话或播客电台。
- 6.11 手势回归：在非 key 与 key 浮窗中，对定时、传输、模式、分页、收藏、滑杆、卡片、滚动与输入各连续双击 100 次；控件只执行自身动作，窗口不收起。只有外侧 14pt 真实空白边缘和显式拖动把手的双击可以切换窗口状态。
- 6.11 控制栏回归：宽窗口中定时与播放模式必须贴在控制栏右端，弹性空间仅位于音量之后；顺序循环、随机、单条循环分别为 `repeat`、`shuffle`、`repeat.1`，不出现图钉；开启 15/30/60 分钟时图标和文字均不得贴住胶囊边缘。
- 6.12 快捷键回归：验证显示/隐藏与展开/收起默认键不同；真机覆盖 PulseDock 非前台、全屏 Space、菜单栏常驻、隐藏、小窗和展开六种状态。设置冲突键时必须显示失败且不得创建第二个窗口。
- 6.12 天气策略回归：约 2 公里内的行政区漂移不得更新，跨区的真实移动应更新；固定模式下重启、切换 Wi-Fi/Clash 不改变地点，定位失败保留旧天气。
- 6.14.1 定位可靠性回归：`locationUnknown`、短时超时和精度不足均不得清空原地点或天气；自动模式仅开启最长 20 秒的短时定位会话，随后按 30 秒、2 分钟、10 分钟重试并停止本轮重试。定位服务关闭、权限拒绝或受限制时不得循环请求；手动选城、取消、反向地理编码迟到回调和陈旧/超过 5 km 精度的位置均不得覆盖当前选择。设置页主文案不得显示 `kCLErrorDomain` 或原始错误码；公网 IP/代理/Clash 节点不得参与定位。
- 6.12 音频策略回归：HTTP 仍在播放器前阻止；HTTPS 首包失败分别映射 TLS、DNS、连接、超时和流中断；失败来源冷却五分钟且最近成功来源优先。智能模式必须保留系统失败与直连失败两段证据。
- 6.12 AI 诊断回归：普通 TLS、ChatGPT、OpenAI API 均执行三轮采样，报告包含中位数、范围和成功轮数；单轮异常不得单独判定为服务不可用。
- 6.12 GUI 压力回归：自动测试对 SwiftUI 内容区与原生按钮执行 100 轮连续双击命中断言；发布前仍需真机逐项双击设置、快捷键选择、定位、音频、分页与滑杆。
- 6.12.1 版本标识回归：展开窗口左上角显示 `v<CFBundleShortVersionString>`；窄窗口不得挤压时间和收起按钮，SwiftUI 源码不得硬编码具体版本号。
- 6.12.2 局域网路由回归：同子网直接放行；`192.168.31.0/24` 经活动 `en0` 专用路由到 `10.33.48.138` 应放行；只有 `default` 路由或 `utun` 路由不放行；无专用路由但 SSH 端口 2 秒内可达可作为补充证据。
- 6.12.3 快捷键回归：自定义键码/修饰键持久化往返一致，旧六项值可迁移；录制时旧热键不触发；相同、无效或被占用的组合必须回滚；`Esc` 取消后恢复注册。
- 6.12.3 焦点回归：点击天气城市框显示蓝色焦点环、光标及 IME marked text；点击搜索、位置分段控件、卡片空白或其他设置后焦点环和光标消失，文本保留。
- 6.13.1 额度回归：工作台显示可多选，小窗口置顶始终为 0 或 1 项且不出现 `+N`；Cursor 摘要为综合剩余百分比。冷启动未解锁时 Codex、Cursor、Clash 与 API 额度均为空且不执行读取，解锁一次后才刷新。
- 6.13.1 局域网回归：`route -n get` 返回带 `WASCLONED`/`CLONING` flags 的默认网关派生主机路由时，必须判为非专用路由；G1-002 不在附近时进入 `expectedOffline`，不探测、不告警。
- 6.13.2 出口卡回归：首屏只显示国家与城市，不出现大号 IPv4/IPv6；必须明确“经系统代理观测”或“直连观测”。展开详情可查看并复制地址、确认地址族、服务商和查询时间，刷新不得改变天气地点或局域网设备范围。
- 6.13.2 紧凑额度回归：保险库解锁后，平衡小窗口同时保留最多一个置顶 API/Codex 额度与独立 Clash 剩余额度；Clash 不占置顶名额。未解锁时两者均不得出现。
- 声音视觉与控制回归：在 0%、3%、34%、35%、80% 间连续拖动音量，播放器外框、窗口 frame、定时和播放模式控件不得跳动；静音后应为真实 0，恢复回到上一次可听音量。至少核对雨、海浪、森林、壁炉、咖啡馆、列车、城市夜景与棕色噪音八种场景，具象声音不得回退为同一频谱模板；暂停和“减少动态效果”必须为静态画面。
- 声音搜索是发布阻断项：必须分别用系统简体拼音和英文输入法，在应用原本非活跃、菜单栏/Dock恢复、环境音/音乐/广播切换及播放状态刷新期间完成输入、选词、回车、`⌘A/C/V/Z`；三个页面均须持续显示插入光标。只验证ASCII或程序调用 `makeFirstResponder` 不算通过。
- 声音分页人工回归：每类页面固定 2×3 卡片；下一页不能依据过滤后的卡片数量错误禁用；切换浏览分类不得改变正在播放条目、播放模式或播放器皮肤。
- 环境音“发现”分类人工回归：分类必须是内容宽度、左对齐、自动换行的紧凑标签；不得恢复为两列等宽长条。至少在 500 pt 常规宽度和窄展开宽度下检查“全部”、选中态及搜索框首屏位置。
- 远程状态回归：SSH/GPU 采集成功、Codex 端点未返回 HTTP 状态时应显示“资源采集正常 + 独立端点诊断”，不得呈现为整台设备部分异常或触发告警。
- 智能兼容人工回归需在 Clash 全局代理下验证系统路由失败后可有限回退、播放器显示实际路由、停止后本机随机监听消失；不得以关闭 ATS 或代理作为通过条件。
- 设备快捷设置必须能把鼠标移入并完成网络范围、置顶和任务隐私选择，面板不得在移动过程中消失。
- 工作音乐/广播必须逐条显示第三方来源；网络收音机“信号”只能描述缓冲状态，不能伪装为 FM 信号强度。
- 自动测试不能证明菜单栏可见、多屏拖动、真实音源地区可达或 Keychain 系统弹窗不存在；这些必须在测试报告中明确保留为人工项。
- 发布前记录至少 10 分钟界面响应压力测试：主线程延迟 P95/P99/最大值、CPU/RSS、丢失/重复点击数以及采样堆栈。出现无法解释的 >500 ms 停顿时不得发布。
