# PulseDock 发布检查清单

## 平台范围

- [ ] 根目录 `VERSION` 与 `Resources/Info.plist` 一致。
- [ ] 本次只构建、测试和发布 macOS。

## 代码与数据

- [ ] 用户需求已写入 `BACKLOG.md` 或本次版本范围。
- [ ] 版本号、构建号和安装包名称一致。
- [ ] 数据模型向后兼容；新增持久化字段有默认值或迁移策略。
- [ ] Keychain、安全信息和配置导出边界未被破坏。
- [ ] 冷启动、页面切换和后台刷新不会访问 Keychain；秘密只在显式操作后读取/保存。

## 自动化测试

- [ ] 执行 `./scripts/test.sh` 并全部通过。
- [ ] SSH/GPU 相关修改执行至少一个授权设备的只读冒烟测试。
- [ ] SSH 监控参数包含 `ClearAllForwardings=yes`，不继承别名中的端口转发。
- [ ] 执行 Release 构建，无编译错误；新增警告已处理。
- [ ] `plutil`、`codesign --verify` 和 ZIP 完整性通过。

## 人工交互

- [ ] 仅外侧真实空白边缘和显式拖动把手的双击可在紧凑/展开之间切换；完整内容画布内的任意交互只执行自身动作。
- [ ] 紧凑和完整态的非交互背景均可拖动；按钮、输入框、Slider、菜单、链接、滚动条与可选文本不移动窗口。
- [ ] API、飞书、天气、Clash、SSH 输入框可输入并支持 `⌘A/C/X/V/Z`。
- [ ] 工作音乐与广播搜索框均有可见插入光标；系统简体拼音 marked text、候选提交、回车搜索和英文输入正常。
- [ ] 语言切换后，导航与设置中的已本地化文案立即变更；动态诊断原文保持可读。
- [ ] “使用当前位置”仅在点击后触发系统定位授权，拒绝后不会覆盖手动城市。
- [ ] 手势图标按住可拖动、短按不误拖、双击仍切换紧凑/展开。
- [ ] 浮窗可拖动；菜单栏与 Dock 两种收起路径均有可恢复入口。
- [ ] 时间线首次打开无明显冻结，继续加载更早事件正常。
- [ ] SSH 卡片的紧凑、展开、刷新、复制诊断和任务模式状态一致。
- [ ] 局域网 SSH 目标为私有 IP，且满足同子网、活动物理网卡专用路由或 2 秒端口可达之一时允许探测；只有默认路由、VPN 虚拟网卡或公网目标时不得冒充局域网证据。
- [ ] 设备卡能直观看到自动/局域网/VPN/公网范围，切网期间不制造时间线/飞书告警风暴。
- [ ] 设备/API 置顶和上下移动后重启仍保持顺序。
- [ ] 设备快捷设置 popover 在鼠标移动到网络范围、任务隐私与排序项时保持可交互，不提前关闭。
- [ ] GLM 连接器不会调用模型补全；Cursor 模式不保存 token，真实只读测试只输出状态。
- [ ] Codex 多窗口名称、已用/剩余和完整刷新日期可区分；Token 活动不可用不影响额度卡。
- [ ] 声音页默认静音；三种媒体可区分；网络流仅点击后连接；停止/退出无残留音频；来源与许可可打开。
- [ ] 音量在 0/3/34/35/80% 与静音/恢复间切换时，窗口和播放器布局不跳动；窄宽度下按既定两行布局显示，不截断控制。
- [ ] 环境场景至少人工核对雨、海浪、森林、壁炉、咖啡馆、列车、城市夜景和棕色噪音；具象场景有对应构图，暂停/减少动态效果为静态且不持续高刷。
- [ ] 声音翻页、收藏恢复和播放模式语义正确；顺序/随机/单条循环对应 `repeat`/`shuffle`/`repeat.1`，不显示图钉；智能兼容显示系统/直连路由，停止后 loopback 中转端口消失。
- [ ] 广播“大陆中文”仅为 CN；港澳台、全球中文独立并在不足六条时完成普通话/粤语与后续批次回填；工作音乐搜索不出现 FM/新闻/谈话广播。
- [ ] 非 key 浮窗连续操作定时、模式、分页、收藏、滑杆不触发全局收起；右键仍可设置自定义定时与查看模式菜单。
- [ ] `⌥ Space` 在隐藏/可见之间切换，`⌥ ⇧ Space` 在小窗/展开之间切换；隐藏时分别召唤小窗/完整窗口，快捷键冲突有明确提示。
- [ ] 两个快捷键录制框可接受用户组合；录制时旧热键不触发，`Esc` 取消，冲突时显示候选组合和原因并保留旧值。
- [ ] 天气城市及其他原生输入框仅在编辑时显示蓝色焦点环与光标；点击任意非输入位置后结束编辑。
- [ ] 天气固定模式不会因网络或代理变化跳城；自动模式在跨越约 2 公里且行政区变化后更新，漂移和定位失败保留原地点。
- [ ] 音频首包预检有界完成，失败类别与系统/直连路径证据可见；失败来源冷却、成功来源优先，不出现无限重试。
- [ ] AI 诊断报告包含三轮采样的中位数、范围和成功轮数；可达但慢使用黄色性能状态，不误报服务不可用。
- [ ] 中文目录显示来源组成；补充语言索引和本机收藏不得放宽 HTTPS、国家、语言、编码与公开地址校验。
- [ ] 展开窗口左上版本胶囊与 Info.plist 一致，且不会挤压时间、菜单和“收起”按钮。
- [ ] Clash Verge 使用 Unix Socket 时能验证版本并同步 provider；任意 socket 路径被拒绝。
- [ ] 完全退出旧版后启动新版，拒绝/不可读旧凭据不会产生连续钥匙串密码框；基础监控仍可用。

## 文档与交付

- [ ] 更新 `CHANGELOG.md`。
- [ ] 更新 `README.md` 当前版本摘要。
- [ ] 更新 `outputs/PulseDock使用文档.md`。
- [ ] 生成与当前版本号一致的 `outputs/PulseDock-<版本>测试报告.md`。
- [ ] 生成与当前版本号一致的 `outputs/PulseDock-<版本>发布说明.md`，并写入最终 ZIP SHA-256。
- [ ] 更新 `BACKLOG.md`，已完成项移入更新日志，未完成项保留。
- [ ] 若测试流程或安全边界变化，同步更新 `TESTING.md` / `SECURITY.md`。
- [ ] 最终回复提供 App、ZIP、用户手册和更新日志的可点击路径。
