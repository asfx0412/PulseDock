# PulseDock 待开发清单

> 规则：只在一组需求的范围、交互与验收标准足够清晰后再进入版本开发；本文件不是已交付功能。

## 6.14.1 已交付（macOS 天气定位可靠性）

- 定位从一次性 `requestLocation()` 改为仅在用户触发或自动检查时开启、最长 20 秒的短时更新会话；成功、取消、超时、权限拒绝后立即停止，不进行持续后台追踪。
- `locationUnknown`、短时超时与精度不足被视作临时状态：保留最后有效地点与天气，按 30/120/600 秒有界退避自动再试，完成一轮后回到常规检查。
- 系统定位服务关闭、授权拒绝或受限制是非重试状态；界面提供可理解的处理方向，底层 Core Location 错误码仅保留在帮助/诊断信息。公网 IP、系统代理与 Clash 节点仍完全不参与天气位置。
- 定位会话、反向地理编码、手动城市选择和自动重试均受请求代际约束；陈旧（超过 30 秒）或精度超过 5 km 的位置不得修改天气城市。

## 6.14.0 已交付（待 GitHub 在线更新端到端验证）

- Codex、天气和 API 连接器统一保存刷新状态与最后成功快照；短暂失败保留旧值并按 30/120/600 秒退避，保险库锁定取消额度/API 重试。
- Codex 等待成功 `initialize` 响应（最多 12 秒）后才发送额度读取；工作台失败采用可展开提示与立即重试，连续失败三次才升级。
- Cursor 设置页显示 Auto/指定模型明细，工作台以综合剩余摘要显示。
- 更新器签名范围含发布说明，增加严格 URL/版本/SHA、Bundle ID、arm64、代码签名和启动确认回滚；GitHub Secret 配对与真实在线升级仍是正式发布阻断项。

## 下一版本 P0 · 数据读取可靠性、自动恢复与可处理提示（2026-09-01 已确认）

### 问题与根因

- 当前 Codex、天气及部分 API 读取失败后，会以新的失败快照直接覆盖工作台上的最后有效值；用户只看到“`--` · 刚刚更新”，既误以为数据真的刚刷新，也无法判断应等待、检查网络还是重新登录。
- Codex 读取器每次冷启动本机 `codex app-server`，但只等待固定 2 秒就发送 `account/rateLimits/read`。新版 app-server 在模型刷新、网络波动或启动负载下可能超过 2 秒；初始化尚未完成时提前送出的请求会被忽略，随后被 12 秒等待误判为“额度不可用”。实测同一账户在等待初始化成功后可返回额度与重置券，因此这不是账户失效。
- 天气端点在瞬时 DNS、代理、TLS 或网络故障后，会直接变为未配置样式，下一次常规 30 分钟周期前没有恢复机会。Open‑Meteo 请求本身可达，但 TLS 建连可能明显变慢，单次失败不能等同于天气不可用。
- 工作台目前将状态说明直接塞进副文案会挤压简略布局；而完全隐藏失败原因又让用户无从处理。Clash 本地订阅元数据仍可显示不代表系统代理、天气或 Codex 网络链路正常，三者必须独立判断。

### 统一状态模型

- 为 Codex、天气、API 连接器及后续可重试的数据源建立同一套展示状态：**正常、刷新中、临时失败、首次失败**。状态独立于业务值和刷新时间，不能以 `--`、空数组或“刚刚更新”猜测。
- 正常：显示最新值与常规更新时间；刷新中：保留最后成功值，不清空卡片；临时失败：保留最后成功值并记录本次失败；首次失败：此前从未成功，才显示 `--`。
- 锁定保险库是独立的“等待解锁”安全状态，不得被误归为网络/临时失败；解锁前仍不读取或显示额度，也不安排自动重试。
- 每个来源保存最近成功快照、最近成功时间、本次失败类别/时间、连续失败次数、下一次重试时间与刷新代际。过期旧值必须明确标为旧数据，不能伪装为最新结果。

### Codex 与天气自动恢复

- Codex 必须严格按 `initialize` 请求 → 收到确认响应 → `initialized` 通知 → `account/rateLimits/read` 的顺序执行。初始化等待改为有界的较长超时（建议 12 秒）；未收到确认时不得提前写入额度请求，并返回“初始化超时”而不是笼统“不可用”。
- 优先评估新版 `app-server daemon/proxy` 是否能安全复用；若不采用守护进程，冷启动必须串行、可取消、无残留进程，并通过上述握手确认兼容新版协议。
- 读取失败后自动采用退避重试：建议 30 秒、2 分钟、10 分钟；成功后清零失败计数并恢复 Codex/API 的 5 分钟、天气的 30 分钟正常周期。手动刷新可以立即触发一次新代际，但不允许并发启动多个同源请求。
- 天气失败不得覆盖最后有效地点与天气；天气首次失败显示“尚未取得数据，正在自动重试”。定位/反向地理编码失败继续沿用既定原子更新规则，不得用坐标或出口 IP 覆盖地点。

### 低干扰提示与用户处理

- 工作台保持简略：正常和刷新中只显示简短更新时间；已有旧值的临时失败不塞入长副文案，仅在相应卡片角落显示低干扰的橙色提示图标。
- 用户悬停或点击提示图标后显示 popover：失败类别（初始化、认证、DNS、代理、TLS、HTTP、解析等）、上次成功时间、下一次自动重试倒计时、最近一次安全摘要，以及“立即重试”按钮。不得显示 Token、API Key、完整 URL 查询参数或系统敏感路径。
- 连续失败达到阈值（建议 3 次）才把提示升级为明显状态，并提供“查看诊断”与按来源适配的处理入口；一次失败、后台自动重试和成功恢复均不弹系统通知、不打断工作。
- 成功刷新立即清除临时失败提示和倒计时；若用户点“立即重试”，卡片显示刷新中但保留旧值，失败后重新按退避计划计算。

### 验收标准

- 模拟 Codex 初始化在 3–8 秒后才返回：读取器必须等待确认后获得额度，不能提前发送并显示 `--`；模拟初始化超时必须进入可解释、可重试的首次/临时失败状态。
- 曾成功读取 Codex 64%、天气 26°C、API 余额后模拟 DNS、代理拒绝、TLS 超时、HTTP 5xx 与解析失败：主值保留，卡片出现提示图标，popover 显示上次成功时间、原因与下一次重试；成功后恢复“刚刚更新”。
- 首次启动且无成功快照时，只有此时可显示 `--`；副提示说明正在自动重试，并能在不重启 App、不手动刷新下于网络恢复后自动填充。
- 验证 30 秒、2 分钟、10 分钟退避及成功重置；重复点击“立即重试”、切换页面、工作台/小窗同时出现时，每个来源最多一个在途读取。
- 验证保险库锁定、解锁、重新锁定全过程：锁定不显示旧额度、不网络重试；解锁后只触发一次读取；失败提示不泄露额度、密钥或旧快照。
- 工作台正常状态不增加长文本；提示 popover 可键盘访问、可关闭，窄窗口不遮挡主要刷新/配置控件。连续三次失败才升级，恢复后不残留告警或系统通知。

## 下一版本 P0 · 免费 GitHub Releases 自动更新闭环（2026-08-31 已确认）

### 当前基础与实施前审计

- 仓库当前已有 `AppUpdateService`、启动后及每 2 小时检查、菜单栏“检查更新…”，以及 Ed25519 密钥生成、签名清单生成和 `.github/workflows/release.yml` 草案；这些只能视为**未验证代码骨架**，在真实 Release 与旧版升级测试通过前不得写成已交付功能。
- 实施第一步必须逐项核查现有代码，而不是另起一套更新器：确认客户端公钥来源、语义版本比较、并发检查去重、后台静默失败、用户确认弹窗、下载限制、ZIP 解包、Bundle ID/版本校验、替换回滚与重启路径。
- 核查 GitHub 仓库 Actions Secret 中是否存在 `PULSEDOCK_UPDATE_PRIVATE_KEY_BASE64`。Secret 列表只能证明名称存在，不能证明值与客户端公钥配对；必须通过一次受控签名清单或真实测试 Release 完成公私钥配对验证。任何日志、产物、Issue、文档和提交都不得输出私钥。
- 修正文档与代码的频率漂移：统一为“已安装 App 每 2 小时后台检查一次”；菜单栏始终允许用户主动“检查更新”。后台检查失败保持安静，用户主动检查失败才显示可理解的错误。

### 客户端范围

- 已安装 App 启动后按持久化的上次检查时间节流，每 2 小时后台获取一次固定 HTTPS 地址的 `latest-macos-arm64.json`；禁止每次窗口显示、页面切换或网络波动都重复请求。
- 菜单栏提供“检查更新…”。无新版时仅对用户主动检查显示“已是最新版本”；发现新版时展示版本号、可信发布时间和更新说明，只有用户明确确认后才下载 ZIP。
- 下载前后执行分层校验：清单必须通过内置 Ed25519 公钥验签；更新 ZIP 必须匹配清单 SHA-256；下载地址必须为允许的 GitHub Releases HTTPS 域与预期仓库；解包后必须核对 `PulseDock.app`、Bundle ID、目标版本和 arm64 架构。
- 签名载荷必须覆盖所有会影响用户决策或安装的字段，包括版本、下载地址、SHA-256、发布时间和更新说明（或更新说明摘要）；不得出现“核心字段已签名、但弹窗说明可被替换”的缺口。JSON 采用固定规范化载荷，客户端与发布脚本共用 fixture 验证字节完全一致。
- 安装器在退出当前进程后原子替换旧 App，保留可恢复备份；替换、启动或新 App 自检失败时自动恢复旧版本。成功启动新版后再清理备份和临时目录，不得留下私钥、更新 ZIP 或可执行脚本。
- 更新 App 不依赖 Apple Developer Program；界面与文档必须明确：Ed25519 只证明 PulseDock 发布者签名和包完整性，不等同于 Apple Developer ID、公证或 Gatekeeper 信任，首次启动及系统策略变化后仍可能出现 Gatekeeper 提示。

### GitHub Actions 发布流程

- 仅在推送与根目录 `VERSION` **严格匹配**的 `v<版本>` tag 时发布；不匹配立即失败，不创建半成品 Release。
- macOS runner 执行完整 `./scripts/test.sh` 与 Release 构建，核对 `VERSION`、Info.plist、ZIP 文件名、Bundle 版本、Bundle ID、arm64 架构、ad-hoc 签名和 ZIP 完整性。
- workflow 从 GitHub Secret `PULSEDOCK_UPDATE_PRIVATE_KEY_BASE64` 只读加载私钥，用它签署 `latest-macos-arm64.json`；私钥不得写入工作区持久文件、缓存、Artifact、Release 或日志。缺失、Base64 无效或与客户端公钥不配对时发布失败。
- Release 同时上传 `PulseDock-<版本>.zip` 与 `latest-macos-arm64.json`。清单下载 URL 必须精确指向同一 tag 的 ZIP，SHA-256 必须在最终 ZIP 生成后计算；Release 更新说明与签名清单说明来自同一份受控输入。
- workflow 发布前用仓库中的客户端公钥反向验证刚生成的签名，并重新校验 ZIP SHA-256；验证失败时不得调用 `gh release create`。

### 端到端验收与发布阻断条件

- 建立一对仅用于测试的 Ed25519 fixture，覆盖：有效清单、字段被篡改、更新说明被篡改、错误公钥、错误 SHA-256、非 GitHub/非 HTTPS URL、超大文件、错误 Bundle ID、错误版本、非 arm64 App 和损坏 ZIP；除有效样本外必须全部拒绝安装。
- 进行至少一次真实 GitHub Actions 测试 Release：从已安装旧版启动，等待/手动发现新版，查看说明，确认下载，完成验签与哈希校验，替换旧 App、自动重启并显示新版本；同时确认旧设置、保险库状态策略和用户数据不丢失。
- 分别验证“稍后”、无网络、下载中断、磁盘不足、App 所在目录不可写、替换失败、新版无法启动和 Gatekeeper 阻止等路径；失败必须保留或恢复可启动旧版，不能留下两个运行实例。
- 验证 2 小时节流跨重启有效，连续点击“检查更新”不会并发下载；后台失败不弹窗打扰，手动失败提供错误原因和重试入口。
- GitHub Secret 配对验证、完整 Release 构建、签名/哈希反向验证及旧版→新版端到端升级任一未通过，自动更新功能都不得标记完成或进入正式 Release。
- 发布完成后同步更新 `docs/AUTOMATIC_UPDATES.md`、README、用户手册、测试报告、发布说明与 `RELEASE_CHECKLIST.md`；文档中不得再保留“24 小时检查”或“尚无自动更新”等与实际版本冲突的描述。

## 下一版本 P0 · 设置页额度详情与工作台摘要分层（2026-08-31 已确认）

### 目标与页面职责

- **设置页负责详细查看与配置。** 每个额度来源完整展示官方返回的额度组成、周期名称、已用/剩余百分比、进度条、重置时间、更新时间、数据来源与认证方式；不能为了与工作台统一外观而只保留一个综合百分比。
- **工作台负责快速扫读。** 每个用户勾选的来源只显示名称、一个主要剩余值、更新时间和刷新入口，不重复设置页的多窗口进度条、认证信息或长说明。
- 两处必须复用同一份额度快照与刷新状态，只改变展示密度；不得为了生成工作台摘要再次请求接口，也不得让两个页面出现数值不一致。

### Cursor 与多周期来源

- Cursor 设置卡恢复详细的两部分额度：分别展示 Auto 与指定模型用量的已用/剩余比例；如果本地接口还能稳定提供综合值，可作为卡片摘要，但不能取代这两项明细。
- Cursor 工作台卡只显示一个“综合剩余百分比”。综合值沿用既定、可审计的换算规则，Auto 与指定模型的长文本不进入工作台。
- GLM、Codex 等多周期来源在设置页保留官方返回的全部周期/窗口；工作台仍只显示该来源最受限周期的剩余百分比，并在副文案中说明“最低剩余额度周期”。
- DeepSeek 等余额型来源显示官方余额和币种，不伪造百分比；没有公开额度详情的来源明确显示“端点可达，但未公开剩余额度/速率限制”。

### 布局与交互

- 设置页的明细区允许纵向增高或折叠，但每个周期保持一致的信息结构；Key 输入与“保存并刷新”仍属于连接配置区，不与额度进度条混为一行。
- 工作台卡保持当前紧凑双列布局；长名称截断时提供悬停完整名称，主要值不得被刷新按钮挤压。
- 小窗口规则不变：最多一个用户置顶的 API/Codex 摘要，同时保留独立 Clash 剩余额度；设置页的详细窗口数量不得扩散到小窗口。

### 验收标准

- Cursor fixture 为 Auto 已用 10%、指定模型已用 21% 时：设置页分别显示两条明细及各自剩余 90%/79%，工作台只显示一个综合剩余值，小窗口只在 Cursor 被置顶时显示同一个摘要值。
- GLM fixture 含月度、短周期窗口 1、短周期窗口 2 时：设置页三项全部可见，工作台只显示最低剩余 43%；两处更新时间和刷新状态一致。
- 锁定保险库后，设置页、工作台和小窗口都不得保留可见额度；解锁后一次读取应同时驱动三处恢复，不产生重复请求。
- 对 0、1、3、6 个工作台来源验证卡片高度与双列对齐；工作台不得重新出现设置页式的大块多进度条。

## 6.13.2 已交付 · 公网出口语义与 Clash 紧凑额度回归（2026-08-31）

- “当前出口”首屏只显示国家和城市，不再把长 IPv6/IPv4 当作主标题；原始地址、地址族、完整归属、服务商与查询时间收进可展开详情，并支持复制地址。
- 出口请求路径明确显示为“经系统代理观测”或“直连观测”。该信息仅用于网络观察，不参与天气位置、G1-002 网络范围或其他位置判断。
- 平衡小窗口恢复 Clash 独立剩余额度：布局为“番茄钟 + 最多一个用户置顶的 API/Codex 额度 + Clash 剩余额度 + 天气”。Clash 不占置顶名额；保险库未解锁时仍不展示。
- 自动回归新增两条发布阻断规则：出口卡不得以原始 IP 作为大标题；小窗口源码必须保留独立 Clash 剩余额度。

## 6.13.1 已交付 · 额度置顶语义、统一解锁门禁与局域网静默修复（2026-08-31）

- “工作台显示”支持多选，与“小窗口置顶”彻底分离；小窗口最多显示一个用户选择的额度来源，不显示第二项和 `+N`。
- Cursor 使用综合已用百分比换算综合剩余百分比作为摘要，详细分类不再撑宽工作台或小窗口。
- 凭据保险库成为本次运行所有额度源的统一门禁；解锁前 Codex、Cursor、Clash 和 API 连接器不读取、不展示，解锁一次后按原刷新周期运行。
- G1-002 的仅局域网判断拒绝 `WASCLONED/CLONING` 默认路由派生项；这种路由不再证明设备位于附近网络，SSH 探测转为灰色预期离线并静默。
- 发布流程恢复生成版本测试报告与发布说明，并在发布检查清单中列为每版必备产物。

## 6.13.0 已交付 · 常驻额度汇总与可置顶连接器（2026-08-31）

### 目标与边界

- 在“额度连接器”中统一管理 Codex、GLM、Cursor、DeepSeek 与后续兼容来源；用户可将任意额度来源标记为“常驻显示”，并按自己的顺序排列。
- 常驻来源完整显示在工作台的“常驻额度”区域；平衡小窗口因宽度有限，只显示排序靠前的两项与 `+N`，点击额度组可回到完整列表。置顶不是告警优先级，也不会额外触发网络请求。
- “汇总”仅指将多个来源集中展示，**绝不**把百分比、金额、Token、请求数或订阅流量求和、平均或伪造为一个总百分比。Clash/良心云是流量订阅，继续保持独立卡片，本版不混入 AI 额度汇总。

### 数据模型与刷新策略

- 保留 Codex、GLM、Cursor、DeepSeek 各自现有的读取服务作为唯一事实来源；新增纯展示适配层，统一输出来源名称、主要指标、单位、状态、更新时间、最近成功时间与来源/认证说明。不得把 Codex 复制进通用 API 快照，避免重复读取 `app-server`、状态漂移或双份卡片。
- Codex 增加为内建“Codex 本机官方额度”连接器：只映射既有本机 `app-server` 的只读额度快照，刷新时复用现有 Codex 刷新动作；无 PulseDock API Key，不读取、导出或刷新 Codex 登录 Token，不能重复添加或删除。
- 百分比型来源（Codex/GLM/Cursor）仅在各自内部取“最受限周期”作为常驻摘要，展开仍可查看全部周期；DeepSeek 显示官方余额与币种；请求数、Token 或文本型来源保留原单位。只有明确百分比的来源才可参与低额度判断。
- 常驻、取消常驻和排序只持久化布局偏好，不改变现有五分钟刷新节流，不增加后台请求。设置页、工作台和小窗口必须共享同一份已读取快照。

### 状态、保险库与交互

- 将“保险库已锁定、尚未请求”从错误/不可用中分离为中性灰色“等待解锁”。GLM、DeepSeek 在该状态下不得发出任何网络请求；Cursor 本机会话与 Codex 本机 `app-server` 不依赖 PulseDock 保险库，可继续按原权限读取。
- 请求失败后可保留上次成功数据，但必须标注“上次成功 · 未刷新”，并分别记录本次观察时间与最近成功时间；不得把旧数据伪装为刚刷新。余额型来源在没有用户阈值前不得被擅自标成低额度告警。
- 设置页标题改为“额度连接器”。每项明确显示数据来源和认证方式；Codex 显示“本机 app-server · 官方只读 · 无 PulseDock API Key”。菜单使用“常驻显示 / 取消常驻”，说明它会影响工作台与小窗口；内建 Codex 不显示 Key 输入框和移除动作。
- 小窗口不得因多个常驻项无限扩宽或挤压天气/番茄钟；无论来源处于加载、等待解锁、错误还是过期状态，常驻位置都保持稳定，不因短暂状态跳动消失。

### 迁移与验收

- 6.12.3 升级后自动、幂等地注入且只注入一个已启用、默认常驻的 Codex 内建连接器；既有连接器的 UUID、顺序、常驻状态和保险库 Key 映射不得变化。导出配置仅含非秘密的启用/排序/常驻偏好。
- 验证 0、1、2、6 个常驻项：工作台显示完整，平衡小窗口保持前两项加 `+N` 且不溢出，重启后顺序不变；Codex 88%、GLM 52%、DeepSeek CNY 10 时不得出现“合计 150%”或平均值。
- 锁定保险库时 GLM/DeepSeek 显示灰色等待解锁、网络调用数为零；Codex/Cursor 仍可读取；手动刷新或同时打开设置、工作台和小窗口时，Codex 只执行一次读取。低额度时间线与小窗口优先提示仍应指向具体来源。

### 已完成的执行闭环

- Sol 完成模型、迁移和验收边界审查；Terra 实现后，Luna 依次发现锁库状态、摘要指标、刷新状态、刷新与锁库竞态、保留 UUID 碰撞等边界，并均经过 Sol 定案、Terra 修复、Luna 复测。最终迁移、单位语义、保险库零请求、刷新代际、布局、完整测试与签名构建均已通过。

## 后续版本 P0 · 当前位置的地名完整性与天气原子更新（2026-08-31 已确认）

### 问题与根因

- 现有自动定位在 Core Location 返回经纬度、但反向地理编码失败时，仍然将 `当前位置 22.60, 113.99` 包装为成功的 `WeatherLocation`，然后直接写入当前天气地点和搜索框。因此地名丢失后被坐标覆盖，如果紧接着的天气请求失败，卡片便只留下不友好的坐标和“天气更新失败”。

### 解决方案

- 将定位结果拆分为“坐标可用”和“已解析的行政地名”两个明确状态。反向地理编码失败不再作为地名成功：坐标只能出现在诊断详情中，不得写入天气卡片主标题、搜索框或固定地点偏好。
- 采用两阶段提交：先获得“带行政地名的候选地点”，再为该候选地点取得新的天气快照；只有两者都成功时才一次性替换当前地点和天气。任一步失败都保留上次有效的地名和天气，仅显示非破坏性状态。
- 如果首次使用没有旧地点，显示“当前地点待解析”和“重试地名解析”，而不是把坐标伪装成城市。此时用户仍可选择手动搜索城市。
- 地名更新仍使用现有的跨越约 2 公里 + 行政区变化防抖条件；坐标漂移、反编码短暂失败或天气源暂时不可用都不得覆盖已确认的城市/区县/镇。
- **验收：**在已显示“南山区·深圳市·广东省·中国”后模拟反向地理编码或天气请求失败，卡片不得变为经纬度；屏幕上只能看到最后有效地名与可重试状态。

## 后续版本 P0 · 凭据保险库与连接器状态分层（2026-08-31 已确认）

### 问题与根因

- GLM 和 DeepSeek 是需要 API Key 的连接器。统一凭据保险库锁定时，PulseDock 有意不将它们的 Key 读入内存，所以不会发起官方额度请求。Cursor 则读取 Cursor 已登录的本机会话额度，不使用 PulseDock 的 API Key，所以在保险库锁定时仍可显示。这是有意为之的安全边界，不是 Cursor 突破了保险库。
- 现在界面把“保险库锁定、未发起请求”和“真正网络/认证/额度不可用”都划在“不可用”的视觉语义下，因此容易让人误以为 GLM 和 DeepSeek 出了故障。

### 解决方案

- API 连接器增加独立的“等待解锁”中性状态（灰色锁图标），不再继续用橙色“不可用”或红色故障语义。文案为“已暂停网络请求：需解锁保险库后才能读取 API Key”。
- 每张卡强制显示“数据来源 / 认证方式”：GLM、DeepSeek 显示“统一保险库 · API Key”；Cursor 显示“Cursor 本地会话 · 无 PulseDock API Key”。这只说明数据来源，不显示、不导出任何秘密。
- 在保险库锁定时，全局“刷新全部连接器”必须跳过需 Key 的连接器，并在卡片内提供明确的“解锁并刷新”操作。不自动解锁、不自动弹出钥匙串访问请求。
- 保险库锁定前已成功的用量快照可作为“上次成功数据（未刷新）”保留，但不得被标为当前数据；无快照则只显示等待解锁状态。
- **验收：**锁定保险库后，GLM/DeepSeek 是灰色“等待解锁”且无网络请求；Cursor 仍可显示本机会话数据，但卡片清楚说明其不使用 PulseDock 保险库；解锁后才会发起 GLM/DeepSeek 官方额度请求。

## 6.13.2 已交付 · 公网出口卡的语义与路径观测（2026-08-31）

- 卡片主视觉只显示**国家和城市**，例如“美国 · 洛杉矶”；不再将原始 IPv6 或 IPv4 地址作为大号主标题。IP 地址、地址族（IPv4/IPv6）、ASN/服务商、查询时间放入可展开、可复制的详情。
- 显示必须明确区分路径：“经系统代理观测”或“直连观测”。这只说明本次出口查询的请求路径，不宣称代表所有应用流量、当前 Clash 节点名称或用户的真实物理位置。
- 该卡定义为**网络观察数据**：绝不参与天气城市/区县、自动定位、G1-002 等设备的可信网络匹配、局域网判定、VPN 连接判定或任何自动切换。
- 对 IPv4 和 IPv6 分别保留地址族证据；仅查到 IPv6 时展示“IPv6 出口”而不视为异常。后续可以增加低频的双来源交叉校验；若结果不一致，明示“出口定位存在差异”，不伪装成精确定位。
- **验收：**IPv6 出口的卡片首屏仍不出现长 IP 串；用户一眼能区分国家/城市和“系统代理”/“直连”；切换代理或刷新出口不得改变天气地点，也不得让局域网设备从静默预期离线变成故障告警。

## 后续版本 P0 · 局域网设备的可连接环境与告警降噪（2026-08-28 已确认）

### 目标：“不在设备附近”是预期离线，不是 SSH 故障

- 将 G1-002 等只能在特定办公室/机器人网络附近访问的主机，从简单的“局域网 IP 判定”改为单独的**可连接环境策略**。设备在缺少该环境证据时必须显示“预期离线”，暂停 SSH 探测、首页红色横幅、时间线故障事件和飞书告警。
- **不得用默认路由派生的 host route 作为“身处设备网络”证据。**macOS 对 `route -n get <目标>` 可返回经默认网关派生的 `WASCLONED` 主机路由；它仅代表“会尝试交给默认网关”，不代表 `10.x` 目标处于当前局域网。

### 交互与配置

- 在每个上述设备的设置中提供“访问策略：公网 / VPN / 仅在可信局域网”。对“仅在可信局域网”显示可编辑的可信网络列表。
- 当用户已在机器人旁、且手动测试 SSH 成功时，提供“将当前网络设为 G1-002 可连接环境”的明确操作。保存网络指纹：优先 Wi-Fi SSID/BSSID，辅以默认网关、活动私有子网和可选的网域 DNS。不依据公网 IP、Clash 出口或单一目标 IP 反推当前位置。
- 当处于新网络但用户需要尝试时，提供“测试此网络”和“观察 2 分钟”。只有测试成功后才可加入可信网络；观察结束后自动回到静默策略。
- 当前网络不匹配时，设备卡使用中性灰色，文案为“预期离线：当前网络不是 G1-002 的可连接环境”，并显示上次成功采集时间。不使用红色“SSH 不可用”横幅。

### 状态机与验收

| 环境判定 | SSH 后台探测 | 告警行为 |
| --- | --- | --- |
| 可信网络匹配 | 执行 | 连接失败才告警 |
| 可信网络不匹配 | 不执行 | 预期离线，静默 |
| 新/无法判断网络 | 默认不执行 | 静默，可人工测试 |
| 用户明确观察期 | 执行 | 仅在观察期报告实时失败 |

- **验收：**G1-002 在机器人办公网外不再进行后台 SSH，首页不显示红色不可用横幅；回到已保存的可信网络后自动恢复探测，此时 SSH 失败才产生故障告警；默认路由产生的 `WASCLONED` 结果不得触发探测。

## 6.12.3 已交付 · 快捷键录制与输入焦点（2026-08-28）

- 两组全局快捷键保留默认值，但不再限制为预设列表；用户点击录制框后直接按新组合。录制、冲突检测、失败回滚、`Esc` 取消、持久化和旧值迁移均有明确状态。
- 所有 `FocusableTextField` 仅在 AppKit field editor 活跃时显示焦点环与光标；点击其他控件或内容背景必须结束编辑，不破坏中文 IME 组字。

## 6.12.2 已交付 · 跨子网局域网 SSH 判定（2026-08-28）

- 局域网范围从“必须同子网”扩展为三层证据：同子网直接放行；不同私网子网但存在经活动物理网卡的非默认专用路由时放行；否则只做 2 秒 SSH TCP 握手补充判断。
- 路由证据与设备在线状态分离：进入正确办公网但机器人关机时，应执行 SSH 探测并报告设备离线，不得误显示“当前网络暂停”。

## 6.12.1 已交付 · 界面版本标识（2026-08-28）

- 展开窗口左上品牌副标题旁显示低对比度版本胶囊；值读取应用包版本，不在 SwiftUI 中硬编码，便于从截图确认安装包和排查旧进程。

## 6.12.0 已交付 · 全局快捷键与窗口状态（2026-08-28）

- 新增两组可配置的全局快捷键，并在“设置”页提供录入、清除、恢复默认和冲突提示。默认建议为 `⌥ Space`（显示/隐藏）与 `⌥ ⇧ Space`（展开/收起）；若与 macOS 或已注册的应用快捷键冲突，必须提示用户并拒绝静默覆盖。
- **显示/隐藏快捷键：**隐藏时召唤为小窗；小窗或展开态时隐藏。不得执行不可见的额外状态跳转，也不得改变当前页面、正在运行的计时或音频。
- **展开/收起快捷键：**小窗与展开态之间切换；若窗口当前隐藏，直接以展开态召唤。这样避免把“隐藏 → 小窗 → 展开 → 小窗 → 隐藏”塞进一个难以预测的四态循环。
- 全局快捷键注册失败、被系统保留、设置项无效或失去注册时，设置页应明确显示原因，并保持旧的有效快捷键不受影响；应用退出时必须解除注册。
- **验收：**在 PulseDock 非前台、全屏应用前台、菜单栏常驻和窗口隐藏四种状态下，各快捷键均只触发其定义动作；连续按压不得创建多个面板、抢走正在编辑的文本或改变音频播放状态。快捷键录入本身、设置页控件和其他交互区域仍不得被双击收起逻辑抢占。

## 6.12.0 已交付 · 天气位置自动跟随（2026-08-28）

- 在设置中提供“固定地点 / 自动跟随当前位置”两种位置来源。默认保持固定地点；只有用户主动开启自动跟随并授予 macOS 定位权限后，才允许后台更新地址。
- 天气数据与位置检测使用独立周期：天气维持每 30 分钟自动刷新；自动跟随启用时，窗口可见或近期活跃期间每 30 分钟执行一次低精度单次定位，窗口隐藏且设备长期静置时降为每 2 小时一次，窗口重新显示时立即检查一次。
- 位置更新增加防抖：与当前坐标距离不足约 2 公里时不反向地理编码；超过阈值后，只有区/县或镇/街道确实变化才替换卡片地址，避免定位漂移造成地址来回跳动。
- 手动选择城市、区县或镇后，自动切回“固定地点”，确保手动选择始终优先；设置页显示当前模式、上次定位时间、定位权限/失败原因，并提供“立即检查”和“暂停自动跟随”。
- 自动跟随不得依据公网 IP、Clash 节点或出口国家推断位置；不得因定位失败清空现有天气。失败时保留最后一次有效地点和天气，并显示数据更新时间。
- **验收：**固定地点模式下重启、切换网络和代理均不得改变地址；自动跟随模式跨越约 2 公里且行政区变化后应在下一次检查更新，原地静置、GPS 漂移或仅天气刷新不得改地址；撤销定位权限后保留旧地点并明确提示，不反复弹出授权窗口。

## 6.12.0 已交付 · 候选项合并（2026-08-28）

- 音频已交付首包预检、失败来源五分钟会话冷却、最近成功来源优先与分阶段错误类别；智能模式保留系统路由失败后再尝试会话直连的证据。
- AI 网络诊断已交付三轮低频采样，以中位数、范围和可达轮数区分持续慢、偶发冷连接与不可达。
- 中文目录已交付语言补充索引、受限后续批次、本机已审核收藏补充和来源覆盖文字。独立第二公共目录因尚无许可与稳定性均满足要求的候选，保留适配边界但不接入来源不明网页流。
- GUI 自动回归已加入 100 轮连续双击压力断言；完整真机键盘冲突、全屏 Space、定位跨区和直播源长时间卡顿仍列发布人工验收，不能由离线测试冒充。

## 6.11.0 已交付（2026-08-27）

- 港澳台中文和全球中文改为按地区并发读取，并在中文标签不足时用普通话、粤语标签受限回填；每次检索仍通过国家、语言、HTTPS、公开地址与编码复核。若公开目录本身收录不足，明确显示实际可用数，不伪造候选。
- 播放器收敛为顺序循环、随机、单条循环三个标准图标；定时器增加胶囊内距，右侧控制组对齐，模式按钮之后不再产生无意义留白。
- 双击收起的范围收窄到外侧真实边缘空白及可见拖动把手；完整内容画布的任何交互元素都优先完成自身操作。
- **后续 P1：**为地区中文目录引入经审核的补充提供方适配器和来源覆盖率指标；只有在许可、可用性与安全审查通过后才接入，不以抓取网页代替公开目录。

## 6.10.0 已交付（2026-08-27）

- 广播已拆分为大陆中文（严格 `CN`）、港澳台中文和全球中文；工作音乐已排除 FM/谈话型目录结果。
- 播放器已完成单图标定时/模式、传输图标缩小及控件双击保护；播放失败保留系统与智能直连两段路径文字。
- 洞察已默认前 5 条并自动收起；天气定位保留区县/镇级反向地理编码；Token 图去除内部说明并进一步抽稀刻度；AI 可达但慢改为黄色性能观察文案。
- **仍待后续 P0：**播放首包预检、失败来源会话冷却、音频错误完整分类、三轮 AI 采样及完整 GUI 压力/连续双击人工验收。这些不因本版编译通过而视为完成。

## 6.6.0 已交付核心（2026-08-25）

- 声音搜索交付原生 AppKit 编辑桥、可见光标、中文输入法 marked text 保护与真实 `NSHostingView` 焦点测试；真机第三方输入法兼容仍列人工回归。
- Radio Browser 交付 `limit + offset` 每页 12 条、空结果正确结束、收藏最小元数据和音乐/广播播放模式；环境音扩充、完整收藏管理页和更深的三套视觉改版仍留在后续。
- 音频交付智能兼容、跟随系统、仅直连三种模式；会话级直连中转首期只支持公共 HTTPS MP3/AAC，HLS 安全改写、备用同台音源和更细 TLS 分类留在后续。
- GLM 交付请求单飞、字段演进解析、数字字符串、业务错误、响应边界和失败时旧快照保留；安全诊断 UI 与真实 429 长周期观察仍留在后续。
- Clash Verge 交付已知本机 Unix Socket 控制器发现；不修改 Clash 配置，不扫描任意 socket 或局域网。
- 设备设置交付可停留 popover，替代易消失的“网络范围”级联菜单；鼠标拖拽排序仍未交付。
- 6.7.2 交付局域网目标范围判断：通过 `ssh -G` 只读解析 HostName，并将目标私有 IPv4 与当前活动本地子网比较；范围外的局域网设备不再进入首页红色告警。设备卡已显示自动/局域网/VPN/公网标识。
- 6.8.0 交付后台串行系统指标采样、主线程响应延迟证据、即时定时按钮、首击/原生输入焦点收敛、Codex 图表日期抽稀、去重 Cursor 卡、可审计中国节假日月历、24 条 CC0 环境音与原始候选批次翻页判断。真实简体拼音/第三方输入法和 10 分钟 GUI 压力验收仍是发布后必须持续收集的实机证据，不得被离线测试替代。

## 下一小版本 P0 · 声音内容与控制台纠偏（2026-08-25 实机反馈）

### P0 新增 · 广播语义、播放器手势隔离与 AI 路径诊断（2026-08-26 已确认）

#### 广播分类必须说真话

- 用户已确认：**“本地中文”严格定义为中国大陆电台**，不得再用语言标签 `chinese` 代替地域筛选。Radio Browser 查询必须附加中国大陆的国家过滤并在本地再次校验国家元数据；不满足大陆条件的台不得混入该分类。
- 港澳台必须单独提供“港澳台中文”类别；“全球中文”可作为第三个明确命名的类别，允许海外中文台，但绝不能占用“本地中文”的名称。
- 目录源国家字段缺失、与筛选地域矛盾或无法可靠判断时，不进入“本地中文/港澳台中文”结果；宁可少，不把南极、塞浦路斯、新西兰等国际台伪装成大陆本地电台。

#### 播放器控件不能被全局双击抢走

- 现象：选择 15/30/60 分钟、分页、播放模式等操作后，第二击被 `NSPanel` 背景双击识别，错误收起为小浮窗。根因不是单个定时按钮，而是菜单关闭/SwiftUI 命中切换时，第二个 AppKit mouseDown 被错误归类为背景。
- 所有按钮、Menu/Picker、Slider、分页、收藏、播放控制与文本编辑器都必须建立统一的“交互事件保护期”；控件点击后的短窗口内，背景双击手势不得切换展开状态。全局双击只能在两次都命中真正的非交互背景时生效。
- 定时器改为一个可见计时图标与分钟状态：单击立即循环 `关闭 → 15 → 30 → 60 → 关闭`；自定义分钟移至右键/长按辅助菜单或明确的设置入口。移除其额外下拉箭头。
- 播放模式不再用“模式图标 + 下拉箭头”表达。保留单一可点击图标，并按单击顺序切换固定电台、单曲循环、列表循环、随机；图标与悬停说明同步变化。不得要求用户在普通使用中打开文字菜单。
- 上一条、播放/暂停、下一条、停止的**视觉符号**缩小约 20%，但保持原有或更大的命中框，避免变小后难以点击。

#### 音频失败需要可诊断、有限恢复，而非无限缓冲

- 保持 HTTPS 与系统 TLS 校验，不以关闭 ATS、忽略证书或改写系统/Clash 作为“修复”。
- **新增实机问题（2026-08-27）：**目录显示“安全 HTTPS 音源”只说明元数据通过 HTTPS/codec/公开地址过滤，不代表该直播流能在当前 Clash、直连 DNS 与出口节点路径上取得可播放首包。当前“智能兼容”先走系统路径、约 9 秒后切到仅直连；当直连 DNS 也不可用时，两条路径失败最终被压缩成“网络连接已中断”，用户无法判断是电台、代理还是回退自身失败。不得再把“安全 HTTPS”表述为“可播放”。
- 播放前增加有界、低成本的首包/响应预检，并把结果限定为“本次网络下可尝试播放”，不伪造长期可用性。目录排序优先最近成功播放的来源；失败来源在当前网络会话内进入短期冷却，不反复推荐或无限重建播放器。
- 增加“跟随系统 / 智能兼容 / 仅直连”的逐路径证据：系统路径首包、智能直连是否被触发、直连 DNS/连接结果、最终路径与失败阶段均可见。智能回退失败后必须保留原系统路径失败证据，而非覆盖为一个通用错误。
- 播放状态需明确区分：源站 TLS/证书失败、代理路由失败、DNS/连接超时、直播流中断、缓冲不足、HLS 当前不支持中转、智能直连失败。卡片显示最近一次分类结果和实际路径（系统/智能直连/仅直连）。
- “智能兼容”先走系统路径；在可安全中转的 MP3/AAC 流发生一次受限超时或连接失败时，自动尝试一次会话级直连。两条路径均失败后停止自动重试并标记“当前网络不可用”，避免 CPU、网络与告警噪声。
- 对短暂卡顿采用有界重缓冲而不是立即重复建 player；连续失败才显示失败。不能保证失效电台、地区限制或源站拥塞可被本机修复，必须如实说明来源层级。

#### AI 网络诊断改为“证据分层”，不把慢误称为坏

- 当前报告中普通网站 49 ms、ChatGPT 1940 ms、OpenAI API 2518 ms 都可达，且 `401` 证明 API 请求已经到达 OpenAI；这应标注为“AI 路径较慢，服务可达”，不是网络失败，也不能直接断言“Clash 节点不好”。
- 普通 Clash 节点延迟通常只测代理节点/测试目标，不能代表到 ChatGPT/OpenAI 的完整路径，因此与 PulseDock 的 AI 端到端耗时不同不矛盾。
- 诊断改为至少三次低频采样并显示中位数、离散度、DNS/TLS/首包/总耗时；单次冷连接只记录观察，不触发“切换节点”建议。连续多轮超过阈值才给出可选换节点建议。
- 报告分为“可达性”“AI 路径性能”“Codex 本地服务”三项；所有可达时使用黄色观察而非失败红色，并解释 `401/403` 的预期语义。

#### 洞察排行默认紧凑

- 应用活跃排行榜默认只显示前 5 条；底部提供“查看全部 · 共 N 条”。展开只使用已有内存排行数据，不重新请求、不影响活跃累计。
- 用户点击展开后，15 秒没有滚动、点击或切换今日/7 天/30 天时自动收回前 5 条；任何交互会重置计时。自动收起只作用于这张排行卡，不改变洞察页、滚动位置或时间范围。

#### 天气位置显示到用户选择的行政区

- 当前天气按经纬度查询已足够精细，但展示模型仅保留城市名称，因此会把实际位置统一显示为“深圳市 · 中国”。下一版将展示与选择的粒度解耦：用户可在手动搜索结果中选择市、区/县、镇/街道等可用层级；卡片优先显示“城市 · 区/县”，空间足够且地理编码可靠时可显示镇/街道。
- “使用当前位置”只在用户主动点击后请求 macOS 定位权限，再用坐标执行反向地理编码；若系统或地理服务只返回城市级结果，必须诚实显示城市，不猜测区/镇。手动位置始终优先，可一键恢复到城市级。
- 天气位置继续与公网 IP、Clash 节点、出口国家完全解耦；不因代理切换而自动改变城市、区县或镇。

#### Codex Token 图只保留有效信息

- 移除额度/Token 卡片中“各窗口独立显示，不再使用 #1/#2”这类内部实现说明。它不能帮助用户判断额度、刷新时间或 Token 活动，且挤占图表空间；若需要解释多窗口，放入悬停帮助或使用文档，不默认显示。
- Token 图已经支持悬停查看完整时间与 Token 数，但 X 轴两端日期仍可能被裁剪、遮挡或重叠。下一版以实际 plot area 计算最多 3–4 个稀疏刻度，为首尾预留内边距；窄宽度时自动只显示首/中/尾或隐藏轴文字，绝不截断为“08/…”。
- 完整日期、精确 Token 数及窗口/周期信息只在悬停或点选柱子后的详情中显示。图表默认只承担趋势，不堆叠说明文字。

#### 验收标准

1. 选择“中国大陆”时，返回项目国家字段均为 China/CN；港澳台与全球中文不会混入。没有可信地域字段的台不显示在地域类结果中。
2. 对定时、模式、分页、收藏、滑杆、播放/停止各连续双击 100 次：对应控件仅执行其本身动作，窗口不得收起；真正空白背景双击仍能可靠收起/展开。
3. 单击模式图标即可看见固定/单曲/列表/随机图标切换；定时器无重复下拉箭头，15/30/60/关闭均即时生效。
4. 对模拟的 TLS、连接断开、缓冲、HLS、直连失败和系统路由成功样本，状态文字、自动回退次数和最终建议均可确定性验证；不得无限重建播放器。
5. 对 49 ms 普通网站、1.9–2.5 s OpenAI 的样本，报告显示“可达但 AI 路径慢”；只有连续多轮慢样本才建议切节点。`401/403` 不得被标记为连通性失败。
6. 排行默认恰为五条；展开后可看全部，15 秒无交互自动恢复五条，不改变当前筛选和排行数值。

### P0 发布阻断 · 全局点击偶发延迟/无响应（2026-08-25 实机采样）

- 用户实测声音页定时按钮以及其他控件偶尔需要等待后才响应。不得继续把它作为单个按钮视觉问题修补；该现象定义为“输入事件到动作执行的端到端延迟”，需要覆盖窗口非激活首击、主线程停顿、手势仲裁和控件禁用状态。
- 已确认的确定性根因：定时控件使用 `TapGesture(count: 2).exclusively(before: TapGesture(count: 1))`。为了判断第二次点击是否到来，SwiftUI 必须延迟单击回调到系统双击识别窗口结束，因此该按钮不可能即时响应。这不是用户操作问题。
- 2026-08-25 对正在运行的 PulseDock 6.7.2 (672) 做只读进程采样：首个进程快照约 15% CPU、123 MB RSS，稍后回落至约 2.9% CPU、100 MB RSS；5 秒堆栈中主线程 878 个样本有 844 个处于事件等待，说明没有持续性死锁。主线程工作样本主要落在 `MonitorStore.sampleMetrics → HardwareTemperatureReader → PDReadChipTemperature`，后台 SSH `Process.wait` 位于 utility 线程，并未直接堵塞 AppKit 主事件循环。
- 已确认的结构风险：芯片温度 HID 服务枚举每 2 秒在主线程执行；`MonitorStore` 每秒发布 `now` 并重算活跃排行，每 2 秒连续发布 CPU/内存/GPU/网速/温度等多个字段，每 30 秒在主线程原子写活动 JSON。任一单项通常较短，但可能和 SwiftUI 大页面重算、窗口重新激活或点击同帧叠加，形成间歇性停顿。
- 本次长采样没有捕获用户描述的数秒级 hang，因此不得在没有新证据时声称“已经定位唯一根因”。下一版先加入本地、无网络的主线程响应监测：后台每 100–250 ms 向主线程投递轻量 ping，记录最近/最大延迟、发生时间、当前页面、播放状态以及正在进行的本机任务；只保留有限聚合，不记录点击坐标、键盘内容、文件或窗口标题。
- 定时交互取消互斥单双击：定时图标改为立即执行的普通 `Button`，单击即时循环关闭/15/30/60；旁边独立的小菜单提供自定义分钟和直接选择，或以长按/右键作为辅助入口。不能继续用“单击必须等待双击”的协议。
- 面板首击必须可验证：展开窗口从非 key 状态开始，对声音、设备、导航、刷新、分页、播放和定时控件分别发送真实 AppKit mouseDown/up；第一次点击必须既激活窗口又执行一次动作。必要时为承载 SwiftUI 的 hosting view 实现 `acceptsFirstMouse`，但不得重复投递事件造成按钮执行两次。
- 系统指标采样移出主线程：建立串行后台 metrics actor/worker，CPU delta、接口字节、IOKit GPU、HID 温度、电池和代理状态均在后台采样，主线程一次性应用不可变快照。温度扫描降为 5–10 秒，并缓存可复用的 HID 服务发现结果；失败时复用短期旧值而不是立刻再次全量枚举。
- 活跃记录写盘改为后台、去抖和合并；每秒只更新内存账本，30–60 秒最多提交一次不可变数据快照。应用退出时允许有限同步收尾，但不得在普通按钮事件周期执行 JSON 编码/原子替换。
- 降低全树刷新：时钟显示使用局部 `TimelineView` 或独立时钟模型；系统指标打包为单个快照；声音页面只观察 `AmbientSoundService`，不得因温度、排行榜、远端设备或天气的每次变化重建 2×3 音源网格与播放器。
- 在“诊断”页增加“界面响应”证据：最近 5 分钟最大主线程延迟、超过 100/250/500 ms 的次数、当前 CPU/RSS、活跃 SSH 探测数和音频状态。该数据只在本机内存中滚动，不进入飞书或导出中的秘密字段。

#### 量化验收

1. 定时按钮单击到状态变化的本机测量 P95 小于 100 ms，不再受系统双击间隔影响；自定义时间仍可在独立菜单完成。
2. 非 key 展开浮窗的第一次点击，在导航、分页、播放、暂停、停止、定时、刷新、设备展开各执行 100 次；动作不得丢失、重复或触发窗口拖动。
3. 播放网络音频、三台 SSH 同时探测、温度采样和目录翻页并行 10 分钟，主线程 ping P95 小于 50 ms、P99 小于 150 ms，不得出现超过 500 ms 的无解释停顿。
4. 空闲展开声音页的平均 CPU 以 60 秒窗口测量，目标低于 5%；停止播放且无刷新时 RSS 目标低于 120 MB。硬件/系统差异导致未达目标时必须在报告中给出分项堆栈，不能隐藏。
5. 测试报告必须包含真实 `sample`/signpost/主线程延迟统计与测试场景；只通过编译或“手动点起来正常”不得标记为修复。

### P0 · 洞察页图表、Cursor 去重与节假日日历（2026-08-25 实机反馈）

- **Codex Token 14 天图表不得再堆叠日期。** 当前 `startDate` 以 `yyyy-MM-dd` 字符串作为 Chart 的离散 X 轴，窄窗口下会尝试给每个柱子绘制长标签，`.automatic(desiredCount:)` 不能稳定地对离散类别轴抽稀。解析为真实 `Date` 或显式提供稀疏刻度：常规宽度最多显示 4 个 `MM/dd`，更窄时只显示首/中/尾 3 个；鼠标悬停或点选柱子时再显示完整日期与 Token 数。缺失日期显式为 0，但不伪造服务端未返回的日期。
- **删除洞察页重复、过时的“Cursor 个人额度”大卡。** 设置页的“Cursor 本地额度（实验性）”连接器已经是唯一配置和状态来源；洞察页不再同时展示“官方未开放、PulseDock 不读取本地数据”的矛盾说明。如需引导，只在 API 连接器的空状态提供一次“添加 Cursor 额度源”，不再新增占高卡片。
- **“普通日期”卡升级为可交互的工作日历摘要。** 折叠态只显示“今天·工作日/休/班”、当日节日名以及“距下一节假日 N 天”，不让洞察页继续变长。点击后以 popover/sheet 展开月历，而不是在主 ScrollView 内无限向下堆卡片。
- 详细月历使用 7 列网格，明确区分普通工作日、周末、法定休假日和调休上班日；选中日期后显示公历、星期、节日名、“休/班”类型、所属连休区间、相关调休日及用户当日人工覆盖。月历下方提供“接下来 3 个假期/调休”紧凑列表。
- 节假日模型从两个硬编码集合升级为可审计的年度安排：保存年份、官方来源 URL/发布日期、节日连休区间、逐日类型与调休对应。官方尚未发布的年份显示“官方安排尚未发布”，只按用户工作周和周末计算，禁止根据旧年自动推测。
- 可选显示农历日期，但必须与“国务院法定放假安排”分栏标注；传统节日或农历日期不能被误标为法定休假。

#### 验收标准

1. 在紧凑和展开最小宽度下，14 天图表不得出现文本重叠或裁剪；日期刻度最多 4 个，每个柱子都可查看完整日期和数值。
2. 洞察页不再出现重复的 Cursor 大卡；Cursor 额度的配置、刷新、错误和数据来源只有一处权威 UI。
3. 日历 popover 可以前后切换月份、选择日期、通过键盘导航和 Escape 关闭；休/班/周末不仅依赖颜色，还必须有文字或形状区分。
4. 用 2026 年元旦、春节、国庆及全部调休日做确定性测试；对没有官方安排的未来年份做“不推测”测试。

### P0 回归 · 日历连续点选延迟（2026-08-26 实机反馈）

- **现象：**6.8.0 的月历 popover 可打开，但选中一个日期后，立即点击另一日期常无响应，需等待数秒。这不是日期计算准确性问题，而是 popover 内连续鼠标事件与视图刷新的响应性缺陷，按 P0 交互回归处理。
- **已发现的结构风险：**popover 直接构造为 `ChinaHolidayCalendarPopover(initialDate: store.now)`，而整个 `ExpandedPanel` 观察 `MonitorStore`。`store.now` 每秒发布，即使日历已打开，其入参、详情区和“接下来的安排”仍被全树周期性重新计算。同时日期网格使用 `LazyVGrid` 嵌在 SwiftUI popover 中，popover 自身又是与主 `NSPanel` 分离的窗口；需要通过事件测量确认是周期性重建、首击/key-window 转换，还是 lazy grid 命中与布局造成丢点。在取得数据前不把任一项宣称为唯一根因。
- **拟修复：**打开日历时固定一份 reference-date 快照，popover 不再订阅每秒变化的 `store.now`；将月份网格、选中日和节假日详情隔离为只依赖本地 `@State` 的小视图。月份数据只在切月时预计算一次；42 个以内的固定单元优先使用非 lazy 网格，避免没必要的延迟创建和命中更新。
- **事件验证：**在 popover 的日期 Button action 前后用本地 signpost/时间戳记录 `mouseDown → selectedDate 变更 → 选中样式上屏`，并与诊断页主线程延迟对照。如果主线程正常但 action 没收到，就继续检查 popover 窗口的 `acceptsFirstMouse`/key 状态，不用增加人为延迟的方式“修复”。

#### 验收标准

1. 同一月内连续点击 30 个不同日期，每次选中态和下方详情必须在 100 ms 内一起更新，不丢点、不需要第二次点击。
2. 快速交替执行“选日 → 下月 → 选日 → 上月 → 回到今天”100 次，不得卡住、关闭 popover 或让主浮窗抢回 key window。
3. 日历打开时同时运行系统指标、声音播放和 3 台 SSH 探测；日期点选 P95 小于 100 ms、P99 小于 150 ms，无 `>500 ms` 无解释停顿。
4. 使用鼠标、触控板和键盘分别验证；自动测试必须向真实 popover 窗口发送 AppKit mouseDown/up，不能只直接调用 `selectedDate = date`。

### 分页不能再用“过滤后数量”猜测结束

- 6.6.0 每次只向 Radio Browser 取 12 条原始记录，经过 HTTPS、codec、失效台、重复与私网过滤后可能只剩 6 条；界面又以“安全结果少于 12 条”禁用下一页，导致服务器明明还有结果却永远只能看到第 1 页。这是分页终止条件错误，不是目录内容不足。
- 下一版使用原始目录 cursor/offset 判断是否还有候选；每个 UI 页可有界地继续读取后续原始批次，直到凑满 12 条安全且去重的结果或目录真正耗尽。不得把过滤掉的记录当成目录末尾。
- 为查询词、场景、媒体类型分别保存页游标和安全结果缓存；切换分类后页码归零，返回时可恢复。显示数字页码与上一页/下一页；未知总量时不伪造总页数。
- 自动测试覆盖：首批 12 条仅 6 条可用但第 13–24 条仍有安全结果、连续多批均被过滤、最后一批为空、切换场景后 offset 独立、旧请求迟到不能覆盖当前页。

### 环境音和发现分类扩充

- 环境音从 6 条扩充为首批 18–24 条，固定 **2 列 × 3 行、每页 6 条**；至少覆盖雨、海浪、溪流、森林、鸟鸣、壁炉、咖啡馆、图书馆、键盘、列车、机舱、夜晚虫鸣、雷雨、风、棕/粉/白噪音等方向。
- 每条环境音必须逐项核验真实内容、响度、循环接缝、HTTPS、作者与许可；不能再次用同一合成底噪换名称凑数量。无法确认可分发许可的音源不内置。
- 工作音乐发现入口从当前 4 项扩充为：Lo-fi、轻爵士、古典、钢琴、电子氛围、无人声、深度工作、咖啡馆、游戏原声风格、阅读/写作等。
- 网络广播从当前 4 项扩充为：本地中文、新闻、谈话、文化、科技、财经、体育、语言学习、古典、爵士和世界电台；分类只是目录筛选，不对节目内容和长期可用性背书。
- 环境音、工作音乐、网络广播三类目录统一为 **2 列 × 3 行、每页 6 条**。每一类都提供“发现 / 我的收藏”、搜索框、网络模式、页码和上一页/下一页；环境音搜索可匹配声景名称、标签、作者或用户添加的合法音源，不使用 Radio Browser 假装搜索自然录音。
- 三类页面排版骨架一致，减少学习成本；内容卡片图标、元数据和播放器主题按媒体类型区分。统一排版不能退化为三类播放器共用同一套视觉。

### 声音 UI 回归 · 环境音“发现”分类过长（2026-08-26 实机反馈）

> **6.9.1 已实现：** 环境音分类已改为内容宽度的左对齐自动换行 `DiscoveryChipFlow`；不再使用两列弹性网格或 `maxWidth: .infinity`，因此不会拉成半屏长条。默认两行/“更多”收敛策略仍作为后续内容规模扩大时的优化项。

- **已确认根因：**音乐/广播的发现项使用内容宽度的紧凑标签；环境音分类在两列 `LazyVGrid` 中同时对 `Label` 和 Button 使用 `frame(maxWidth: .infinity)`，因此每个分类被强制拉满半列，形成长灰条。这与“全部”按钮、收藏入口以及音乐/广播的紧凑节奏不一致。
- **拟修复：**三类媒体的“发现”共用一个紧凑、内容自适应的 `DiscoveryChip` 组件，统一图标尺寸、字号、左右内边距、选中色和键盘焦点。环境音分类改为左对齐自动换行的 chip 流，不再强制等宽；保留“全部”与“我的收藏”的清晰层级。
- **数量策略：**默认最多显示两行常用分类；分类增多时提供“更多”展开，而不让发现区持续推低搜索框和六张内容卡。选中的类别在收起状态下仍必须可见。

#### 验收标准

1. 环境音、工作音乐和广播的发现标签在字号、高度、图标对齐和选中态上使用同一规范，不再出现环境音独有的半屏长条。
2. 中文、英文、日文、韩文和繁体中文下不截断关键分类名；长文案使用自然宽度与最大宽度上限，不因单个标签撑坏整行。
3. 展开最小宽度及 500 pt 常规宽度分别截图比对；“发现”区默认不得超过两行，搜索框与首屏 2×3 内容仍应保持紧凑。

### 声音 UI 回归 · 音量操作导致控制区跳动（2026-08-26 实机反馈）

> **6.9.0 已实现，仍待真实 GUI 验收：** 控制区已拆为固定的传输/音量/定时模式槽位；图标使用固定框，Slider 固定宽度，窄宽度切为两行；静音改为真实 0 并恢复上一次音量。自动测试覆盖样式映射与静音恢复，实际拖动布局稳定性仍须在目标 Mac 复核。

- **已确认根因：**当前播放器把传输按钮、音量、定时器和播放模式放在同一个横向弹性布局中；音量图标会在 `speaker.slash.fill / speaker.wave.1.fill / speaker.wave.2.fill` 三种不同固有宽度之间切换，Slider 又使用 `maxWidth: .infinity`。拖动音量时 SwiftUI 会重新分配整行宽度，所以滑杆、定时器和模式按钮发生横移，看起来像播放器乃至窗口被拉伸或压缩。
- **拟修复：**播放器控制区拆为“传输控制 / 音量 / 定时与模式”三个稳定槽位；扬声器按钮使用固定宽高和固定对齐框，Slider 使用明确的最小、理想与最大宽度，不再吞占剩余空间。常规宽度保持单行；窄宽度由明确断点切换成两行，而不是临时挤压。
- **窗口约束：**音量变化只能更新音频增益和图标状态，不得修改 `NSPanel` frame、播放器外框高度或相邻区域位置。静音也改为显式按钮状态，数值可到 0，不能靠 Slider 的 `0.02` 下限制造“近似静音”。

#### 验收标准

1. 在 0%、3%、34%、35%、80% 间快速拖动 100 次，窗口 frame 与播放器外框尺寸完全不变，定时和模式按钮位移不超过 1 pt。
2. 展开最小宽度、500 pt 常规宽度、不同语言与 15/30/60 分钟计时文字下分别截图比对；无控件被截断、覆盖或压成短线。
3. 播放环境音、工作音乐和广播时使用同一套稳定控制槽；鼠标拖动期间主线程无长任务，Slider 连续响应且不会误触整窗拖动或双击收起。

### 环境播放器视觉重构 · 从通用频谱改为类型化微缩声景（2026-08-26 实机反馈）

> **6.9.0 首批已实现，持续扩充：** 24 条内置录音已由明确 `sceneStyle` 驱动，具象场景不再落入通用棕色频谱兜底；已实现雨、城市雨、雷雨、微风、森林、潮汐、溪流、水下、壁炉、暖炉、咖啡馆、图书馆、键盘、电车/列车、城市夜色、夜虫与棕色噪音。更高保真度与正式视觉评审仍属于后续迭代。

- **已确认根因：**当前 `AmbientSceneDeck` 只对雨/雷、风/树叶和水下做了三类绘制；其余类型全部落入“棕色渐变 + 34 根频谱柱”的通用兜底。咖啡馆、壁炉柴火、温暖炉火等因此只更换标题、颜色和 SF Symbol，运动规律与构图完全相同，缺少材质、空间和声音语义，呈现明显的模板化/AI 感。
- **数据模型先行：**环境音条目新增显式 `sceneStyle` 与 `sceneVariant`，由审核过的目录元数据决定场景，不再通过 `id.contains(...)` 或标题猜类型。未知类型显示克制的静态封面并标记待设计，不再自动套用假频谱。
- **统一视觉语言：**采用轻量“微缩窗景 / 桌面小景”语言：低饱和配色、前中后景、有限材质纹理与符合物理方向的缓慢运动；保留当前产品的复古气质，但不使用廉价大渐变、装饰性频谱柱或只放一个放大的 SF Symbol。三类播放器外框尺寸和控制位置仍保持一致。
- **类型化场景首批：**
  - 雨与雷雨：窗框、玻璃雨痕、远景灯光与低频远闪；城市晚雨增加街灯倒影和车流光迹，不能和普通雨共用同一画面。
  - 海浪、溪流与水下：分别使用分层潮汐/岸线泡沫、横向水流/石面涟漪、水下焦散/缓慢气泡，运动方向与声音来源一致。
  - 森林、鸟鸣、微风与夜虫：树冠剪影、叶影、晨光或月色、少量萤光点；鸟鸣不画持续频谱，微风不伪装成水波。
  - 壁炉与炉火：可辨认的原木、火焰轮廓、余烬和局部暖光；“壁炉柴火”偏近景燃烧，“温暖炉火”偏室内空间氛围，二者共享引擎但不共享同一构图。
  - 咖啡馆、阅读与键盘：杯口蒸汽、桌面/窗光、书页或键盘局部反馈；咖啡馆体现室内空间和轻微人影层次，不再显示音频柱。
  - 列车、机舱与城市空间：窗外视差、轨道/灯带或舱内环境光，用慢速横向运动表达旅途，不堆无意义粒子。
  - 白/粉/棕噪音：仅这类抽象声音允许使用频谱、纸纹或呼吸式色域，并明确区分频率倾向；抽象视觉不能反向复用到具象场景。
- **性能边界：**继续使用 SwiftUI `Canvas`/矢量图层，不引入视频动态壁纸。播放且可见时目标 8–12 FPS；暂停、不可见、低电量、Reduce Motion 或高温时降为静态/低帧。相同静态资源与路径缓存复用，场景动画不能使 PulseDock 自身成为发热源。

#### 验收标准

1. 至少 12 个代表性类型完成独立视觉审查；盲看截图时能在不读标题的情况下区分雨、海浪、森林、壁炉、咖啡馆、列车和抽象噪音。
2. 咖啡馆、壁炉柴火、温暖炉火不得再出现相同频谱构图；同类 variant 可以复用绘制引擎，但配景、色温和运动语义必须不同。
3. 每个场景提供播放动画、暂停静帧、Reduce Motion 静态版和深浅主题校验；文字对比度、图标语义与辅助功能标签通过检查。
4. 连续播放 30 分钟并同时运行系统监控与 SSH 探测，动画不可导致明显温升或交互卡顿；场景不可见后时间轴停止，CPU/内存基线写入发布验收报告。

### 三套同尺寸、不同语义的播放器视觉

- 当前截图中的环境音页面会继续显示上一台广播/磁带机，这不是目标效果。浏览内容与全局 Now Playing 分离后，环境音播放时显示与当前声音匹配的动态声景播放器；广播和音乐仍分别显示收音机与磁带机。
- 环境音播放器与音乐、广播播放器使用相同外框尺寸，内部是接近“动态壁纸”的低干扰场景动画：雨声显示玻璃雨滴/波纹，海浪显示分层潮汐，森林显示缓慢叶影，壁炉显示低频火光，雷雨显示有上限的远闪。每条声音拥有明确的动画主题和静态降级画面。
- 动画使用 SwiftUI `Canvas`、渐变、少量粒子和低频 `TimelineView` 等程序化方案；目标 10–15 FPS，播放器不可见/暂停时停止时间轴，Reduce Motion/低电量/严重热状态下切为静态或 1–2 FPS。不得为装饰长期播放视频、持续高刷或增加第二套音频引擎。
- 工作音乐磁带机补齐磁带纸标、双卷轴、播放方向、低频机械状态和队列信息；广播收音机补齐木质机身、台名窗、调谐刻度、扬声器网格与 `LIVE` 状态，但不伪造 FM 频率和信号强度。
- 三套播放器共享控制位置、尺寸、辅助功能和音量/定时逻辑，确保切换页面时布局不跳动；视觉动画、时间语义、标题和状态来源按真实媒体类型渲染。

### 播放器控制改为紧凑图标语义

- 上一条、播放/暂停、停止、下一条统一使用 `backward.fill / play.fill|pause.fill / stop.fill / forward.fill`，保留 tooltip、辅助功能名称和至少 28×28 pt 点击区；失败时播放按钮变为重试图标。
- 播放模式用一个图标菜单代替长文字 Picker：固定电台、单条循环、列表循环、随机播放分别使用可区分图标；菜单内继续显示完整文字和当前勾选，不能只靠图形猜含义。
- 定时器使用一个 `timer` 图标与紧凑剩余时间。单击按 `15 → 30 → 60 → 关闭` 循环；双击打开可编辑 Popover，允许输入 1–240 分钟并提供常用值。该控件必须拦截双击，不能触发整个浮窗展开/收起。
- 音量区域改为独立弹性槽：扬声器图标随静音/音量变化，Slider 设置明确最小宽度，不再被定时器和模式菜单挤成短线；必要时播放器控制拆成两行。
- 三类播放器使用同一控制协议但保留各自视觉外壳；环境音不得继续显示收音机，直播流不得绘制虚假歌曲总进度。
- 修复“切到环境音页仍显示上一台广播，但模式菜单已经变成环境音单条循环”的状态错配：当前浏览类型与正在播放类型分离。全局 Now Playing 明确标出真实媒体类型；模式按钮始终作用于正在播放队列，未播放时才作用于当前浏览类型。

### Clash 用量口径消除歧义

- 设置页同时显示“已用、剩余、总计、数据更新时间与来源文件”，不再只突出剩余值让用户与 Clash Verge 的“已用 / 总量”产生冲突。
- 单位明确采用二进制 `GiB/TiB` 或在界面说明当前 `GB` 的换算口径；不得把 1 TiB 同时写成 1000 GB 和 1024 GB。
- provider 节点名称里的“剩余流量”可能是机场写入且刷新滞后的普通文本，不能作为权威数值；PulseDock 只用 `subscription-userinfo` / 本机 profile 元数据的 `upload + download + total` 计算，并显示元数据更新时间。
- 验收示例：总量 1 TiB、已用 80.9 GiB 时，必须同时显示“已用 80.9 GiB · 剩余约 943.1 GiB · 总计 1024 GiB”，并与 Clash Verge 的 `80.9 GB / 1.00 TB` 明确解释为同一组数据的不同口径。

## 6.6.0 已交付核心 / 后续 P1 · 音频 TLS 与 GLM 额度增强

### 网络音频 TLS 失败

- 当前 Radio Browser 的 `lastcheckok` 只代表第三方目录最近认为电台可用，不代表该电台的 HTTPS 证书、TLS 版本与当前 Mac/Clash 出口一定兼容。目录搜索与 AVPlayer 音频播放是两条独立链路，界面必须区分“目录镜像失败”和“真实流媒体主机失败”，不能统一显示成 TLS 错误。
- 2026-08-24 实机证据：规则模式下中国广播可播放；搜索“鞍山”时 `de1` 镜像正常返回 HTTP 200 和 0 条结果，随后 `at1/nl1` 镜像发生 TLS/DNS 失败，当前实现错误地用最后一次失败覆盖了“没有结果”；BBC World Service 同一 HTTPS MP3 地址直连可持续收到 HTTP 200 音频，经当前 Clash 节点则在 TLS 阶段超时。因此需要同时修复搜索聚合逻辑与播放路由诊断，不能把两者归结为同一个播放器故障。
- 合法的 HTTP 2xx JSON 空数组视为一次成功查询并显示“没有找到结果”，不得继续请求其他镜像后用其 TLS/DNS 错误覆盖；只有网络失败、HTTP 异常或响应不可解析时才切换镜像。镜像进入短时熔断并记录最近成功时间，错误卡显示失败镜像、阶段与重试入口。
- 播放失败需分类为：证书不受信任、证书过期/主机名不匹配、TLS 握手失败、DNS、连接超时、HTTP 错误、编码不支持和直播断流；显示可读原因与不含 query/userinfo 的主机证据。
- 不关闭 ATS、不忽略证书、不为单个电台加全局 TLS 例外。单个电台失败时在本次会话标记“TLS 不可用”并提供换台；多个不同主机连续 TLS 失败时，再建议检查系统时间、Clash HTTPS 解密/证书和当前节点。
- 若“中国电台普遍失败，海外电台正常”且当前使用海外代理出口，将其分类为“可能的地区/回国路由问题”：可能是电台 CDN 限制中国大陆 IP、海外 DNS 返回不可用节点、跨境回程或代理 TLS 路径。界面提供脱敏主机名与“建议对该主机 DIRECT”说明，但 PulseDock 不自动修改 Clash 规则或 DNS。
- 点击播放后才做有界的延迟验证，不在翻页时批量握手所有电台。播放模式允许“失效时自动下一台”，但默认不自动跳过用户选中的广播。
- 新增受控测试：单台 TLS 失败不影响其他音源；多主机失败才提示本机/代理路径；错误证据不含 URL 查询、凭据或完整重定向链。

### 智能兼容播放：尽量不要求用户修改 Clash

- 新增声音网络模式：`智能兼容（推荐） / 跟随系统 / 仅直连`。首次选择智能兼容时只解释并授权一次；后续不要求用户切换 Clash 模式、手工增加 DIRECT 规则或重复确认。
- 智能兼容按固定状态机工作：先跟随系统网络播放；连续缓冲 8–10 秒后进行有界诊断；优先重试同一电台的其他安全 HTTPS 音源；代理链路失败但直连探测成功时，再使用 PulseDock 的会话级本地音频中转直连播放。
- 本地中转只绑定 `127.0.0.1` 随机端口并使用会话随机路径，只接受当前已验证的公共 HTTPS 音源；拒绝 localhost、私网、链路本地地址、userinfo、降级 HTTP 与不安全重定向。停止、换台、网络变化或退出后立即关闭，不写 Clash 配置、系统代理、PAC、证书或全局环境变量。
- 首阶段支持 MP3/AAC 连续流；HLS 需要安全改写播放列表与分片 URL，单独验收后开放，未支持时自动换用同台 MP3/AAC 源并明确提示，不伪装成功。
- “跟随系统”严格遵守系统/Clash 路由，不做直连回退；“仅直连”只影响 PulseDock 广播/专注音乐请求，不影响其他应用。用户随时可以关闭智能兼容并清除会话状态。
- 直连回退属于隐私可见行为：播放器状态必须显示“系统路由 / 智能直连 / 备用音源”，但不展示公网 IP、节点名或完整流地址；默认不写播放历史、时间线或飞书。
- 若所有安全路径均失败，停止无限“缓冲中”，显示最后阶段、累计等待时间和“重试 / 换源 / 跟随系统”操作；不得自动循环请求造成高 CPU、流量放大或节点压力。
- 验收：不修改 Clash 的情况下，中国直连电台和代理可用电台正常播放；模拟代理 TLS 超时且直连成功时可在一次授权后自动恢复；停止后本地监听端口消失；拒绝私网/重定向绕过；同一流最多一个在途诊断和一个播放器；连续 30 次失败切换无资源泄漏。

### GLM Coding Plan 返回 HTTP 200 但无法显示额度

- 当前“GLM 返回数据格式无法识别”只会在请求已得到 HTTP 200，但响应体不能解析为 JSON object 时出现。这不能等同于“剩余为 0”或“API Key 错误”；可能是 HTML/WAF/代理页、空响应、顶层结构变化或上游业务错误仍以 200 返回。
- 保留官方用量查询路径 `GET https://open.bigmodel.cn/api/monitor/usage/quota/limit`、裸 `Authorization` 与 `Accept-Language`；不发送模型请求，不用 prompt 消耗来猜额度。
- 解码层先验证 HTTP status、`Content-Type`、有界 body 长度、顶层 object/array 形状、`success/code/msg/error` 业务 envelope 和 `data.limits`；HTML 或非 JSON 返回明确标记“上游返回非 JSON，可能被网关/代理替换”，不再只显示“格式无法识别”。
- 额度行兼容未知类型与字段演进；优先读取上游名称与明确的 `remaining/currentValue/usage/number/percentage/unit/nextResetTime`，只在有可靠数值时计算剩余。积分、prompt、Token、MCP 和百分比必须保留原始单位，不互相冒充。
- 当刷新返回无法识别的新结构时，保留最后一次成功快照并标为“已过期 / 本次刷新失败”，不立即把可用额度清成 `--`。
- GLM 连接器建立单飞与节流：后台 5 分钟刷新、全部刷新和单项手动刷新同时发生时只复用一个在途请求；快速重复点击显示当前请求或最短等待时间。HTTP 429/1302/1305 尊重 `Retry-After` 或指数退避，不立即连续重试。
- 新增“复制安全诊断”：只包含 HTTP status、Content-Type、body 字节数、JSON 顶层类型和有限键名，不包含 API Key、完整响应、账号/组织 ID 或原始错误页。
- 离线 fixture 覆盖：现有 `TOKENS_LIMIT/TIME_LIMIT`、未知额度类型、只有剩余数值、业务错误 envelope、顶层 array、HTTP 200 + HTML、空 body、超长 body 与旧快照保留。

## 6.6.0 已交付基础 / 后续 P1 · 声音中心 2.0：可发现、可收藏、三种真实播放语义

### 1. 分页与新手发现

- 环境音固定为 **3 行 × 2 列，每页 6 个**；首期将精选库扩充至 18–24 条，每条必须有 HTTPS 源、可追溯作者/许可、实际试听与响度校验；不再用难以区分的合成白噪音/风扇噪音凑数。
- 工作音乐与网络广播每页保留 **2 列 × 6 行，每页 12 个**，改为目录端 `offset + limit` 按页请求，不再一次取 24/30 个后只显示前 12 个。每个类型、分类和搜索词独立保存当前页和会话内页缓存。
- 页脚显示当前页、上一页/下一页和已加载数量。只有第三方目录返回可靠总量时才显示“N/M 页”；否则只显示“第 N 页”，不伪造总页数。
- 为不知道搜什么的用户提供“我想专注 / 放松 / 阅读 / 写代码 / 睡前 / 随便听听”引导入口，转换为经过审查的标签组合；搜索保留给熟悉台名、风格或国家的用户。
- 扩展分类：工作音乐增加钢琴、电子氛围、游戏原声风格、无人声、咖啡馆、深度工作等；广播增加本地/中文、新闻、文化、谈话、科技、语言学习、体育、古典/爵士和国家/语言筛选。

### 2. 收藏与播放顺序

- 新增独立“我的收藏”页，可按全部/环境音/专注音乐/广播筛选，支持取消收藏、最近收藏、名称排序和手动排序。
- 收藏不再只保存 ID；本机保存最小非秘密元数据（类型、站点 UUID、名称、来源与最后可用状态），播放前重新解析直播地址。失效项标记“暂不可用”，不静默删除。
- 不把三种媒体硬套成同一个“循环”：
  - 环境音：默认单条循环，可选播放一次、列表循环和随机；
  - 专注音乐直播流：可选按当前列表、随机或固定电台；“固定”表示断流时尝试重连，不伪称单曲循环；
  - 网络广播：默认手动换台，可选“只在失效时自动下一台”，防止新闻/节目被意外切换。
- 显示明确的播放模式图标和文字，上一条/下一条始终作用于当前页、当前场景或收藏队列，不在切页后暗中改变队列。

### 3. 进度与时间的真实语义

- 环境录音在 AVPlayer 返回有限时长和可定位区间时，显示已播/总时长、可拖动进度条和循环计数；未取得时长时显示“时长未知”。
- 当前工作音乐和广播均来自网络直播流，通常没有可信总时长；这类卡片显示 `LIVE`、本次已收听时长、连接/缓冲状态，不绘制虚假的总进度条。
- 未来只有接入合法、可定位的点播音频时，才显示常规歌曲/播客进度条、总时长和 seek。

### 4. 三套独立的复古播放器

- 环境音改为独立的“复古野外录音机 / 自然声景台”：大音量旋钮、时长窗、声波/磁带轮与场景标签；不复用广播调谐元素。
- 专注音乐使用真正的复古磁带机：磁带纸标、卷轴、模式键、金属/深色外壳；动画只在播放且页面可见时低频运行。
- 网络广播使用独立的“木质台式收音机”：电台名窗、模拟调谐刻度、扬声器网格、音量/换台旋钮和 `LIVE` 指示灯；不伪造 FM 频率或信号强度。
- 三类播放器共享同一底层 AVPlayer 状态机与音量/定时能力，但视觉结构、主操作和时间语义分开；不再只替换一个图标与标题。

### 5. 质量、隐私与轻量化验收

- 将“工作音乐”在来源层明确标注为“专注音乐电台 / 网络直播”，不让用户误以为是可控进度的单曲点播。
- 结果卡增加码率、编码、国家/语言和最后可用状态；过滤过长标题、不可读元数据、不安全地址与明显不符合类别的条目。
- 切换音源和定时停止提供 0.3–1 秒可关闭的淡入/淡出，避免突然放大或截断；默认音量仍保持低于系统中位。
- 分页只缓存目录元数据，不预取封面或音频；全应用只保留一个播放器，停止/退出后释放流和观察者。
- 默认不记录播放历史；收藏、分页和播放模式只保存在本机，不写入时间线/飞书。
- 播放失败只做就地提示，不生成飞书告警；网络切换后按模式重连，避免告警风暴。
- 新增离线测试：分页越界/取消/旧回调、收藏 round-trip 与失效项、三类播放策略、直播不显示假进度、有限音频 seek、切页不改队列、默认静音与单播放器约束。

### 6. 下一版 P0 阻断 · 工作音乐与网络广播搜索框间歇性丢失中文输入和光标

- 2026-08-25 二次实测确认：6.6.0 并未解决该问题。广播和工作音乐搜索框有时可输入英文，但无法启动/维持中文输入法组合，插入光标同时消失；同一运行中又可能偶尔恢复。这是**间歇性焦点与 field editor/IME 会话丢失**，不是 Radio Browser 不支持中文，也不是搜索参数编码问题。6.6.0 不再标记为已修复。
- 当前自动测试虽把 `FocusableTextField` 放入 `NSHostingView` 并可通过代码使其成为 first responder，但没有模拟“应用原本不活跃 → 首次点击无边框 accessory NSPanel → 创建 field editor → 输入法 setMarkedText/commit → 播放状态持续刷新”的完整事件序列。因此测试通过只能证明控件可构造，不能证明真实中文输入可靠。
- 高概率触发链已缩小：`FocusableFloatingPanel.sendEvent` 和 `ReliableTextField.mouseDown` 都会在同一个 mouseDown 内执行 `NSApp.activate + makeKeyAndOrderFront`，随后立即把原事件交给 `NSTextField`。应用原本已激活时常常成功；从其他应用返回、播放器刷新或窗口刚恢复时，激活/key-window 切换尚未完成，field editor 可能没有稳定成为 first responder，于是出现“ASCII 偶尔能进、IME marked text 失败、caret 消失”。这需要真实事件验证后再定根因，不能只靠猜测提交补丁。
- 现有 `FocusableFloatingPanel.sendEvent` 同时负责应用激活、全窗双击和背景拖动；SwiftUI 文本字段在 field editor 尚未创建时可能只命中包装视图，若被误判为普通背景，点击会被拖动/双击状态机截获，面板虽然成为 key window，搜索框仍不会成为 first responder。下一版必须把“窗口激活”和“文本编辑获得焦点”拆成可验证的顺序，交互控件命中后不得进入背景手势状态。
- 为声音搜索建立显式焦点协调器：第一次 mouseDown 只完成应用/面板激活；在当前事件结束后的主线程周期确认 `isKeyWindow`，再创建 field editor、调用 `makeFirstResponder` 并显示插入点。不得在面板和字段两层重复争抢激活。
- 编辑期间 `updateNSView` 不得写回 `stringValue`，不仅是 `hasMarkedText == true` 时；外部绑定只在字段未编辑时同步。中文组合期间播放器、目录加载、页码和状态 Badge 更新不得重建字段或中断 field editor。
- 工作音乐与广播分别保存查询和显式焦点 ID；切换页面、从菜单栏/Dock恢复、展开/收起、点击播放、目录返回和窗口失去/重新获得 key 状态时都有确定的 resign/restore 规则。按 Escape 退出编辑，Enter 只在 marked text 已提交后搜索。
- 搜索框必须显示与主题对比度足够的插入光标、焦点环、选区和输入法候选窗口；透明度、浅色/深色主题、窗口置顶以及程序坞隐藏状态不得让 caret 变成不可见。不得通过关闭全窗双击/拖动来绕过问题。
- 中文输入法组合期间必须保留 marked text，不得因 `directoryLoading`、搜索状态刷新、分页或播放器状态更新重建字段并丢失尚未提交的拼音；搜索请求只接收已经提交的 Unicode 文本并继续使用 `URLComponents/URLQueryItem` 编码。
- 相同焦点契约扩展到城市、SSH、API、飞书和 Clash Secret 等所有输入框，避免声音页使用临时特例后其他页面再次回归。
- 自动验收增加真实事件路径：窗口从非活跃状态开始，发送 mouseDown/keyboard 事件后面板为 key、field editor 为 first responder、背景拖动未 armed；模拟 `setMarkedText → insertText`，期间连续刷新播放状态，字段不被重建且最终 Unicode 文本完整；双击字段不展开/收起窗口。
- 增加20轮压力序列：从其他应用返回、菜单栏恢复、Dock恢复、切换音乐/广播、播放中输入、暂停时输入和修改透明度后输入；每轮检查 caret、候选窗、marked range与提交结果。
- 发布前必须在真机分别用macOS简体拼音、英文输入法完成工作音乐与网络广播搜索：可见闪烁光标，可输入“鞍山”“南京音乐广播”，候选选择与回车提交正常；再复核一次第三方中文输入法兼容性。任一场景失败时不得发布下一版本，不能再降级为“人工回归待确认”。

### 暂不并入本次

- Apple Music、Spotify、QQ 音乐、网易云音乐账号、点播歌单和歌词仍需单独评估官方 SDK/OAuth、账号安全、版权与地区条款，不与本次分页/UI 优化混在同一版本。
- 番茄钟自动开播/休息换声暂不默认开启；后续只以明确的用户可选规则接入。

## 6.5.0 已交付 · Codex 多窗口、专注声音与远程稳定性（2026-08-24）

- 基于 OpenAI 官方 `codex app-server` 独立实现多个额度窗口、重置券与 Token 日活动；Codex Runway 只作为信息架构研究，未复制其 AGPL 源码、OAuth、UI 或资产。
- 环境音、工作音乐、网络广播进入同一声音页；工作音乐和广播使用 Radio Browser 第三方目录的 HTTPS 流，提供复古磁带机/收音机、搜索、场景、收藏、单流播放和定时停止。
- 完整态普通背景恢复双击与拖动；控件和文本编辑保持隔离。手动选城会取消旧定位并阻止迟到回调覆盖。
- SSH 设备与 API 连接器交付置顶、上移、下移；远程设备交付网络范围、90 秒网络切换宽限、连续 3 次失败阈值、最多 3 台并发。
- SSH 解析交付输出实时排空/限长、缺 marker 降级、数值范围与溢出保护；GLM 多周期额度改为独立状态单元，不再连写 `#1 1%`。
- 未交付：鼠标直接拖拽排序、工作台模块自定义布局、远程 SSH 密钥自动引导、会话级 Codex 代理、Apple/Spotify/QQ/网易云账号、歌词和番茄钟声音联动。

## 6.4.2 已交付 · 单次解锁闭环与真实环境音（2026-08-19）

- 解锁统一保险库时不再自动扫描旧版分散 Keychain 项；旧凭据导入改为显式、非交互操作。
- 增加单实例保护，降低旧进程/重复进程同时触发钥匙串访问的风险。
- 白噪音首批改为六条 CC0 真实录音网络流，交付来源、许可、音量、停止和定时；不下载、不记录历史。
- 完成产品规范、架构与数据源目录。

## 6.5.0 已交付 · 崩溃防回归与远程解析隔离（2026-08-21 复核）

- 用户提供的报告来自 6.0.0（600），崩溃线程位于 `SSHMonitorService.parse`，异常为 `EXC_BREAKPOINT / SIGTRAP`；旧版对重复 PID 使用要求键唯一的字典构造，远端多 GPU 进程数据触发运行时断言。6.1.0 已按 PID 跨卡归并，当前 6.4.2 不再走该路径。
- 本次复核继续消除 GPU UUID 字典的同类隐患：重复 GPU 行安全去重，禁止任何来自 SSH、`nvidia-smi`、Slurm 或外部 API 的未验证数组直接进入 `Dictionary(uniqueKeysWithValues:)`。
- 下一版发布门禁增加异常远端输出矩阵：重复 GPU UUID、跨 1–8 卡重复 PID、重复 `ps` 行、空行、截断 CSV、非 UTF-8 替换文本、混合 stderr、超长字段和采集中断。
- 单台设备解析失败必须转化为该设备的“采集数据异常”快照并保留有限证据，不能终止其他设备任务或整个应用；解析器输入设置行数与长度上限，防止异常主机造成内存放大。
- Release 构建保留可符号化信息与版本/构建号；收到崩溃报告时先判断实际版本，已修复旧版本不得误判为当前版本回归。

### 验收标准

1. 重复 GPU 和 PID 测试连续执行 1,000 次无崩溃、无重复显存累计；
2. 任一设备返回畸形数据时，其余设备、菜单栏、浮窗和本机监控继续工作；
3. 崩溃或解析错误证据包含版本、设备别名、阶段和安全截断原因，但不包含主机地址、Token、路径或未脱敏命令；
4. `scripts/test.sh` 静态扫描远端解析代码，禁止重新引入对不可信键的 `Dictionary(uniqueKeysWithValues:)`。

## 6.4.1 已交付 · 凭据升级恢复与控制器可解释诊断（2026-08-19）

- 旧版分散凭据在一次解锁后进行非交互迁移；不可读项目在原 API/飞书/Mihomo 卡片重新填写，不弹出成组密码框；
- API 密钥支持原地保存并刷新，飞书支持就近保存；统一保险库在本次会话解锁后静默保存；
- Mihomo 自动发现展示每个候选的验证结果；Clash 本地额度与控制器同步完全解耦；
- 6.4.0 合成环境音因质量不达标下线。真实录音环境音保留为后续独立验收范围。

### 后续环境音重新上线门槛

1. 仅使用许可清晰、可追溯来源的真实录音或用户主动添加的音源；
2. 雨声、海浪、森林、咖啡馆和柔和风扇声必须可在加入前试听，且听感明显不同；
3. 默认不播放、不下载，不抓取账号或播放历史；播放失败不得影响监控主循环；
4. 增加持续试听和 CPU/内存/网络占用人工验收，不再以程序生成噪声冒充自然环境音。

## 6.4.0 已交付 · 统一凭据、额度可信度与白噪音首版（2026-08-19）

### P0 · Cursor 手动刷新无效

- 已定位根因：`refreshAPIConnectorsAsync` 只处理“需要 API Key 且已缓存/手动解锁”的连接器；Cursor 本地额度明确不需要 API Key，却落入最终的“凭据尚未解锁”分支，因此右上角手动刷新也不会调用 `CursorUsageService`。
- 修正分支：不需要 API Key 的连接器直接执行探测；需要 Key 的连接器才进入统一凭据保险库。Cursor 刷新需依次显示“发现数据库 → 读取本地会话 → 请求用量 → 成功/HTTP 状态/字段不兼容”，避免按钮点击后只有笼统提示。
- Cursor 卡片加入独立刷新、来源、数据时间、周期结束与错误详情；接口或字段变化时保留脱敏响应结构诊断，不记录 access token、邮箱和完整响应。

### P0 · 单次解锁的 PulseDock 凭据保险库

- 将 Clash Secret、飞书 Webhook/签名密钥和各厂商 API Key 从多个 Keychain 项目迁移为一个版本化的 `PulseDock Credential Vault` 项目；应用启动仍不主动读取。
- 设置页顶部提供唯一的“解锁凭据”入口。用户完成一次 macOS 钥匙串授权后，本次运行的全部连接器共用内存快照；各模块不再分别显示“读取已保存/修复旧凭据”，也不再要求逐项输入系统登录密码。
- 增加“已解锁 / 已锁定 / 需要迁移 / 读取失败”四种明确状态、手动锁定、保存全部变更和只清除 PulseDock 保险库；任何秘密不进入日志、时间线、配置导出或诊断复制。
- 旧版分散项目不在后台自动迁移。首次升级提供一次可预览迁移；无法无提示读取的旧项目允许用户在统一表单重新填写后一次保存，再安全清理旧项目。
- 默认策略建议为“退出 PulseDock 后锁定，本次运行只解锁一次”；是否在 macOS 锁屏/睡眠后立即重新锁定，需用户确认。

### P0 · Clash 实时用量与控制器自动发现

- 2026-08-19 只读检查证据：Clash Verge `profiles.yaml` 在 11:51:40 更新，良心云元数据约剩余 `946.02 GiB / 1000 GiB`；运行进程监听 `127.0.0.1:33331`，持久配置却写为 `127.0.0.1:9097`。运行端口、配置端口与 PulseDock 手填端口不一致是控制器失败的直接原因。
- 增加“自动发现控制器”：只扫描 Clash Verge/Mihomo 进程实际监听的本机回环端口和已知配置候选，对候选请求 `/version` 验证；不扫描局域网，不修改 Clash 配置，不输出 Secret。
- 用量数据源按可信度排序：本机控制器 `/providers/proxies` 中的 `subscriptionInfo` → Clash Verge `profiles.yaml` 的 `extra` → 用户选择的兼容文件。每个值显示来源、数据时间和是否缓存；不访问机场网页、不读取订阅 URL/Token。
- “同步”拆成可解释步骤：发现运行内核、验证 Secret、刷新 provider、重新读取 provider 用量、必要时降级到本地元数据。运行配置与文件配置不一致时直接展示二者，不要求用户猜端口。
- 修复文件监听与五分钟兜底刷新，确保 Clash 原子替换 `profiles.yaml` 后仍重新挂载监听；手动刷新必须立即更新卡片或显示明确失败阶段。

### P1 · API 与 Clash 设置 UI 重构

- 设置页顶部新增紧凑的“凭据保险库”状态卡，只保留一个主按钮：解锁/保存/锁定根据状态切换；危险操作进入菜单并二次确认。
- API 新增流程移入独立表单/弹层，常态页面只显示连接器卡片。每张卡统一为：厂商与状态、主额度、周期/重置、来源与更新时间、单项刷新、更多菜单；GLM 的多个周期用标签/进度而不是一行挤满文字。
- Clash 拆为“订阅用量”和“本地控制器”两张卡：前者直接预览剩余流量、到期、来源和新鲜度；后者显示自动发现结果、监听端口、内核版本、Secret 状态和最近同步证据。横向八按钮布局彻底移除。
- 所有错误提供“查看原因”，正文保持一行可扫读；成功、降级、需要操作分别使用稳定的绿/橙/蓝语义，不用 `--` 隐藏真实状态。

### P1 · 新功能：白噪音与环境音 MVP

- 新增独立“声音”页，首批提供雨声、海浪、森林、咖啡馆、风扇/空调和键盘环境音；默认不播放、不下载。
- 音源只使用许可清晰的开放互联网资源或用户主动添加的 URL，显示来源与许可；建立最近播放去重、收藏、音量和定时停止。
- 紧凑浮窗只增加一个低干扰的播放/暂停入口；完整页使用轻量复古音响视觉。音乐账号、歌词、复古磁带机和网络广播不进入本次 MVP。

### 6.4.0 验收与后续边界

1. Cursor 本地额度无需 Keychain 解锁即可手动刷新，并给出真实成功或失败阶段；
2. 一次解锁后 Clash、飞书及所有 API 连接器在本次运行不再分别请求系统密码；退出后内存秘密清空；
3. Clash 能自动识别实际运行控制器端口；控制器不可用时仍显示本地元数据中的剩余流量、来源和时间；
4. API/Clash 设置在当前面板宽度下无截断、无八按钮横排，主操作和错误原因可见；
5. 白噪音默认静音，关闭页面或停止定时器后无残留下载与高 CPU 占用；
6. 发布前完成自动测试、真实 Cursor 只读冒烟、Clash 本机只读联调、钥匙串单次解锁人工回归，并同步 README、使用文档、CHANGELOG、测试报告与安全说明。

自动测试、Release 构建和安装包校验已完成；真实 Cursor、Mihomo 端口发现和钥匙串单次授权仍保留为用户机器上的人工回归项。雨声/海浪等开放互联网音源、排序置顶、SSH 密钥引导不并入本次版本。

## 6.3.1 已交付 · 钥匙串静默访问修复（2026-08-18）

- 启动、后台轮询和页面切换零钥匙串读取；API 凭据只在手动刷新时解锁一次并缓存于本次运行；
- Clash、飞书秘密改为显式读取/保存，输入时不再逐字符写入；所有钥匙串操作禁止弹出认证 UI；
- 旧凭据权限不兼容时提供钥匙串访问修复入口；Clash、飞书支持二次确认后清除 PulseDock 对应项目；
- 临时签名包仍不能保证旧钥匙串 ACL 跨版本继承，正式 Developer ID 签名与公证保留为公开发行前工程任务。

## 6.3.0 已交付 · 交互隔离、可靠定位与可解释连接器（2026-08-18）

- 双击展开移至 AppKit 面板事件层，并排除原生输入/按钮/开关/菜单/滚动控件；窗口尺寸在内容替换前原子设置；
- 当前位置改为单飞请求，支持取消、15 秒超时、过期回调隔离与坐标回退显示；
- 设备详情允许最多四台同时展开；
- Mihomo 控制器增加监听、`/version`、Secret/HTTP 和 provider 数量的诊断入口；
- API 连接器增加 DeepSeek 官方余额类型，并修正 GLM 同类额度的重复标签。

## 6.3.1 已交付范围说明 · macOS 钥匙串授权不重复打扰

- 已确认根因：应用启动时连续读取 Clash、飞书和 API 等多条 Keychain 记录；当前字段在每个字符变化时都会删除并重建记录。若旧记录的访问控制、登录钥匙串状态或开发包临时签名身份发生变化，macOS 会对每一条访问重复要求“登录”钥匙串密码，甚至在应用已显示后继续排队弹窗。
- 改为按需读取：启动不读取秘密；只有用户打开对应设置、启用飞书/控制器、手动刷新相应 API 时才读取一次。刷新任务复用内存中的短暂值，禁止每轮轮询访问 Keychain。
- 改为显式、安全保存：输入框只保留内存草稿；点击“保存安全配置”或离开字段并通过短暂防抖后才写入，值未变化时不做删除/新增。删除配置需二次确认。
- 增加钥匙串修复入口与按功能清除操作：不显示秘密，不要求用户把系统密码交给应用或聊天；更完整的凭据清单页留到公开发行准备阶段。
- 正式发布仍需改用稳定 Developer ID 签名与公证；开发/临时签名包已明确提示旧凭据的访问控制可能无法跨版本稳定继承。

### 验收标准

1. 没有启用秘密功能时启动 PulseDock 零钥匙串弹窗；
2. 启用一项功能时最多出现一次对应授权，之后同一运行会话、定时刷新和界面切换不重复弹窗；
3. 输入 API Key、Webhook、Secret 的中间字符不写入/删除 Keychain；
4. 用户拒绝授权时功能显示“凭据不可读”，基础监控继续正常运行，且不陷入连续弹窗；
5. 用户可在应用内安全移除 PulseDock 自身项目，不影响其他应用钥匙串数据。

以上运行时修复已在 6.3.1 交付；稳定 Developer ID 签名、公证与更完整的凭据管理页将在公开分发准备阶段完成。

## 6.5.0 已交付基础 / 后续 P1 · 可排序、可置顶的个人工作台

- 6.5.0 已为 API 连接器和 SSH 设备建立 `pinned`、`sortOrder`，并交付置顶/取消置顶、上移/下移；顺序仅存在本机非秘密配置。
- 后续把相同机制扩展到工作台模块和娱乐收藏，并增加“自动按异常临时前置”的明确标识。
- 展开态提供可见拖动把手拖拽排序，同时提供菜单中的“上移、下移、置顶、取消置顶、恢复默认顺序”，满足触控板、键盘和辅助功能使用；紧凑浮窗不承担复杂排序交互。
- 工作台卡片支持“自定义布局”模式，用户可排序或隐藏非核心模块；异常、网络与热风险的安全提示仍可按需临时前置，并标明是系统临时优先而不是用户顺序丢失。
- 设置页的“远程设备 · 隐私与任务识别”沿用实际设备排序，并在名称旁显示置顶状态，避免截图中设置页与设备页顺序不一致。

### 验收标准

1. 拖动、上移/下移和置顶后重启应用，API、设备与工作台顺序完全保持；
2. 置顶设备在设备页、设置页和紧凑异常摘要中的相对优先级一致；
3. 同时存在异常与置顶项时，界面说明“异常临时前置”还是“用户置顶”，不产生不可解释重排；
4. 恢复默认顺序只影响当前类别，且需要确认，不删除配置、凭据、设备或历史数据。

## 6.5.0 已交付基础 / 后续 P1 · API 额度卡片的信息层级

- 6.5.0 已修复 GLM 与 Codex 多周期把编号和百分比连写的问题；窗口名称、弱化编号、等宽“已用”和进度条分层展示。
- 每个额度周期改为独立的小型状态单元：周期名称作为标题，窗口编号使用弱化标签（如“窗口 1”），百分比使用等宽数字和明确的“已用”语义，并配一条低高度进度条。
- 月度/MCP、短周期窗口 1、短周期窗口 2 分行或自适应换行；窄窗口优先纵向排列，不允许为了塞进一行而压缩字体或截断关键数字。
- 数字采用可区分 `1`、`I`、`l` 的字体特征与 tabular figures；窗口编号不使用与额度百分比相同的字号、字重和颜色。
- 同一规则应用到 Cursor、DeepSeek 余额、Codex 和后续厂商插件，统一展示“周期、已用/剩余、重置时间、数据来源”，避免各连接器自行拼接长字符串。

### 验收标准

1. `窗口 1，已用 1%` 在 100%、125%、150% 缩放下均不会被误读成 `11%`；
2. 三个及以上额度周期在当前 500 pt 面板宽度内无横向拥挤、截断或重叠；
3. 色盲模式或关闭颜色提示后，仍可仅通过文字、分组和进度值理解状态；
4. VoiceOver 能分别朗读周期名称、窗口编号、已用百分比和重置时间，而不是朗读一整段无结构文本。

## 6.5.0 已交付首期 / 后续独立模块 · 沉浸娱乐页

- **首期已交付：** 六条 CC0 环境音；Radio Browser 工作音乐与网络广播；场景/搜索/收藏/去重/音量/定时；磁带机与网络收音机。默认不播放、不下载、不读取账号或历史。
- 以工作背景音乐为核心，同时提供白噪音、雨声、咖啡馆、自然环境音与网络广播；默认不播放、不预下载。开放声音只接入具有明确许可的目录或用户主动添加的 URL，并保存来源与许可信息。
- 音乐和广播明确区分视觉与交互：音乐为复古磁带机（封面、磁带转动、播放队列、收藏）；广播为老式收音机（台名、频率刻度、信号状态、收藏电台）。紧凑浮窗仅提供播放/暂停、上一项/下一项、音量；完整页提供选择、定时停止和场景。
- 建立去重队列：按音源内容 ID、最近播放和场景分组避免重复。用户收藏和播放历史默认仅保存在本机；开启同步前必须明确授权。
- 歌词默认关闭。用户手动开启后可调整字体、字号、颜色、透明度与显示位置；不抓取受限制来源或将歌词用于训练/再分发。
- Apple Music、Spotify 仅在用户完成官方 OAuth/MusicKit 授权后显示可用账户控制和允许读取的播放历史；QQ 音乐、网易云音乐仅在存在合规官方授权能力时接入，不通过 Cookie 抓取、破解 DRM 或非公开接口绕过限制。
- 番茄钟联动作为第二阶段：用户可分别定义专注、短休、长休时播放/暂停/切换的声音方案；具体自动化规则待用户确认后实现。

### 后续仍需确认的产品边界

1. 首批开放音源优先级：白噪音/环境音、Lo-fi 背景音乐、网络电台三者的排序；
2. 是否优先支持“用户粘贴流媒体 URL”，还是先做开放目录的搜索与收藏；
3. Apple Music 与 Spotify 是否只需展示/控制当前播放，还是需要导入完整收藏与历史；
4. 番茄钟在专注、短休、长休三个阶段各应播放、暂停还是切换到不同声音。

## 下一版 P0 · SSH 可达性、任务识别与可操作修复（待开发）

### P0 · 局域网设备从“同子网”升级为真实可达性判断（2026-08-26 对齐）

- **用户场景：**G1-002 是机器人。Mac 只要处于机器人/配套服务器附近的网络、设备已开机，就可以 SSH。Mac 地址可能是 `192.168.31.x`，机器人是 `10.33.48.138`，中间由路由器提供可达路由；“不在同一 /24”不等于“不在设备环境”。
- **现有缺陷：**6.7.2/6.8.0 通过 `ssh -G` 解析目标后，把目标私网 IPv4 与当前本地接口子网直接比较。这可以静默明显不相关的 Wi-Fi，但会把经本地网关/静态路由可达的机器人误判为“当前网络暂停”。
- **新判断链：**
  1. 使用 `ssh -G <alias>` 只读解析实际 HostName、Port、ProxyJump 与用户，不建立会话。
  2. 通过 macOS 路由表查询目标实际使用的接口、网关和路由类型；有经当前物理网卡或指定 VPN 的有效路由时，不再因地址不同子网而直接暂停。
  3. 在网络变化、手动刷新或进入候选网络时，先执行一次小超时、不登录、不发送应用数据的 SSH TCP 端口探测。端口可连立即认定“处于设备可达环境”，不再要求同子网。不依赖 ICMP/ping，因为设备或网关可能屏蔽 ICMP。
  4. 只有明确无路由、主机不可达，且当前网络不匹配已确认指纹时，才将局域网设备标为灰色“当前网络暂停”。
- **网络指纹学习：**某台局域网设备首次 SSH 成功后，可由用户确认“将当前网络记为 G1-002 可用环境”。仅保存非秘密的本地接口类型、子网、网关、目标路由和可选的用户标签；不保存 Wi-Fi 密码、SSH 密钥内容或远端凭据。默认不依赖 SSID/BSSID，避免为了网络判断强制索取定位权限。
- **状态必须分类：**
  - TCP 可连：“设备在线 · 正在验证 SSH”；
  - 指纹匹配/路由存在，但超时：“设备可能关机或网络不稳定”，先短周期复核，不立即发飞书；
  - `connection refused`：“主机在线，SSH 服务/端口不可用”；
  - 明确无路由且指纹不匹配：“当前不在设备网络范围”，预期暂停；
  - TCP 通但 SSH 认证失败：“网络正常 · SSH 认证需处理”，不误报为机器人离线。
- **冲突与安全边界：**不同地点可能使用相同私网网段，所以不能仅靠网段自动学习；必须结合目标路由和一次真实成功的 SSH 证据，且学习前让用户确认。PulseDock 不修改系统路由、Clash、SSH config 或机器人网络设置。

#### 验收标准

1. Mac 为 `192.168.31.x/24`、G1-002 为 `10.33.48.138`，中间存在可用路由且 TCP/SSH 可达时，必须显示在线，不得因不同子网暂停。
2. 切换到与已确认指纹无关且无目标路由的网络时，30 秒内转为预期暂停，不进入首页红色异常、不记失败次数、不发飞书。
3. 在已确认的 G1-002 网络中关闭机器人，应先显示“可能关机/待复核”；连续失败达到阈值后再告警，恢复开机后只发一次恢复。
4. 分别对无路由、TCP 超时、connection refused、SSH 认证失败、主机密钥变更和成功登录建立确定性回归，每类都有不同的可读原因与告警策略。
5. 学习、删除或更新网络指纹都需要可见操作；配置导出只包含非秘密路由特征，不包含凭据或定位历史。

- 设备页不再只能“单台展开”：提供总览、可同时选择最多 4 台的对比详情、以及单设备完整诊断三种视图；总览始终保留所有设备的关键状态与显存矩阵。
- 对 4090、5090、A100 等硬件采用同一任务采集链路：优先 Slurm/调度器；未安装或无权读取时，在用户授权的“脱敏启动命令”模式下将 `nvidia-smi` PID 与进程摘要关联。没有调度器元数据或可读取命令时，明确显示“无法可靠识别任务”，不猜测训练内容。
- “部分异常”拆分为 SSH、Codex CLI 发现、DNS、TLS、代理、实际 Codex 请求端点、GPU 采集等独立状态；展示脱敏命令、超时阶段、HTTP 状态和一键复制证据。仅“发现 codex 可执行文件”不再暗示端点可用。
- 每一种异常提供可复制的排查步骤；“修复”操作先展示将执行的只读检测或变更预览，必须由用户确认，绝不默认修改远端 shell、代理、systemd、VS Code/Cursor 或全局环境。
- 新增受控 SSH 引导：用户确认主机指纹与远端账号后，PulseDock 可为**单台设备生成专用 ed25519 公钥**，仅在一次性安全密码输入会话中安装公钥；不保存服务器密码。私钥以 `0600` 保存在本机 SSH 目录，口令可选存于 macOS Keychain；每台设备可撤销、轮换并进行连接测试。
- 局域网 / VPN / 公网设备可分别标记；网络切换进入 1–3 分钟宽限期，网络条件不满足时显示“预期离线”并暂停 SSH 轮询。飞书仅在连续失败达到阈值时通知一次，并在恢复时通知一次；支持外出模式暂停所有局域网设备。

### 异常必须可解释、可处理，而不是永久橙色标签

- 设备总体状态拆成 SSH 可达、主机资源、GPU 采集、任务识别、Codex CLI、Codex 网络端点六个维度。A100 的 GPU 与 SSH 正常、只有可选 Codex 端点探测失败时，不再把整台服务器笼统标为“部分异常”；显示“计算正常 · Codex 探测受限”。
- 异常按影响分级：`预期离线`、`监控信息不完整`、`工作负载风险`、`连接故障`、`安全/资源严重异常`。只有影响用户任务或连续达到阈值的故障进入主告警和飞书；可选探针失败不能长期占据最高优先级。
- 每条异常卡必须回答四件事：**哪里失败、依据是什么、是否影响实际任务、现在能做什么**。只有错误字符串而没有动作的异常不允许进入稳定 UI。
- 提供受控动作：立即复查、查看分阶段证据、复制安全命令、在终端打开、标记网络条件、暂时静音该探针、恢复默认监控。安全可逆的本地修复可在预览后执行；远端写入、代理或密钥变更始终需要明确确认和回滚说明。
- `401/403` 若证明真实 Codex HTTPS 端点已完成 DNS/TCP/TLS 并返回 HTTP，应归类为“网络端点可达、未授权探针”，而不是“端点无状态”；超时、DNS、TLS、代理污染与 HTTP 响应必须分别显示。
- 暂时无法自动解决时，保留最后成功值、年龄和影响范围，并允许用户只静音该子探针；不得让一个无法修复又不影响训练的橙色标签永久污染整台设备状态。

### 设备快捷设置浮窗：替代不稳定的悬停二级菜单

- 2026-08-25 实测：设备菜单中的“网络范围”使用 SwiftUI 嵌套 `Menu`。在右侧空间不足时，AppKit 将原生子菜单展开到左侧；鼠标从主菜单移向子菜单时菜单跟踪分支会提前丢失，导致子菜单在用户准备点击时消失。该问题发生在 `borderless NSPanel + SwiftUI Menu + 独立 NSMenu 子窗口` 的交互链，不是网络范围配置本身失效，也不是用户移动方向错误。
- 用户已确认下一版改为**点击打开、可停留的设备快捷设置浮窗**：点击设备卡片的滑杆图标后显示锚定 Popover/浮层，不依赖 hover；鼠标进入浮窗不会关闭。点击外部、按 `Escape`、点击“完成”、删除该设备或切换到另一设备时才关闭。
- 同一时间最多打开一个设备快捷设置浮窗；浮窗标题显示设备名称和当前健康状态，主体直接提供置顶/取消置顶、上移、下移、网络范围四项单选、进程用户名、Slurm 当前用户任务、任务隐私模式、立即刷新和复制诊断。网络范围不再放入二级悬停菜单。
- 网络范围使用明确的单选组并显示辅助说明：自动判断、仅局域网、需要 VPN、公网可达；选中后立即保存本机非秘密配置，并给出轻量确认，不因浮窗关闭而回滚。
- 设备右键菜单可保留为无二级层级的快速操作入口；任何仍保留的 `NSMenu` 都不得包含必须横向移动才能操作的 hover 子菜单。
- 快捷设置浮窗属于交互控件区域，必须从全窗口双击展开/收起和背景拖动命中中排除；在浮窗内点击、滚动、拖动单选项或选择文本时不得移动主窗口或触发收起。
- 支持键盘与辅助功能：滑杆按钮可通过 Tab 聚焦，Space/Return 打开；方向键切换网络范围，Escape 关闭并把焦点还给原设备按钮；VoiceOver 朗读设备名、分组、当前值与操作结果。
- 验收：分别在面板左/中/右侧设备、不同显示缩放和靠近屏幕边缘时连续打开 20 次，浮窗始终完全位于可见屏幕内；鼠标从按钮移动到任一设置项不会消失；切换设备不会残留旧设备内容；所有修改重启后保持；浮窗打开期间主窗口双击和拖动不会误触。

### 验收标准

1. 用户可同时比较多台设备的 GPU 显存，而不需要轮流展开；
2. 设备没有 Slurm、没有权限或 GPU 进程为空时，原因与下一步操作可见；
3. SSH 密钥引导不会持久化远端密码，且必须验证主机指纹、变更预览、连接成功和撤销路径；
4. 更换 Wi-Fi 或 VPN 不会产生 SSH 故障风暴或飞书重复告警；
5. 端点无 HTTP 状态、HTTPS 超时、未发现 CLI 分别给出不同证据与安全的建议，不以自动全局代理修复掩盖问题。

## 6.5.0 已交付核心 / 后续人工回归 · 浮窗双击与稳定布局

- 实际根因是 AppKit 命中测试把 `NSScrollView/NSClipView` 整个后代树都视为交互控件，导致完整态多数卡片背景无法双击。6.5.0 已取消容器级排除，只排除真实控件、编辑文本和滚动条。
- 双击继续由面板级 AppKit 识别；空白、标题和滚动文档普通背景支持切换，输入框、开关、按钮、菜单、Slider、链接和文本编辑不被第二击抢占。
- 展开/收起采用单次状态事务：先计算并设置目标窗口尺寸、约束与可见区域，再切换内容；动画期间不向底层控件传递第二击，完成后才恢复命中测试。
- 工作状态卡的“继续工作”改为固定最小触控区域、不会被压缩的独立控件；窄窗口与放大显示下优先换行或转为图标+提示，禁止文字与开关重叠或裁切。

### 验收标准

1. 连续 20 次从标题、空白区、卡片与滚动区双击切换，窗口尺寸、标题、导航和工作状态卡均不闪烁、裁切或残留旧布局；
2. 双击 `继续工作`、任何 Toggle、按钮、输入框与 Picker 时，只执行该控件的原生语义，不意外展开/收起；
3. 双击空白区、标题区和拖动图标保持可靠展开/收起；
4. 100%、125%、150% 显示缩放下，“继续工作”标签和开关均完整可见且可点击。

## 6.5.0 已交付核心 / 后续人工回归 · 全窗口可发现拖动

- 6.5.0 已由面板记录非交互背景的 mouseDown，移动超过 4 pt 后调用窗口拖动；左侧把手仅保留为视觉提示，不再是唯一入口。
- 紧凑态：状态文字、指标胶囊之间的空白和卡片背景均可按住拖动；展开按钮、异常快捷操作、右键菜单等控件继续执行原功能。
- 展开态：顶部标题区、页面边缘、卡片非交互空白区可拖动；输入框、按钮、开关、Picker、Slider、菜单、链接、可选择文本和 ScrollView 滚动内容不得被窗口拖动抢占。
- 拖动识别与双击展开/收起必须共用一次 AppKit 命中测试：短按仍可单击，双击仍切换尺寸，达到移动阈值后才进入窗口拖动，不能同时触发卡片操作。
- 鼠标进入可拖动背景时使用开放手掌提示，拖动时使用抓取手掌；键盘焦点和 VoiceOver 不依赖拖动手势。

### 验收标准

1. 紧凑态从左、中、右三处非控件背景各连续拖动 10 次，均可稳定移动窗口；
2. 展开态可从标题、边缘和卡片空白处移动，但拖动输入框、Slider、滚动区和可复制证据不会移动窗口；
3. 单击异常胶囊、展开箭头和按钮不发生位移，双击非控件区域不误触操作；
4. 多显示器、不同缩放和窗口贴近屏幕边缘时，拖动后窗口仍可完整找回。

## 6.5.0 已交付核心 / 后续 P1 · 当前位置请求状态

- 当前位置服务在此前版本已具备单飞、request ID、取消和 15 秒超时；旧待办中的根因描述已经过时。
- 6.5.0 新增用户选择 generation：定位中手动选城会取消请求，旧回调即使迟到也不能覆盖手动城市。
- 后续仍需把定位与反向地理编码拆为两个独立超时，并增加可注入 provider 的完整状态机自动测试。
- 为定位与反向地理编码分别设置有限超时；无回调、权限变化、网络不可用、反查失败都必须停止定位、清理任务、复位 UI，并保留最后一次有效手动/当前位置。
- 成功后优先显示城市与区县；反查失败时显示脱敏坐标和“天气已按坐标获取”，不能以“当前位置 · 定位坐标”冒充地名；设置页增加最近定位时间、授权状态、重新定位和取消按钮。

### 验收标准

1. 连续点击“使用当前位置”、授权中再次点击、网络切换中点击，始终最多存在一个活动定位请求；
2. 任意请求在限定时间内无回调时自动恢复可点击状态，并给出“定位超时，可重试”的可读原因；
3. 旧请求晚到的回调不能覆盖后来选择的手动城市或更新后的当前位置；
4. 成功、拒绝、失败、超时、取消后均能再次定位，不出现永久“定位中…”。

## 下一版 P1 · Mihomo 控制器连接自检与可解释同步（待开发）

- 控制器同步前先探测本机地址的 TCP 监听状态，再请求 `/version`；明确区分“端口没有进程监听”“连接被拒绝”“HTTP 401 Secret 错误”“HTTP 400 请求/内核不兼容”“网络超时”和“提供者刷新失败”，不再统一显示“无法连接服务器”。
- 页面提供“检测控制器”而非直接“同步并重读”：显示当前配置的地址、是否监听、内核版本、Secret 是否已配置（不显示内容）、可刷新提供者数量以及最近一次成功同步时间。
- 对配置文件中的 `external-controller` 增加启用前检查说明：保存配置后必须让正在运行的 Mihomo 内核重载/重启，且实际运行配置可能被 Clash Verge 的覆写配置替代；PulseDock 不修改该配置、不开放监听到局域网。

## 6.1.3 已交付质量修复（2026-08-17）

- 全窗口任意可见区域双击切换紧凑/展开，保留可见“收起”按钮作为无歧义入口；
- 时间线改为懒加载并按 60 条分页，避免首次进入时同步构建最多 500 个可选文本卡片；
- GPU 总显存改用 GB，折叠态改为紧凑卡片矩阵，详情区分显存、计算利用率、温度和功耗；
- NVIDIA 进程与 Slurm 采集显示成功、无任务、未安装、超时或错误，不再把缺失信息伪装成“没有任务”；
- 建立更新日志、测试规范、发布检查清单和安全说明，并将文档同步列为发布门禁。

## 6.2.0 已交付 · 输入可靠性、位置来源与额度连接器重构（2026-08-17）

### 已修复范围

- 6.1.3 展开浮窗中的城市、API、飞书、Clash 和 SSH 输入控件在部分实际 macOS 会话仍无法取得第一响应者；现有窗口焦点策略不足以作为交付保证；
- 现有 API 连接器对所有厂商只发 `HEAD` 并读取标准 rate-limit 响应头。GLM Coding Plan 的额度不以这些头公开，故即使 Key 与模型 `glm-5.2` 正确也会显示“不可读”；
- 语言选择只写入 `AppleLanguages` 偏好，绝大部分 UI 文案仍是硬编码中文，不会即时切换；
- 当前显存矩阵已表达核心状态，但下方“GPU 运行详情”重复信息，扩大设备卡片高度；
- 目前仅有细小拖动条，缺少用户可见、可长按拖动的浮窗拖拽图标；
- 天气仅允许手动选城，不能在用户授权后从 macOS Core Location 取得当前位置。

### 交付内容

- 将浮窗交互改为可验证的焦点模型：展开即成为 key/main window；输入控件首击可编辑，支持 `⌘A/C/X/V/Z`；为每类输入新增 UI 回归测试与人工测试脚本；
- 新增明显的“长按拖动”把手图标：按住后开始拖动，短按不移动，双击仍切换展开/收起；
- 保留 GPU 显存矩阵，将温度、功耗与计算利用率合并至单卡矩阵格；移除重复的 GPU 运行详情，任务/诊断按需展开；
- 天气采用“手动城市优先 + 使用当前位置”双路径：仅在用户点击后申请 macOS 定位权限；定位失败时保留已锁定城市，绝不按公网 IP 或 Clash 出口自动跳城；
- 建立真正的运行时本地化：使用 String Catalog / `LocalizedStringKey`，语言切换后立即刷新导航、按钮、状态、设置和空状态；动态证据可保留原文；
- API 连接器按厂商策略化，而非一律 HEAD：保留通用 rate-limit 模式；新增“GLM Coding Plan 用量”专用只读连接器，显示来源、刷新时间、5 小时/每周周期和不可用原因；不以模型调用探测额度，避免产生套餐消耗；
- Cursor 个人额度加入“本地会话实验性”模式：显式授权后只读 `state.vscdb` 中的 access token、仅内存使用、不读取 refresh token、不写库、不进 Keychain/日志/时间线；显示套餐金额、Auto/指定模型用量比例、周期结束和非官方来源提示；团队用户优先走官方 Admin API。

### 验收结果

1. 重新启动、从菜单栏恢复、从 Dock 恢复后，所有输入框均可在第一次点击后输入、全选、复制、粘贴与撤销；
2. 长按拖动图标可稳定移动浮窗，普通单击不会误拖，双击任意位置展开/收起不回归；
3. 8 卡设备折叠态完整显示显存、温度、功耗和利用率，不再出现一组重复的 GPU 详情卡；
4. 定位授权拒绝、无网络或定位失败不会更改已手动选择的天气城市；
5. 切换至英语、繁中、日语或韩语后，无需重启即可切换已覆盖的 UI 文案；
6. GLM 专用连接器不发送聊天/补全请求，不消耗一次 prompt；无法读取时给出可复制的官方来源与原因；
7. Cursor 连接器不持久化 Bearer Token；401 仅重新读取 access token 一次，仍失败提示在 Cursor 内重新登录。

## 6.1.2 已交付稳定性修复（2026-08-16）

- 修复非激活浮窗动态切换导致的双击失效和所有 TextField/SecureField 无法输入；补充标准编辑菜单与窗口焦点回归测试；
- SSH 登录与 GPU/任务采集分层，A100 的慢 `nvidia-smi` 不再被误判为 SSH 离线，并为显存缓存标明时效；
- 新增可见拖动/双击区域，同时保留明确的展开、收起按钮。

## P1 · DeepSeek Harness 与官方余额联动（待确认具体 Harness）

- 首选使用 DeepSeek 官方 `GET /user/balance` 读取 CNY/USD 总余额、赠金与充值余额，API Key 仅存 Keychain；
- 可选发现社区 `deepseek-harness` CLI/MCP，显示版本、`dsh doctor` 健康、API 延迟、错误码、Token 成本与缓存命中率；
- 不让 PulseDock 代理或转发用户提示词，只读取显式输出的结构化观测数据；第三方 Harness 与 DeepSeek 官方 API 来源必须分开展示；
- 需要用户确认所指项目仓库，以及希望“仅监控”还是让 Cursor/Codex 通过 Harness 发起 DeepSeek 请求。

## 6.1 已交付基线（2026-08-14）

- 修复 SSH GPU 进程解析在重复 PID 时触发的 Swift 崩溃：PID 现在按跨卡任务归并，并防止同一 GPU/PID 对重复计数；
- 远程设备改为紧凑总览与按需详情，显存占用成为主指标；支持复制设备诊断和两分钟实时观察；
- 已提供 `设置 → 远程设备 → 隐私与任务识别` 的逐台模式：仅 Slurm/调度器任务（默认）或显示脱敏启动命令；
- Codex 远程探测区分“默认环境未发现 CLI”与“未安装”，并提供发现路径、DNS、TLS、端口拒绝、超时、HTTP 状态等证据；
- 时间线首次启动写入初始化事件，番茄钟、手动 AI 网络诊断和本机状态事件会写入本地账本；账本写入失败可见；
- 展开面板具备标准可激活键盘焦点，诊断、时间线与远程证据支持文本选择复制；
- 社区重置信号区分当前与历史确认度，显示上游生成时间和本机获取时间。

## 6.1.1 已交付稳定性修复（2026-08-14）

- 展开态不再以全局双击手势争抢设备卡片的点击与滚动；标题区双击和“收起”按钮都可可靠缩回浮窗；
- SSH 设备加入单台“立即刷新”、连接过程反馈，以及超时、认证、主机密钥、DNS、路由和拒绝连接的可读原因/建议/原始证据；
- 折叠卡片恢复每张 GPU 的显存可视化，详细任务维持按需展开。

以下 P0 条目保留为后续增强与更完整验收，不代表 6.1 未交付上述基线。

## 6.0 已交付基线（2026-08-12）

- 本地事件时间线：已实现发生、证据、恢复、持续时间和 30 天持久化；
- Clash 可信同步：已实现本机 Mihomo 提供者主动刷新后重读，并保留证据/新鲜度；
- SSH 设备一期：已实现多设备、SSH/负载/内存/磁盘/Codex POST 端点、NVIDIA GPU，以及可选进程归属和 Slurm 当前用户任务；
- 热风险、飞书 Webhook 告警、通用 API rate-limit 连接器、Keychain 秘密、不含秘密的配置备份与五语基础：已交付。

本清单下方保留的条目是未完成的深度验收范围，尤其是 SSH 会话级代理、全局代理污染检测、反向隧道生命周期和可信任务 ETA。

## P0 · 事件时间线可靠性与可见性修复

### 已发现的问题（6.0 实测）

- 时间线页为空时并不代表时间线组件坏了：6.0 只在少数阈值型异常发生时写入事件，例如外网探测失败、Codex/Clash 剩余额度过低、持续高热风险、SSH 设备故障；在完全正常的使用日，账本文件不会被创建；
- 用户已感知的事件没有统一写入：番茄钟完成/跳过/长休息、工作状态切换、网络路径断开/恢复、手动刷新 Clash（未启用 Controller）、手动诊断结果和额度查询失败仍只写到临时诊断列表或通知，不会出现在时间线；
- 当前的“清除已恢复”会一次性删除全部已恢复事件，缺少二次确认、数量提示和“只清除早于 N 天”的安全选项；
- 账本写入错误目前被静默忽略，界面无法区分“尚未有事件”与“本地文件无法创建/写入”。

### 下一版修复方案

- 首次启动即写入一条可关闭的“时间线已开始记录”系统事件，并显示数据文件状态、最后写入时间和当前事件数量；
- 建立统一 `recordTimelineEvent` 入口：网络、番茄钟、工作状态、手动刷新/诊断、额度、Clash、热风险和 SSH 都必须通过该入口写入；正常信息使用 `info`，异常使用 `warning/critical`，恢复与原事件关联；
- 明确区分“状态采样”和“值得回溯的事件”，并为普通信息采用去重窗口，避免每 2 秒采样一次就灌满 500 条历史；
- 对番茄钟至少记录开始、完成、跳过、短休息、长休息及睡眠校正；对网络至少记录路径断开、外网探测失败、恢复；对手动操作至少记录触发时间、来源和结果；
- 账本保存改为可观测：创建目录、JSON 编码、原子写入失败时在时间线页和设置页展示可复制错误，同时保留最近一次内存事件；
- “清除已恢复”改为二次确认，并提供“仅清除 7 天前已恢复事件”与“导出时间线 JSON”选项；
- 时间线页增加筛选（全部/进行中/异常/信息）和首次使用说明，让空状态能解释“当前没有任何已记录事件”的原因。

### 验收标准

1. 全新安装启动后，时间线立即至少显示一条系统初始化事件；
2. 完成或跳过一次番茄钟、断开并恢复一次网络、执行一次手动刷新，均可在不重启的情况下出现对应条目；
3. 重启应用后，已有事件、恢复时间和持续时间仍正确；
4. 将账本路径设为不可写时，页面明确报告保存失败而不是显示“暂无事件”；
5. 连续 24 小时的正常采样不会因重复信息超过 500 条，也不会持续增加 CPU/磁盘写入。

## P0 · 6.0.0 SSH 崩溃、错误证据与键盘交互修复

### 已确认的崩溃根因（2026-08-13）

- 用户提交的 macOS 崩溃报告显示：`Thread 2` 在 `SSHMonitorService.parse` 的 `Dictionary(uniqueKeysWithValues:)` 内触发 `EXC_BREAKPOINT / SIGTRAP`；
- 当远端 `nvidia-smi --query-compute-apps` 对同一 PID 返回重复记录（一个进程可能关联多个 GPU 或驱动返回重复行）时，PulseDock 将 `ps` 元数据直接构造为以 PID 为唯一键的字典；Swift 遇到重复 key 会断言退出，而不是返回可恢复错误；
- 这是本机解析器缺陷，与远端 A100、Codex CLI 或用户输入无关。截图中的两条 GPU 进程正是能够触发该条件的现实场景。

### 已确认的可解释性与交互问题

- 设备卡片把 `HTTP 000`、4xx/5xx、curl 超时、DNS/TLS 失败与远端认证失败合并为“SSH 可用，但 Codex 端点探测异常”，未显示端点 HTTP 状态、命令阶段或受限长度的原始 stderr，因此无法定位原因；
- 当前浮窗保留 `.nonactivatingPanel` 以实现被动置顶。展开时可成为 key window，设置中的 `TextField` 本应可输入，但普通信息 `Text` 不可选择；窗口没有实现一致的 responder chain、`⌘A / ⌘C / ⌘V` 策略，也没有为设备/诊断证据提供文本选中和复制；
- “社区全局重置信号”来自 `https://www.codexrunway.com/api/status.json`，是 Codex Runway 第三方聚合源，不是 OpenAI/Codex 官方接口。6.0 将最近一次历史事件的 `confidence` 与“当前是否有下一次计划”放在同一张卡里：因此会出现“暂无重置计划 + 置信度 97%”。97% 仅表示该历史事件在第三方源中的置信度，**不是**“下一次会重置 97%”，此展示具有误导性；
- 该卡片显示的是最近一次重置日期，而不是数据抓取/生成时间；即使后台每 30 分钟刷新，用户也无法从界面判断源数据本身是否陈旧或是否正使用缓存。
- 2026-08-14 复核表明：上游 JSON 的 `generatedAt` 已更新到当天，但最新事件仍是 2026-08-11 的 `reset_completed`；这说明“最近：8 月 11 日”在当前上游数据中是历史事件时间，并不等于 PulseDock 没有刷新。由于界面没有显示上游 `generatedAt` 与本机请求时间，用户会自然误判为客户端使用旧版本或旧缓存。

### 下一版修复方案

- 将 GPU PID 元数据解析改为重复键安全的归并：同一 PID 只保留一条一致元数据并计数/关联 GPU，不使用会断言的 `Dictionary(uniqueKeysWithValues:)`；解析任意单台设备失败必须降级为该设备“解析失败”，不得使整个应用退出；
- 增加针对“同 PID 出现在多张 GPU”“空 `nvidia-smi` 行”“异常 CSV”“SSH 返回混合 stderr”的单元测试与进程级回归测试；
- 扩展 `RemoteDeviceSnapshot` 为分层诊断证据：SSH exit code/耗时、DNS、TCP/TLS、HTTP 状态、curl 错误类别和安全截断后的 stderr；卡片显示简明原因，并提供“复制诊断证据”；
- 改造浮窗的键盘模型：展开态成为标准可激活 key/main window；输入控件支持 `⌘A/⌘C/⌘V`；状态与证据文本使用可选择文本；全局 `⌘C` 在聚焦证据卡时复制当前条目，`Esc` 收起；紧凑态继续不抢焦点；
- 社区卡拆分“当前计划状态”和“历史事件可信度”：无未来计划时不显示百分比；历史置信度必须标成“最近一次历史事件确认度”；显示“源数据生成于 / 本机成功获取于 / 是否缓存”，并提供直接打开 JSON 与单条原始来源链接；
- 增加明确的新鲜度状态：例如“上游 4 分钟前生成 · 本机 30 秒前获取”“上游超过 6 小时未更新”“正在使用本机缓存”；手动刷新后必须显示 HTTP 结果、响应版本和最新事件时间，避免把“事件旧”误读为“数据旧”；
- 默认将第三方社区卡降级为“观察信号”，不再把它置于与 Codex 官方额度同等显著的位置；可在设置中关闭。

### 验收标准

1. 同一个远端 PID 出现在 1–8 张 GPU 时，应用持续运行，设备页只显示一条去重进程或明确的跨 GPU 关联；
2. `HTTP 000`、401/403、429、5xx、DNS、TLS、curl 超时和 SSH 失败各自有可见、可复制且不含秘密的原因；
3. 展开面板内文本输入框可使用 `⌘A/⌘C/⌘V`，设备/诊断/时间线证据可鼠标选择和复制；
4. 无“下一次计划”时，社区卡不再展示会被理解为预测概率的置信度；
5. 关闭网络使用缓存时，卡片明确显示缓存状态和源数据年龄。

## P0 · 远程 GPU 任务语义、显存优先视图与设备规模化

### 产品目标

从“远端 `nvidia-smi` 指标展示”升级为“开发者能迅速回答哪台机器、谁、在跑什么、占了多少显存、何时结束”的远程计算工作台；同时在少量设备与数十台设备场景下都保持紧凑。

### 已确认的交互决策

- 设置入口：`设置 → 远程设备 → 隐私与任务识别`；
- 逐台设备提供两种互斥模式：
  1. **仅 Slurm/调度器任务（默认）**：显示 Job ID、任务名、所属用户、队列、状态、已运行时间、TimeLimit 与调度器提供的预计结束时间；
  2. **显示脱敏启动命令（高级）**：仅在用户对该设备显式开启后读取进程启动参数，用规则脱敏并展示可读任务摘要；
- 高级模式必须在开启前说明：它可能包含脚本名、模型/配置标签或项目目录片段；默认关闭，设备删除、模式关闭或切换账户后立即停止采集；
- 不支持“猜测训练任务”。没有 Slurm/调度器元数据、任务主动上报或被授权的命令摘要时，明确显示“无法可靠识别训练任务”。

### G1-002 / Codex 探测修复

- 将“Codex --”改为“默认 SSH 探测 PATH 中未发现 Codex”，不得表述为“未安装”；
- 增加受控的只读路径发现：系统 PATH、`~/.local/bin`、常见 npm 全局 bin、已授权 Conda/uv 路径；不加载用户 shell 初始化脚本、不执行 nvm/asdf 任意脚本；
- 分开展示 CLI 发现状态和云端端点状态。`HTTP 000`、DNS、TLS、代理拒绝、超时、401/403、429、5xx 必须分别给出原因与可复制证据；
- 设备页提供“重新探测（深度）”动作，并标示采样时刻、耗时与探测版本。

### A100 / GPU 视图重构

- 默认从 GPU 利用率主导改为**显存占用主导**：显示已用/总显存、整体显存汇总、繁忙 GPU 数与显存高水位；GPU 利用率退为次要数值或状态点；
- 每张 GPU 的显存条按进程/任务分段，明确标出框架/驱动保留显存与可归属显存；跨 GPU 同一 PID/同一 Job 只归并为一个任务，展开后查看每张 GPU 明细；
- 任务卡优先展示任务名、用户、GPU 列表、总显存、运行时长、调度器 ETA；PID 仅作为次级诊断字段；
- 数据刷新分层：已展开或高显存/高负载设备 5–10 秒，折叠设备 30–60 秒，用户手动开启观察模式时短暂 2 秒；每项显示精确“x 秒前更新”，失败时指数退避；
- 多设备总览采用紧凑列表/表格：健康、SSH、GPU 数、总显存、繁忙卡数、主任务、更新时间；点击设备进入详情抽屉或详情页，避免每台 8 卡设备默认撑满页面；支持按异常、显存占用、最近活动、设备组筛选排序。

### 脱敏启动命令规则

- 仅读取被选择设备上的目标 GPU 进程；不扫描无关进程、日志、环境变量、文件内容或数据集；
- 默认隐藏绝对路径中的用户目录、Token/Key/Secret、URL 查询参数、`--password`、`--token`、`--api-key`、`--config` 值中的敏感片段；
- 界面默认显示归纳摘要，例如“`train.py · model=… · config=…`”，并提供用户确认后的“查看脱敏原文/复制”动作；
- 对非当前 SSH 用户的任务，是否允许读取脱敏命令由设备管理员显式授权开关控制；无授权时退回到 Slurm 元数据；
- 任务所有者可通过主动上报提供更准确的实验名、阶段、epoch、loss 与 ETA；这优先于命令行推断。

### 验收标准

1. 同一训练进程跨 8 张 A100 时，总览只显示一个任务，显存总量与各 GPU 分布可展开核对；
2. 默认 Slurm 模式不读取进程完整命令行；高级模式必须逐台开启并能看到脱敏结果与采集来源；
3. G1-002 中 Codex 位于常见用户级路径时显示“已发现 + 实际路径来源”；找不到时显示“默认探测环境未发现”，不再暗示未安装；
4. 十台各八卡的服务器在总览中不需要滚过 80 条 GPU 行；点击任一设备后才加载完整 GPU/任务详情；
5. 已展开设备的显存变化在目标刷新间隔内反映，界面显示数据新鲜度与失败原因；
6. 无可靠 ETA/任务名来源时绝不生成猜测性结果。

## P0 · 多 SSH 远程设备的 Codex 可用性监控

目标：在本机浮窗与“设备”页面中统一监控多台 SSH 远程设备，快速区分“主机离线”“SSH 连接失败”“远端网络/代理故障”“ChatGPT/Codex 请求流中断”“远端 Codex 服务或认证异常”。

### 计划中的体验

- 用户可添加多台设备：显示名称（如 `G1-002`）、Host、端口、用户名、SSH 配置别名，以及启用状态；
- 每台设备显示独立健康状态、最后成功检测时间、延迟、当前出口/代理摘要、Codex 可用性与最近错误摘要；
- 对具备授权与兼容驱动的 GPU 服务器，显示 GPU 型号、利用率、显存已用/总量、温度、功耗限制状态及每张卡上的计算进程；NVIDIA 优先通过只读 `nvidia-smi` / NVML 数据获取，其他显卡按远端系统实际可提供能力降级显示；
- 显示任务归属的最小必要信息：操作系统用户名、PID、GPU 占用、显存、进程运行时长和（存在调度器时）任务 ID/队列状态；默认不展示命令行参数、环境变量、文件路径、训练数据或项目名称；
- 任务剩余时间只在可靠来源存在时显示：Slurm/PBS/LSF 等调度器给出的 time limit/预计结束时间，或由任务/训练脚本显式上报的 ETA；没有可靠来源时明确显示“无法可靠估算”，不根据 GPU 利用率猜测；
- “在训练什么”分三级授权显示：默认仅显示进程类别（Python、PyTorch、容器等）；设备管理员可允许显示经脱敏的进程标签；仅任务所有者明确选择后才可读取训练脚本主动上报的实验名、阶段、epoch/step、loss、ETA 等元数据；
- 设备列表支持按异常优先排序；紧凑浮窗仅在有异常时显示最高优先级的一台远程设备，避免挤占主机信息；
- 对类似 `stream disconnected before completion` 的问题，保留可复制的分层诊断证据：SSH 连通性、DNS、TLS、`chatgpt.com/backend-api/codex/responses` 连通性、代理环境、远端 Codex 本地服务状态；
- 将“SSH 可连”与“Codex 可提问”明确分为不同状态：SSH 首连/重试成功率、连接耗时、远端端口监听、代理 TCP 可达、真实 Codex POST 端点可达、流式首字节超时、认证状态分别展示；
- 同时识别远端 `~/.vscode-server`、`~/.cursor-server` 与 CLI 启动的 Codex 服务，显示实际进程继承的代理地址；当 `HTTP(S)_PROXY` 指向不存在或失效的本地端口时，直接标为“代理进程未监听”，而不是笼统报“正在思考”；
- 对 SSH 反向隧道显示生命周期：监听端口、拥有会话、最后成功字节、失效原因（端口占用、连接拒绝、隧道断开、本机代理未启动），并区分“临时会话转发”和“常驻服务转发”；
- 新增“会话级 Codex 代理”模式：为单台 SSH 主机专门启动 Codex 会话时，临时建立反向隧道，并且**只**给该次远端 `codex` 进程注入 `HTTP_PROXY`、`HTTPS_PROXY` 与 `ALL_PROXY`；Codex 退出后自动关闭隧道、清理临时监听端口与会话状态；
- 设备页必须把代理隔离状态做成可读证据：是否存在全局代理污染（例如访问内网 `172.18.x.x` 被错误送到 `127.0.0.1:17900`）、代理端口是否监听/是否 connection refused、隧道属于临时会话还是常驻、真实 `chatgpt.com/backend-api/codex/responses` POST 是否可达，以及 SSH 首连超时后的重试恢复；
- 分别展示 shell、VS Code、Cursor 与 Codex CLI 进程实际继承到的代理变量；当它们的值不一致时明确标出影响范围，而不是将所有远端故障归因于“梯子”；
- 支持以“稳定性探测”方式连续执行 3–5 次低频健康检查，记录首连超时、重试恢复与抖动率，避免单次成功掩盖 SSH 网络不稳定；
- 提供安全的“修复建议”而非默认改写远端：检测到需要常驻隧道时提示安装/加载本机 LaunchAgent；检测到远端代理配置不一致时提供可复制的计划与回滚点；
- 多设备采用错峰、低频轮询和手动“立即诊断”，避免同时 SSH 连接导致本机或远端负担过高；
- 首版只做只读诊断：不执行远端重启、不修改代理、不读取远端项目文件或聊天内容。

### 安全与实现边界

- 默认复用用户本机 `~/.ssh/config` 与 ssh-agent/Keychain；不把私钥、密码、Token 或完整远端环境变量写入 PulseDock 数据文件；
- 会话级代理严禁默认写入远端 `~/.bashrc`、`~/.profile`、systemd 用户环境、VS Code/Cursor 的 Node 启动器或任何全局环境；它只在用户显式启动的单次 Codex 子进程范围内生效，避免影响 SAM3、导航、内网模型服务及其他用户服务；
- 首版不支持密码自动输入；需要密码的主机应由用户配置 SSH Key 或通过 agent 登录；
- 每个远端命令设置超时、最小权限与固定白名单，不执行用户拼接的 Shell 片段；
- GPU、用户与任务信息属于多用户服务器敏感运行数据：设备需由管理员显式启用相应采集项；PulseDock 不绕过 Linux 权限、容器隔离或调度器 ACL，不读取其他用户的日志、命令行、环境变量、数据集或模型文件；
- 远端 Codex “额度”仅在远端存在官方可读 app-server 且 SSH 会话已认证时显示；否则明确显示“无法读取”，不尝试绕过认证或抓取 Cookie；
- 对无法判定的流式中断，只报告证据与最可能层级，不声称能够证明是 OpenAI 服务端故障。

### 首版验收标准

1. 可同时添加至少 5 台 SSH Key/agent 认证的设备；
2. 某台设备离线或 SSH 失败时，30 秒内在设备页显示故障，紧凑浮窗出现异常摘要；
3. `chatgpt.com`/Codex 流请求检测失败时，报告必须包含远端时间、错误层级和可复制原始摘要；
4. 普通健康设备的检测不会每秒建连；默认间隔暂定 60 秒并错峰；
5. 所有远端监控均可逐台停用、删除，并可查看上次检测的数据新鲜度。
6. 对 G1-002 这类场景，设备页能在不读取私钥、密码、Cookie 或订阅内容的前提下，把“SSH 抖动、反向隧道失效、远端代理端口拒绝、错误的 VS Code/Cursor 服务环境、真实 Codex 流请求失败”区分开来。
7. 启动会话级 Codex 代理后，远端新 shell、导航/SAM3 进程及内网模型服务请求均不含 `HTTP_PROXY`、`HTTPS_PROXY`、`ALL_PROXY`；只有本次 Codex 子进程使用临时代理。
8. 退出 Codex 后，对应远端临时代理端口和反向隧道必须消失；远端 Codex 请求可用时需给出可验证结果。
9. 出现故障时仅提供可复制、可回滚的修复建议与证据，不直接改写任何全局配置。
10. 对已授权的 NVIDIA 服务器，设备页可展示每张 GPU 的利用率、显存与温度，以及最小必要的任务归属；对未授权/不支持的系统明确说明数据不可用原因。
11. 任务 ETA 与训练内容均必须标注来源（调度器、任务主动上报或不可用）；无来源时不得生成猜测性剩余时间或暴露其他用户任务细节。

### 进入开发前仍需一起确认

- 远程设备是只配置 SSH Host，还是允许额外填写跳板机/ProxyJump？
- 远端的 Codex 主要运行在终端 CLI、ChatGPT 桌面 app，还是两者混用？
- 对 G1-002，是否允许在远端执行一次只读的 HTTPS/Codex 探测命令，以便区分流断发生的位置？
- 是否将“部署远端专用代理/常驻隧道”保留为用户显式确认后的修复操作，并把默认产品能力限制为只读诊断与一键生成修复脚本？
- 会话级 Codex 代理首版是否只支持终端 Codex CLI；VS Code/Cursor 内置 Codex 将仅检测环境而不注入代理，还是需要单独授权的启动方式？
- 目标服务器是否使用 NVIDIA GPU、Slurm/PBS/LSF 等调度器，还是 Docker/Kubernetes/裸进程？不同运行方式决定 ETA 与任务名称能够读取到的可靠程度。
- 你是否拥有这些服务器的管理员权限，或是否能让管理员为 PulseDock 创建一个只读监控账户/最小权限命令白名单？

## P1 · 无风扇 Mac 热风险与能耗监控

目标：面向 MacBook Air 等无风扇设备，识别“单次 CPU 看起来正常、但长时间能耗和温度累积导致机身过热”的工作负载，并将其转化为可行动的降温建议。

### 计划中的体验

- 显示当前热状态、芯片温度、温度趋势（升温/持平/降温）与持续高负载时长，而不只显示瞬时温度；
- 汇总 macOS 可公开读取的能耗代理指标：CPU/GPU 占用、唤醒频率、功耗/能耗影响（系统可提供时）、电池供电状态与电池消耗速率；
- 提供“热风险”而非伪精确瓦数：正常、升温中、高热风险、系统限速/严重四档，并说明触发证据，例如“温度连续 10 分钟上升，Cursor + 浏览器占用持续偏高”；
- 列出高影响应用/进程的短期排名，优先识别编译、Docker、浏览器标签、Cursor/Codex 本地任务等；不读取项目内容、终端命令或网页内容；
- 在浮窗中仅在高热风险时接管主状态，提示暂停高负载任务、连接电源/移至通风表面或结束最耗能应用；
- 提供可选的静默策略：充电、合盖后唤醒或专注时不频繁通知；所有阈值允许调整。

### 实现边界

- Apple Silicon 对第三方开放的精确整机瓦数与逐进程 GPU 功耗有限；首版必须明确标注“估算热风险/系统能耗影响”，不伪造精确功耗；
- 优先使用 macOS 的公开 API/系统命令可获得数据；对需要特权或私有接口的数据，显示“系统未提供”而不是要求关闭安全保护；
- 不做自动结束应用、自动降频或修改电源设置，首版只监测、解释和提示。

### 首版验收标准

1. 连续高负载时能显示温度趋势与持续时长，且不会因一次短峰值误报；
2. 在高热风险时，浮窗给出最主要的高影响应用和一句可执行建议；
3. 低负载时不增加明显后台开销，采样默认不高于现有 2 秒系统指标频率；
4. 无法读取精确功耗时，界面明确为风险/影响估算。

## P1 · 飞书告警出口（群机器人 Webhook）

目标：将真正需要及时处理的 PulseDock 异常推送到指定飞书群；不把飞书变成另一套复杂后台。

### 计划中的体验

- 首版采用飞书群自定义机器人 Webhook 与可选签名密钥，不要求用户授权飞书账号、不读取飞书聊天/文档/日历；
- 仅推送已启用的高价值事件：远程 SSH/Codex 故障、AI 网络诊断失败、Codex 额度低、持续高热风险、番茄钟完成/下班提醒；
- 卡片消息包含设备名、发生时间、简短证据、当前状态和“已恢复”通知；诊断报告可复制，不上传敏感环境变量、IP 全量、私钥、Token 或项目内容；
- 按事件类型支持静默时间、冷却时间与恢复通知，避免同一个断网故障反复刷屏；
- 设置页提供“发送测试消息”和本地发送记录，方便确认 Webhook 是否可用。

### 实现边界与验收

- Webhook 与签名密钥只保存在本机 Keychain，不写入导出文件、日志或诊断报告；
- 默认关闭，用户明确粘贴 Webhook 后才启用；
- 同类告警默认冷却 30 分钟，恢复后只发一次恢复消息；
- 测试消息、失败重试与禁用后的行为都必须可见、可控。

## P2 · 事件时间线、告警策略与可恢复配置

这些是我建议与下一版功能一同规划、但可按工作量拆分交付的产品基础能力。

### 事件时间线

- 将网络、SSH 设备、额度、Clash、温度/热风险、番茄钟等事件按时间线汇总；
- 一次故障可展开查看当时的延迟、出口、代理状态与恢复时间，便于回溯“为什么刚才 Codex 不能用”；
- 本地保留有限历史并支持一键复制，默认不上传。

### 告警策略

- 所有通知统一经过“首次触发、冷却、升级、恢复”规则；
- 支持工作/午休/下班、专注中、勿扰时段分别配置，不让热风险或网络抖动干扰休息。

### 数据健康与配置备份

- 在设置页显示 PulseDock 自己的采样状态、上次成功刷新、错误原因与本机资源占用；
- 支持导出/导入不含秘密的偏好设置（主题、工作时间、阈值、设备定义）；Webhook、SSH 私钥/口令等秘密始终要求重新配置。

## P2 · 多厂商 API 额度与速率限制监控

目标：在用户明确选择厂商并配置只读凭据后，统一展示其 API 的余额、赠金、周期用量、剩余速率限制和刷新时间；不把 ChatGPT/Codex 订阅额度与 API 账单额度混为一谈。

### 计划中的体验

- 按厂商添加连接器：用户选择公司、填写名称与必要凭据后，PulseDock 仅调用该厂商公开、文档化的官方接口；
- 能显示什么由厂商能力决定：预付费余额/赠金、组织或项目用量、预算上限、当前窗口 rate limit、下一次刷新时间、最后成功查询时间；
- 没有官方余额接口时，明确显示“该厂商未提供可读取余额”，可退化为解析 API 响应头里的 rate-limit 信息或让用户手动填写预算上限；不爬控制台、不读取浏览器 Cookie、不调用逆向接口；
- 低余额、预算接近上限、速率限制接近耗尽时进入异常优先级，并可选发飞书通知；
- 默认低频查询（例如 15–60 分钟，按厂商限额调整），并支持手动刷新、来源和新鲜度展示。

### 安全边界与验收

- API Key、OAuth refresh token、组织 ID 等秘密只保存于 macOS Keychain；导出配置、诊断报告、日志和飞书通知均不得包含秘密；
- 尽可能要求只读/账单查询权限，避免使用拥有模型调用、项目删除或成员管理权限的万能 Key；
- 每个厂商连接器必须列明官方接口、所需权限、刷新频率和不可读取字段；
- 用户提供具体厂商后再评估其官方文档，决定是否可以接入，不能承诺所有公司都能读取“余额”。

## 后续候选项

- 常用/高级设置分层；
- 通过用户显式授权的飞书自建应用读取日历/待办（不进入 Webhook 首版）。
