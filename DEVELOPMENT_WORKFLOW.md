# PulseDock 开发协作流程

本文定义开发对话与发布对话的职责边界。它不替代代码审查、自动化测试或用户的最终验收。

## 对话边界

- **本对话：**需求整理、问题诊断、方案设计、开发、测试结果汇总、修复迭代、版本说明与开发计划维护。
- **Locate Windows version 对话：**只负责将已通过本对话验收的版本上传到 GitHub、整理发布产物与发布状态；不在上传对话中擅自修改产品行为或绕过测试门禁。

## 模型职责

| 阶段 | 主模型 | 职责 | 产物 |
| --- | --- | --- | --- |
| 需求与质量门禁 | Sol | 汇总已交付、待解决和截图证据；明确根因、方案、约束、验收项；评审 Luna 的失败结果 | `BACKLOG.md`、测试清单、实施说明 |
| 实施 | Terra | 按 Sol 已确认的范围修改代码和文档；不扩大需求；运行本地构建与相关测试 | 可审查的代码、变更说明、构建产物 |
| 验证 | Luna | 运行快速、可重复的单元/集成/回归测试；提交最小可复现失败证据，不擅自把失败修成通过 | 测试结果、日志、复现步骤 |
| 收敛 | Sol + Terra | Sol 判断失败属于测试、实现或需求缺口；Terra 修复；Luna 重跑受影响测试，直到验收满足 | 通过的测试报告与发布候选 |

## 每个版本的闭环

1. Sol 将需求写成可验证条目：问题、根因、设计、非目标和验收标准。
2. Terra 仅实现已确认条目，并同步更新更新日志/用户文档/测试项。
3. Luna 先运行针对性测试，再运行完整回归；失败结果必须包含命令、版本、最小日志和截图/复现条件。
4. Sol 审核失败证据并决定修复方案；不得用“看起来正常”代替原因分析。
5. Terra 修复后由 Luna 复测；完整测试、Release 构建、签名与安装包完整性通过后才交给发布对话上传 GitHub。

## 当前版本执行记录：常驻额度汇总

- **Sol 已完成方案审查：**额度仅统一展示，不统一抓取或跨单位计算；Codex 以本机官方只读来源接入“额度连接器”，不复制到 API 快照、不使用保险库 API Key。
- **Terra 已实施：**内建 Codex 的幂等迁移、常驻排序、工作台完整列表、小窗口前两项加 `+N`、保险库“等待解锁”状态与自动化回归。
- **Luna 已完成独立复测：**旧版升级唯一 Codex、不同单位不合计、锁库零请求、刷新代际、0/1/2/6 项布局、UUID 碰撞与迁移幂等、完整测试脚本和签名构建均通过。测试中发现的五个边界均已反馈 Sol、最小修复后再次通过。

## 适用原则

- Sol 负责复杂规划与最终质量判断；Terra 负责平衡成本与可靠性的实际开发；Luna 用于高频、可重复的测试与回归。
- 不将凭证、私钥、Token、Cookie 或 SSH 输出中的敏感数据交给测试报告或发布对话。
- 发布对话只能发布明确版本号、校验值和测试报告已经齐全的 ZIP；发现发布后问题时回到本对话重新进入上述闭环。
