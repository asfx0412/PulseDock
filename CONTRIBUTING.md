# PulseDock 开发协作约定

## 仓库职责

本目录是 macOS 版本的唯一工作区，仓库内只接受 macOS 实现。

产品方向是可扩展的 AI 开发者多任务百宝箱，而不是把功能永久限制为单一诊断器；但每个功能域必须先完成来源、状态、恢复、性能与验收闭环。当前只维护 macOS。

## 每次改动必须同步

1. 用户可感知变化写入 `CHANGELOG.md`。
2. 未完成需求与验收标准写入 `BACKLOG.md`。
3. 操作方式变化更新 `outputs/PulseDock使用文档.md`。
4. 数据源、隐私或凭据变化更新 `DATA_SOURCE_CATALOG.md` 与 `SECURITY.md`。
5. 产品语义变化更新 `PRODUCT_SPEC.md`。
6. 架构或持久化变化更新 `ARCHITECTURE.md`。
7. 执行测试、构建、签名和解压复验，生成对应版本测试报告。

## 版本规则

- 修复且不改变数据契约：补丁版本。
- 新功能或向后兼容模型扩展：次版本。
- 不兼容配置、事件或凭据格式：主版本，并提供迁移与回滚说明。
- 版本号、构建号、ZIP 名、README、使用文档和测试报告必须一致。

## 安全审查

提交前搜索 Token、API Key、Webhook、订阅 URL、SSH 地址和未脱敏命令。示例只使用明显的占位值；真实密钥不得进入源码、Markdown、截图索引或测试输出。

## 测试协作

优先运行确定性工具测试。允许把日志整理、文档核对和简单 fixture 审阅委派给更快、低成本的模型，但架构、安全、凭据、远程写操作和发布结论必须由主开发流程复核。需求讨论和待办更新不重复跑完整 Release；准备交付时才执行完整门禁。
