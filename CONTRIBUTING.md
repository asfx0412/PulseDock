# 贡献指南

## 仓库职责

本仓库只接受 macOS 实现。提交前请先说明问题、预期行为、支持范围和验证方式；涉及隐私、凭据、远程连接或外部数据源的修改，应同时说明安全边界。

## 每次改动必须同步

1. 用户可感知变化写入 `CHANGELOG.md`。
2. 操作方式变化更新 `outputs/PulseDock使用文档.md`。
3. 数据源、隐私或凭据变化更新 `DATA_SOURCE_CATALOG.md` 与 `SECURITY.md`。
4. 架构或持久化变化更新 `ARCHITECTURE.md`。
5. 为改动提供可重复的验证步骤；维护者会在合并或发布前完成内部验收。

## 版本规则

- 修复且不改变数据契约：补丁版本。
- 新功能或向后兼容模型扩展：次版本。
- 不兼容配置、事件或凭据格式：主版本，并提供迁移与回滚说明。
- 版本号、构建号、ZIP 名、README 和使用文档必须一致。

## 安全审查

提交前搜索 Token、API Key、Webhook、订阅 URL、SSH 地址和未脱敏命令。示例只使用明显的占位值；真实密钥不得进入源码、Markdown、截图索引或测试输出。

## 测试协作

优先运行确定性工具测试。架构、安全、凭据、远程写操作和发布结论必须由维护者复核；不要把秘密、真实 SSH 输出或未脱敏诊断提交到仓库。
