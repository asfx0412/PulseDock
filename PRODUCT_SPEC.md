# PulseDock 产品规范（平台无关）

## 1. 定位

PulseDock 是面向 AI 开发者的轻量、多任务“工作百宝箱”。诊断是不可削弱的核心能力：它负责回答“我能否稳定工作、哪里出了问题、如何恢复”；额度、远程设备、生产力、天气、专注声音和后续工具则围绕真实工作流程扩展。产品不以收缩为单一监控器为目标，也不以堆叠互不完整的按钮为目标，而是允许多个功能域并存，并要求每个进入稳定区的功能形成完整闭环。

长期架构允许接入不同 AI 平台、服务和任务类型；当前实际开发、测试和发布仍只面向 macOS。平台扩展与当前支持范围必须分开描述。

## 2. 体验契约

- 紧凑态只展示一个主状态和少量关键胶囊；异常优先于正常指标。
- 平衡紧凑态的额度语义为“最多一个用户置顶的 API/Codex 额度 + 独立 Clash 剩余额度”；Clash 流量不占额度连接器置顶名额。
- 完整态承载工作台、洞察、设备、时间线、诊断、设置与声音。
- 双击非交互区域切换紧凑/完整；输入框、按钮、开关、选择器和滚动区域不得被全局手势抢占。
- 用户必须能从系统托盘/菜单栏、任务栏/Dock 和窗口菜单恢复或退出。
- 所有在线数据必须展示来源、更新时间和失败原因；缓存不得冒充实时。

## 3. 功能域

| 功能域 | 必须行为 | 禁止行为 |
|---|---|---|
| 本机状态 | CPU、内存、GPU、温度、热风险、网速、延迟 | 把估算功率写成传感器精确值 |
| AI 网络 | DNS/TCP/TLS/代理/规则/真实端点分层诊断 | 仅以普通网页可开判断 Codex 正常 |
| 额度 | Codex、Cursor、厂商套餐/余额按数据源插件展示 | 调用模型消耗额度来“探测额度” |
| 公网出口 | 国家/城市、代理或直连观测路径；IP 与服务商放入详情 | 将公网 IP 定位当作天气、物理位置或局域网设备范围 |

额度采用分层呈现：设置页是详细信息与连接配置入口，应显示来源返回的各周期/组成、已用与剩余、重置时间和认证说明；工作台每个来源只显示一个可快速扫读的主要剩余值；小窗口最多显示一个用户置顶的 API/Codex 摘要，并独立保留 Clash 剩余额度。三处共享快照，不重复采集。
| Clash/Mihomo | 本地订阅元数据为基线，控制器可选刷新 | 读取/上传订阅 URL 或默认改配置 |
| 远程设备 | 多 SSH、GPU 显存矩阵、任务与健康证据 | 保存远程密码、读取训练日志、猜任务 |
| 生产力 | 活跃排行、工作/午休/加班、番茄钟 | 读取窗口标题、网页、聊天或键盘内容 |
| 时间线 | 发生、证据、恢复、持续时间、本地保留策略 | 把 Token、Webhook、主机秘密写入事件 |
| 通知 | 本地与飞书的首次异常/恢复、冷却去重 | 网络切换时制造告警风暴 |
| 专注声音 | 环境音、工作音乐、网络广播分型；默认静音、来源可见、可停止、可定时 | 自动播放、建立离线库、抓账号/Cookie/播放历史、使用来源不明素材 |

## 4. 状态模型

每个监控对象至少输出：`state`、`observedAt`、`source`、`freshness`、`evidence`、`recoveryHint`。状态统一为 `healthy / degraded / failed / unavailable / checking / stale`。UI 不得用空白或 `--` 隐藏已知错误。

## 5. 刷新与资源预算

- 本机指标：2 秒；网络探针：8 秒；活动计时：1 秒。
- Codex、Cursor、Clash、API 额度：每次运行显式解锁后默认 5 分钟；天气和社区信号：30 分钟。
- SSH 普通轮询按设备错峰；观察模式可 5 秒且限时 2 分钟。
- 无用户播放时不得创建音频解码器；声音只允许单流播放。
- 网络目录最多返回 30 条；只接收 HTTPS 与系统可解码格式；目录失败不得阻塞监控或主线程。
- 后台请求必须可取消、有限超时、避免重入；切页不得触发重复全量采集。

## 6. 安全契约

秘密集中保存为一个平台安全保险库；每次运行由用户主动解锁一次后仅在内存使用。拒绝、锁定或迁移失败时基础监控继续运行。配置导入/导出永不包含秘密。

## 7. 平台范围

当前产品范围仅为 macOS。平台无关的数据契约作为架构参考，不代表对其他平台的支持承诺。

## 8. 6.6 媒体契约

- `MediaKind` 固定为 `ambient / music / radio`；环境音循环、音乐流按列表切换、广播按直播流处理，不能共用“播放结束即 seek(0)”策略。
- `PlaybackState` 固定为 `idle / loading / playing / paused / stalled / failed`；只有播放器真正进入播放态才显示“播放中”。
- 播放器全局单例；切源使用 generation 隔离旧回调；退出、停止与定时结束必须释放流和观察者。
- Radio Browser 只是第三方目录，不为电台内容、地区许可、节目单或永久可用性背书。
- 合法空搜索结果是成功状态；目录分页使用真实 offset，不能用后续镜像错误覆盖空结果。
- 智能直连是当前音频会话的最小权限回退，只能绑定 loopback 并访问已验证公共 HTTPS MP3/AAC；不得修改系统/Clash 配置或放宽 ATS。
- Apple Music、Spotify、QQ 音乐、网易云音乐账号接入与歌词不属于 6.6.0；未来接入必须走官方 SDK/OAuth，不读取浏览器 Cookie。
- 环境音、工作音乐、网络广播的目录均固定为 **2 列 × 3 行、每页 6 条**，并共享发现、搜索、收藏、网络模式和分页的信息架构；三者数据源和搜索含义可以不同，但不得让环境音长期停留为没有发现/搜索/网络状态的二等页面。环境音只有通过许可、内容和链接核验的条目才能入库，禁止用重复或不明来源音源凑页数。
- 三类当前播放器占用相同布局尺寸，但视觉语义必须分开：环境音是与声景对应的动态场景，工作音乐是复古磁带机，网络广播是复古收音机。相同尺寸不等于复用同一皮肤。
- 环境音动态场景优先使用低成本程序化动画，而不是持续解码动态壁纸视频。动画只在正在播放且播放器可见时运行；低电量、降低动态效果、窗口不可见或热状态升高时自动降帧/静止，不影响音频。

## 9. Codex 数据可信层级

1. OpenAI 官方本机 `codex app-server`：账户、额度窗口、重置券、Token 活动。
2. 本机 Codex 会话的脱敏活动统计：仅可称“本机活动”，不能与账户额度相减。
3. ChatGPT 未公开 backend-api：若未来作为实验降级源，必须明确标注，默认不启用。
4. Codex Runway 社区 JSON：只显示公开社区动态，不参与个人额度或倒计时计算。
