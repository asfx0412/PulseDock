# PulseDock 更新日志

本文档采用 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 风格，记录用户可感知的新增、变更、修复与安全调整。版本日期能够从构建记录确认时才填写；早期没有可靠构建日期的条目不会虚构日期。

## [Unreleased]

### 变更

- 项目后续聚焦 macOS。

## [6.12.3] - 2026-08-28

### 新增

- 全局快捷键从六个固定菜单项改为 macOS 式按键录制器：点击后直接按组合键，`Esc` 取消，支持字母、数字、方向键、功能键与常用编辑键。旧版预设自动迁移。

### 修复

- 录制期间暂时注销旧全局快捷键，避免用户按键时窗口被意外隐藏。两项快捷键重复、组合无效或被 macOS/其他应用占用时立即提示，并恢复上一组有效设置。
- 浮窗内的原生输入框在点击其他控件或卡片空白后正确结束 field editor；蓝色焦点环和插入光标仅在真正待输入时显示。

## [6.12.2] - 2026-08-28

### 修复

- 局域网 SSH 设备不再被限制为与 Mac 处于同一 IP 子网。当私网目标存在经活动物理网卡的专用主机/网段路由时允许只读 SSH 探测；无专用路由时再以 2 秒 SSH 端口握手作为补充证据。默认路由、VPN 虚拟网卡和公网地址不会被误认为局域网。
- 只读 SSH 监控强制清除别名中的本地/远程/动态端口转发，避免因用户 SSH 配置中的转发端口已占用而产生警告或额外网络副作用。

## [6.12.1] - 2026-08-28

### 变更

- 在展开窗口左上品牌区的副标题旁显示小号版本胶囊；版本号直接读取应用包的 `CFBundleShortVersionString`，避免界面文字与实际构建版本不一致。

## [6.12.0] - 2026-08-28

### 新增

- 设置页新增两组可配置的全局快捷键：显示/隐藏与展开/收起。隐藏状态按展开快捷键会直接召唤完整窗口；冲突或注册失败会显示明确原因。
- 天气新增固定地点与自动跟随当前位置两种模式；自动模式按窗口活跃状态节流，并以约 2 公里和行政区变化共同防抖。
- 网络音频增加有界首包预检、系统/智能直连路径证据、五分钟来源冷却、最近成功来源优先及 DNS、TLS、连接、超时、流中断、格式、HTTP 分类。

### 变更

- AI 网络诊断对普通 TLS、ChatGPT 和 OpenAI API 执行三轮低频采样，使用中位数与范围判断性能；单次冷连接不再直接建议切换节点。
- 中文地区目录保留国家与语言定向检索，并增加中文/普通话/粤语补充索引、两个受限后续批次、本机已审核收藏补充与可见来源说明；未接入许可不明的网页流。
- GUI 回归增加连续 100 次内容区双击压力断言，继续保证只有外侧真实空白和拖动把手可切换窗口状态。

## [6.11.0] - 2026-08-27

### 修复

- 中文地区目录在 `language=chinese` 结果不足一页时，以相同国家代码补查 `mandarin` 与 `cantonese`，并在安全筛选后继续读取两个受限的目录批次；港澳台与全球中文不再被单一语言标签或首批目录结果截断。
- 播放模式统一为行业通用的顺序循环 `repeat`、随机 `shuffle` 和单条循环 `repeat.1`；旧“固定电台”持久化值向后兼容，但不再显示图钉图标。
- 控制栏将弹性留白移至音量和右侧定时/播放模式之间，播放模式后不再遗留大段空白；定时胶囊增加内边距，分钟文本不会贴边。
- 完整内容画布、卡片、按钮、开关、菜单、滑杆、输入与滚动区域全部禁止背景双击；只有窗口外侧的真实空白边缘和显式拖动把手可切换紧凑状态。

## [6.10.3] - 2026-08-27

### 修复

- 中文地区目录改为服务端 `countrycode + language=chinese + is_https + hidebroken` 定向查询，再本地做安全与语言复核；不再先按全地区热门台取样而漏掉中文台。
- 全局双击仅在外侧真实留白生效，完整内容画布均受保护；解决定时、传输、分页等 SwiftUI 控件未映射为 `NSControl` 时仍会收起的问题。
- 播放控制将定时与模式紧靠音量槽，移除音量后的大段无意义空隙；定时改为紧凑自绘按钮，列表循环图标替换为更清晰的循环箭头。

## [6.10.2] - 2026-08-27

### 修复

- 修复港澳台中文和全球中文误把“一次全球热门查询”当作多地区目录的逻辑。现在针对每个地区并发读取公开目录、合并去重后再校验国家与语言；单一地区暂时失败不会清空其它地区结果。

## [6.10.1] - 2026-08-27

### 修复

- 恢复正常非交互背景双击收起，并增加 SwiftUI 无障碍动作树判定。按钮、开关、菜单、滑杆、分页、卡片操作、滚动和输入框优先于全局手势，双击它们不会缩小浮窗。
- 广播地域目录将候选读取量从 30 条提高到 120 条，再在本地用国家代码和中文语言元数据筛选；大陆、港澳台与全球中文不再因浅层候选而只剩少量卡片。

## [6.10.0] - 2026-08-27

### 修复

- 广播“大陆中文”改为严格的 `CN` 国家筛选并本地复核；新增“港澳台中文”和“全球中文”，不再把任意 `chinese` 语言台伪装成本地台。
- 工作音乐目录增加音乐语义筛选，排除 FM、新闻、谈话和播客等广播型结果；工作音乐搜索不再只是网络广播的另一套皮肤。
- 智能兼容播放失败保留系统路径与智能直连两段证据，不再由后一次失败覆盖为笼统的“网络连接中断”。
- 定时器改为单按钮循环，右键才打开常用值/自定义菜单；播放模式改为单图标单击切换，移除重复下拉箭头。传输图标视觉尺寸缩小，命中区保持可点击。
- 控件操作后短时间内的背景双击不再收起浮窗，避免定时、菜单、分页等连续操作被全局手势抢走。
- 应用活跃排行默认仅前 5 条，可展开完整内存排行并在 15 秒无交互后自动收起；不重新请求或改变统计范围。
- 天气当前位置保留精确坐标并优先展示反向地理编码得到的区/县/镇、城市、省和国家；不再强制降级为约 1 km 的粗略坐标。
- 删除 Codex Token 卡片无意义的内部实现说明，图表 X 轴降为至多两个稀疏刻度，避免窄浮窗日期遮挡。
- AI 诊断在 OpenAI/ChatGPT 均可达但较慢时显示“AI 路径较慢，未发现服务不可用”，不再直接断言节点失效或要求切换 Clash。

### 测试

- 增加大陆中文/港澳台中文目录语义回归；完整确定性测试、Release 编译、签名、arm64、ZIP 完整性及解包后的签名复验通过。

## [6.9.1] - 2026-08-26

### 修复

- 修复环境音“发现”分类仍被两列弹性网格拉成半屏长条的问题。分类现改为与紧凑标签一致的左对齐自动换行流；“全部”和所有声景类型均按内容宽度呈现，不再出现灰色长条。

### 测试

- 新增紧凑发现标签流的 Release 编译回归；完整脚本测试、签名、arm64 与 ZIP 解包校验通过。

## [6.9.0] - 2026-08-26

### 变更

- 环境音视觉由按名称猜测的通用动画改为由已审核目录显式标注的场景类型。雨、城市晚雨、雷雨、海浪、溪流、森林、壁炉、暖炉、咖啡馆、图书馆、键盘、列车、城市夜色、虫鸣与棕色噪音各有独立构图；具象声音不再回退为棕色频谱柱。
- 环境播放器保留与磁带机、收音机相同尺寸，但改用低饱和“微缩声景”Canvas：播放时 10 FPS、暂停或 Reduce Motion 时静态，不下载视频、不创建额外音频引擎。
- 播放控制拆为传输、音量、定时/模式三个固定槽位；窄宽度使用确定的两行布局，而不是动态挤压。

### 修复

- 修复音量图标切换和滑杆弹性宽度导致定时器、循环按钮横移、播放器看似变形的问题。静音为真实 0 音量，恢复后回到静音前的音量。
- 进一步加固浮窗原生文本输入：命中共享 field editor 时仍识别为编辑控件；编辑器已附着或正在输入中文/日文/韩文 marked text 时不再被 SwiftUI 状态回写覆盖。远程设备、天气、API、Mihomo 与声音搜索的普通文本输入统一使用原生编辑桥。

### 测试

- 新增环境声景样式数量、目录 ID 到样式映射、静音为 0 与恢复原音量的确定性回归。
- 完整脚本测试、Release 编译、签名、arm64、ZIP 完整性和解包后签名复验通过；输入法、音量拖动和场景视觉仍保留真实 GUI 人工验收。

## [6.8.0] - 2026-08-26

### 新增

- 诊断页新增本地界面响应监测：滚动记录主线程 P95、P99、最大延迟和超过 500 ms 的次数，同时显示最近一次系统指标后台采样耗时。只保留内存中的延迟数值，不记录点击坐标、输入内容或窗口标题。
- 中国工作日历增加可点击月历 popover，以 7 列网格区分普通工作日、周末、法定休假和调休上班，并显示官方文号、来源与接下来的假期/调休。
- 环境音目录从 6 条扩充到 24 条不重复的 Freesound CC0 录音，每条保留作者、作品页、许可页、provider ID、时长、标签和核验日期；增加 9 个可实际筛选的声景类别。

### 修复

- 系统指标读取移入专用 utility 串行队列，避免 CPU、GPU、网速、HID 温度、电池和代理检查直接阻塞 AppKit 主事件循环；温度降为 8 秒采样并复用缓存值。
- 取消定时器的互斥单击/双击手势，单击现在立即循环 15/30/60/关闭，自定义时长改用独立菜单，不再等待系统双击超时。
- 非激活浮窗首击统一交由 hosting view 和 panel 的单一激活链路；真实 `NSTextField` 在编辑期间不接受 SwiftUI 回写，保留中文/日文/韩文 marked text、光标和活动选区。
- Radio Browser 翻页使用原始候选批次判断是否仍可后翻，不再因 HTTP、私网 URL、编码或重复过滤后只剩 6 条而误禁用下一页。
- Codex Token 图改用真实 `Date` X 轴与最多 4 个 `MM/dd` 稀疏刻度，点击/拖动显示完整日期和 Token 值；删除与设置连接器重复的 Cursor 洞察卡。
- Clash 流量单位明确为 GiB/MiB，并在界面说明二进制计算；例如总额 1024 GiB 减已用 80.9 GiB，剩余约 943 GiB。
- Codex 紧凑卡文案从含糊的“最紧张窗口”改为“最低剩余额度周期”。

### 测试

- 新增主线程延迟滚动窗口与故意停顿捕获测试、24 条环境音来源/许可/唯一性测试、Codex 日期刻度与节假日调休测试，以及非 key 浮窗输入框的 field editor、光标和系统选区回归。

### 已知限制

- 命令行的 AppKit 焦点测试不能完全代替真实简体拼音/第三方输入法、多屏首击与 10 分钟 GUI 压力验收；诊断页数据用于用户复现时提供可量化证据。

## [6.7.2] - 2026-08-25

### 修复

- “仅局域网”不再以“当前有 Wi-Fi/以太网”粗略判断可达。PulseDock 先用系统 `ssh -G` 只读解析 SSH 别名对应的目标主机，再比较目标私有 IPv4 与 Mac 当前活动私有 IPv4/子网；范围不匹配时显示灰色“当前网络暂停”，不进入紧凑浮窗主告警、不增加失败次数、不发送飞书。
- 设备从公网/自动切换为局域网后立即清除旧的红色 SSH 快照，避免异步范围判断完成前首页仍短暂显示旧故障。
- 单台手动刷新也执行相同的网络范围判断，不再绕过局域网/VPN 策略直接连接。
- Wi-Fi 到另一个 Wi-Fi 的路径更新会立即重判全部设备；SSH 探测结束时再次核对范围，避免探测期间切网后由迟到失败覆盖灰色暂停状态。

### 变更

- 远程设备卡在名称下方显示自动、局域网、VPN 或公网图标与文字。公网设备保持跨网络探测；局域网设备只有在目标地址属于当前本地子网时才执行 SSH。
- 范围判断明确不使用 Clash 出口、公网 IP 或地理归属；`ssh -G` 仅展开本机 SSH 配置，不建立连接也不写配置。

### 测试

- 新增同一 `/24` 子网允许探测、不同子网暂停探测和四类范围标签的确定性回归；完整测试、Release 编译、签名与 ZIP 校验作为发版门禁。

## [6.7.1] - 2026-08-25

### 修复

- 纠正 6.7.0 文本焦点实现：先让真实 `NSTextField` 成为响应者，再配置它已经附着的 field editor；解决偶发只能输入英文、中文 marked text 不上屏、光标消失和选区呈非活动灰色的问题。
- 分页加入相邻数字页、前后 1/2 页与指定页输入；页码直接映射目录 offset，按需读取候选，不预取或下载全部音频。
- 工作音乐发现从 4 类扩为 10 类，广播从 4 类扩为 11 类；环境音的六个发现项改为真正可点击的声景筛选。
- 定时器改为图标加剩余分钟；单击严格循环 15/30/60/关闭，双击独占打开自定义设置，不再同时触发单击。
- 播放模式使用 `repeat.1`、`repeat`、`shuffle`、`pin.fill` 图标菜单并保留完整文字和悬停说明。
- 环境声景去掉通用彩色粒子，根据雨、雷、水下、风/森林和棕色噪音分别绘制克制的低频动画。

### 说明

- 环境音在线目录仍需选择可长期依赖、允许再分发且可被 AVPlayer 安全播放的数据源；本版不使用重复音频或仅凭搜索摘要写入未经验证的播放链接。

## [6.7.0] - 2026-08-25

### 新增

- 声音页三类内容统一为每页 2 列 × 3 行的六卡信息架构；环境音也具备发现标签、搜索、网络模式、分页骨架与独立播放器。
- 环境音新增低成本程序化动态声景播放器；工作音乐保持复古磁带机，网络广播保持复古收音机，三者同尺寸但不再共用错误皮肤。
- 播放控制改为图标优先；定时器单击循环 15/30/60/关闭，双击可设置 1–240 分钟；音量槽改为弹性宽度。
- Clash 用量同时展示已用、剩余、总计及百分比，避免与 Clash Verge 的“已用/总量”口径混淆。

### 修复

- 修复浮窗把文本首击与面板激活抢占的问题：文本控件在窗口成为 key 后异步取得 field editor，中文输入法组合文本与插入光标不再由自定义窗口手势覆盖。
- 修复工作音乐/广播分页以过滤后数量错误判断目录结束的问题：每页显示六张卡，但向目录请求足量候选并按真实 offset 翻页。
- 修复浏览声音分类会改变正在播放队列的状态错配；浏览类型与 Now Playing 类型分离，播放模式作用于真实播放队列。
- 远程设备不会因未认证 Codex HTTP 探测未返回状态而标记为“部分异常”；SSH/GPU 采集与端点探测改为独立状态，5xx 和采集缺失才降级。

### 已知限制

- 首批环境音仍只有六条经过许可和播放链接核验的 CC0 录音；分页结构已就位，但不会用重复或来源不明的音频凑数量。
- 中文输入法已具备 AppKit 焦点自测；仍需在目标 Mac 上分别以简体拼音、第三方输入法和窗口非活跃首击完成发布前人工回归。

## [6.6.0] - 2026-08-25

### 新增

- 新增声音网络模式：智能兼容、跟随系统、仅直连；智能兼容可在系统路由持续缓冲后，对当前公共 HTTPS MP3/AAC 流启用会话级本机直连中转。
- 工作音乐与网络广播新增目录端分页、收藏元数据恢复和按媒体语义区分的播放模式。
- 远程设备新增可停留快捷设置面板，集中提供置顶、排序、网络范围、进程用户名、Slurm、任务隐私、刷新和复制诊断。
- Clash Verge 新增受限 Unix Socket 控制器发现与 provider 同步。

### 修复

- 声音搜索使用原生 AppKit 文本字段，修复无插入光标、中文输入法无法组合以及页面刷新覆盖 marked text。
- 修复 Radio Browser 合法空结果继续轮询镜像、最终被 TLS 错误覆盖的问题。
- GLM 刷新增加单飞、业务 envelope、数字字符串和字段演进兼容；失败时保留并标记上次成功快照。
- 设备级联菜单改为 popover，修复鼠标移向二级“网络范围”菜单时菜单消失的问题。

### 安全

- 音频直连只绑定随机 `127.0.0.1` 端口和会话随机路径，拒绝私网、localhost、userinfo、HTTP 降级和不安全重定向；停止或换台立即清理。
- Clash Unix Socket 只允许已知 Clash Verge 本机路径，不接受任意文件路径，也不在命令行传递 Secret。

## [6.5.0] - 2026-08-24

### 新增

- Codex 官方额度支持多个独立窗口，按真实时长显示“5 小时额度”“每周额度”“月度额度”等名称，并为每个窗口分别显示已用、剩余、完整刷新时间。
- 新增 Codex 官方 `account/usage/read` Token 日活动和累计摘要；旧版 app-server 未实现时只降级活动区，不影响额度。
- 声音页新增“工作音乐”与“网络广播”，通过 Radio Browser 第三方公开目录搜索 HTTPS MP3/AAC/HLS 流；提供场景、搜索、收藏、上一条/下一条、音量和定时停止。
- 工作音乐使用低频动画的复古磁带机界面，广播使用老式网络收音机界面，并明确缓冲状态不等同于 FM 信号。
- SSH 设备和 API 连接器支持置顶、上移与下移；设备新增自动、局域网、VPN、公网网络范围。
- 新增 6.5 Codex/媒体确定性测试，覆盖多额度窗口、Token 活动、`rateLimitsByLimitId`、HTTP 音源拒绝、格式过滤与目录去重。

### 变更

- Codex app-server 查询改为短生命周期的两项只读请求，分别按请求 ID 收集；实现依据 OpenAI 官方文档独立编写，不复制 Codex Runway 的 AGPL 源码、OAuth、界面或资产。
- 环境音、工作音乐和网络广播统一由一个播放器协调；默认不播放、不自动恢复、不保存播放历史，不建立离线音频资料库。
- SSH 全量刷新最多并发三台；局域网/VPN 条件不满足时显示“当前网络暂停”，不反复尝试连接。
- NWPathMonitor 观察到网络路径变化后给予 SSH 90 秒恢复宽限；远程异常连续三次出现后才写入时间线并触发飞书。
- GLM 多额度周期改为独立卡片，窗口名、弱化编号、已用/剩余与进度条分层展示，不再连写成 `#1 1%`。

### 修复

- 复核用户提供的 6.0.0 崩溃报告，确认旧版因 SSH/GPU 解析重复 PID 时构造重复字典键而触发 Swift `SIGTRAP`；该根因自 6.1.0 起已按 PID 跨卡归并。
- 进一步移除当前解析器中 GPU UUID 的 `Dictionary(uniqueKeysWithValues:)`，远端驱动重复输出 GPU 行时安全去重，避免同类前置条件崩溃。
- 回归样本同时包含重复 GPU UUID、跨卡重复 PID 和重复 `ps` 元数据，验证显存不重复累计、任务正确归并且进程持续运行。
- SSH stdout/stderr 改为进程运行期间并发排空并限制总量，避免管道填满导致假超时；非法 UTF-8 使用替换字符保留证据。
- GPU/PID/显存字段加入范围、有限数和饱和求和保护；缺失采集 marker 明确降级为“采集数据不完整”。
- 修复展开态整个滚动容器被误判为交互控件、导致多数区域不能双击的问题；普通背景同时支持直接拖动，输入框、按钮、滑杆、菜单和文本编辑仍不被手势抢占。
- 修复定位过程中手动选城后，旧定位回调可能再次覆盖用户城市的问题。

### 性能

- 无播放时不创建播放器或音频请求；目录查询最多 30 条，不批量预取音频或封面。
- Codex stdout 限制为 1 MiB，SSH stdout/stderr 分别限长，防止异常子进程无限增长内存。

### 安全

- 所有网络媒体只接受 HTTPS；Info.plist 不增加全局 ATS 放行。
- Radio Browser 明确标记为第三方目录；不抓取 Apple Music、Spotify、QQ 音乐、网易云音乐账号、Cookie、收藏或历史。
- Codex 监控不消费重置券、不自行刷新登录 Token、不写回 Codex 认证文件。

## [6.4.2] - 2026-08-19

### 修复

- 统一保险库解锁不再自动枚举旧版 Clash、飞书和 API Keychain 项，消除一次解锁后继续排队弹出密码框的代码路径。
- 非交互安全存储查询同时设置禁止交互上下文和失败策略；后台、切页和轮询仍不访问 Keychain。
- 增加 `LSMultipleInstancesProhibited` 与运行时已有实例唤醒，避免重复进程争用安全存储。

### 新增

- 环境音页上线六条真实 CC0 网络录音，提供播放/暂停/停止、音量、定时停止及来源许可。
- 新增完整开发历史、产品规范、架构与数据源目录。

### 变更

- 旧凭据迁移改为用户主动选择；只导入无需系统交互即可读取的项目。
- 环境音只播放单条在线流，不持久下载、不保存播放历史，退出应用立即停止。

## [6.4.1] - 2026-08-19

### 修复

- 修复 6.4.0 升级后旧版 GLM、DeepSeek、飞书与 Mihomo 凭据没有迁移、额度和告警配置看似消失的问题；一次显式解锁后仅静默导入可读旧项，不排队弹出多个系统密码框。
- 已有 API 卡片新增密钥重新填写与“保存并刷新”，无需删除连接器重建；飞书增加就近保存入口。
- Mihomo 自动发现改为逐个验证候选地址，明确区分端口未监听、非兼容端点、Secret 不匹配和可用控制器；失败时不再把候选地址伪装成已发现结果。
- Clash 本地订阅额度与控制器状态分离，控制器不可用时继续显示本地剩余量、来源和更新时间。

### 变更

- 下线 6.4.0 的合成白噪音、粉红噪音和风扇声，停止创建音频引擎；真实环境音在许可、素材差异和试听质量达标前不再提供播放按钮。
- 构建脚本显式适配当前 Swift 6.3.3 编译器与 6.3.2 SDK 接口版本差异，不修改系统工具链。

### 安全

- 凭据迁移和解锁后的保存均采用禁止认证 UI 的 Keychain 路径；无法静默读取的旧项只提示用户重新填写一次，不触发逐项授权。

## [6.4.0] - 2026-08-19

### 新增

- 新增统一“凭据保险库”：本次运行只解锁一次，Clash、飞书与 API 连接器共用内存凭据，退出后自动清空。
- 新增“声音”页首版：白噪音、粉红噪音与风扇声均为本机实时合成，默认不播放、不下载。
- Clash 控制器新增自动发现，检查 Clash Verge 配置候选和实际本机回环监听端口。

### 修复

- 修复 Cursor 本地额度被误判为“凭据尚未解锁”、手动刷新不发起请求的问题。
- Clash 设置拆分为订阅用量与本地控制器卡片，显示当前剩余量、来源、新鲜度与连接证据，不再使用横向拥挤按钮。
- API 连接器改为卡片式状态、单项刷新与折叠添加表单。

### 安全

- 不再逐项读取多个 Keychain 项目；旧分散凭据不会在后台自动迁移或读取。新数据只保存为一个版本化 PulseDock 保险库项目。

## [6.3.1] - 2026-08-18

### 修复

- 启动、后台定时刷新和普通页面切换不再读取 macOS Keychain，修复应用启动后连续出现“登录钥匙串”密码框的问题。
- Clash Secret、飞书 Webhook/签名密钥改为用户明确点击后读取或保存；输入过程只修改内存草稿，不再逐字符删除和重建钥匙串项目。
- API 连接器只在用户手动刷新时尝试读取一次凭据，随后复用本次运行的内存副本；后台五分钟刷新不再触碰钥匙串。
- 修正钥匙串失败状态未显示真实 OSStatus 的文案错误。

### 安全

- 所有钥匙串读、改、删操作强制使用非交互模式：旧凭据权限不兼容时显示修复说明，不再召唤或排队系统密码框。
- Clash 与飞书增加带二次确认的“清除保存”，只删除 `com.pulsedock.monitor` 下对应凭据；清除飞书凭据会同时关闭告警。
- 增加自动回归门禁，检查初始化/后台启动路径没有钥匙串读取、秘密输入字段没有逐字符写入、非交互策略没有被移除。

## [6.3.0] - 2026-08-18

### 新增

- 新增 DeepSeek 官方账户余额连接器，仅访问 `GET /user/balance`，不发送模型请求。
- 新增 Mihomo 控制器检测，显示端口未监听、Secret/HTTP 错误、内核版本和可刷新提供者数量。
- SSH 设备详情允许同时展开最多四台。

### 修复

- 全局双击改为 AppKit 面板级命中测试；原生按钮、开关、输入框、菜单与滚动区不再被第二击抢占。
- 展开/收起先原子调整窗口尺寸再重排内容，修复工作台卡片控件偶发错位或裁切。
- 当前位置改为单飞状态机，新增取消、15 秒超时和过期回调隔离；定位反查失败会显示脱敏坐标。
- GLM 多个同类短周期额度现在编号或使用上游名称，不再显示成两个无差别“5 小时”。

### 安全

- DeepSeek API Key 仅存 macOS Keychain；余额探测使用专用 HTTPS 官方域名，拒绝自定义主机。

## [6.2.0] - 2026-08-17

### 新增

- 新增可见手势拖动图标：按住约 0.18 秒后可拖动浮窗；双击图标仍展开或收起。
- 新增“使用当前位置”天气入口，仅在用户点击后申请 macOS 定位权限；使用 Apple MapKit 将坐标解析为城市。
- API 连接器新增 GLM Coding Plan 只读用量模式，调用官方用量查询而非模型补全请求。
- API 连接器新增 Cursor 本地额度实验模式：只读本机 Cursor access token 并请求当前周期用量。

### 变更

- GPU 显存矩阵现在同时展示显存、计算利用率、温度与功耗；移除下方重复 GPU 运行详情，保留任务与诊断按需展开。
- 语言选择改为运行时 SwiftUI Locale 注入；已本地化的导航、按钮与设置会立即切换，不再仅修改下次启动的偏好。
- API 连接器改为按厂商类型选择：通用 rate-limit、GLM Coding Plan、Cursor 本地额度。
- API 额度连接器统一为启动时查询、每 5 分钟只读刷新；不会用额度探测模型。

### 修复

- 展开面板在首击输入控件时主动激活为 key/main window，修复部分 macOS 会话中城市、API、飞书、Clash 和 SSH 输入框无法编辑的问题。

### 安全

- Cursor 模式不读取 refresh token，不写入 Cursor 数据库，不将 access token 保存到 Keychain、配置、日志、时间线或诊断报告。
- GLM 专用连接器不调用 `chat/completions`，避免为了探测额度而消耗 prompt。

## [6.1.3] - 2026-08-17

### 新增

- 新增全局双击切换：紧凑/展开状态下，卡片、按钮、导航、滚动区和空白区域均可双击切换；可见拖动条仍支持拖动与双击。
- 远程任务区新增采集状态，明确区分 Slurm 无任务、未安装 `squeue`、GPU 进程查询超时、没有计算进程和采集未完成。
- 新增时间线分批加载，默认只创建最近 60 条视图，可继续加载更早记录。
- 建立 `CHANGELOG.md`、`TESTING.md`、`RELEASE_CHECKLIST.md` 与 `SECURITY.md` 工程文档基线。

### 变更

- GPU 总显存从庞大的 MiB 数字改为 GB；单卡显存改为四列矩阵，以颜色、百分比和已用/总量同时表达。
- 展开后的 GPU 详情改为两列运行信息卡，展示利用率、温度和功耗，不再重复绘制八条大型显存进度条。
- “仅 Slurm / 调度器任务”模式会实际执行当前 SSH 用户的 `squeue` 只读查询；“脱敏启动命令”模式只显示经授权的 GPU 进程摘要。

### 修复

- 修复时间线首次点击因最多 500 个可选文本视图同时创建而卡顿数秒的问题。
- 修复脱敏任务为空时没有原因说明、用户无法判断是设置问题还是远端采集超时的问题。
- 修复设备总览和详情重复展示同一组 GPU 显存条导致页面冗长的问题。

### 性能

- 时间线改用懒加载和 60 条分页，降低切页时的主线程布局与文本选择开销。

## [6.1.2] - 2026-08-16

### 新增

- 新增可见拖动条；小窗背景或拖动条可双击展开，展开后可双击标题/拖动条或点击“收起”。
- 新增标准编辑菜单，输入框支持 `⌘A`、`⌘C`、`⌘X`、`⌘V`、`⌘Z`。
- 新增窗口焦点自动化回归测试和可指定 SSH 别名的真实只读冒烟测试。

### 变更

- SSH 登录和远程详细采集分层；“已登录但 `nvidia-smi`/任务查询慢”不再等同于设备离线。
- GPU 详细采集超时时保留带时间戳的上次显存值，但不保留旧任务列表。

### 修复

- 修复运行时切换 `.nonactivatingPanel` 导致 SwiftUI 输入框无法成为第一响应者的问题。
- 修复 API、飞书、Clash、天气和 SSH 配置输入框无法输入的问题。
- 修复繁忙 A100 上 `nvidia-smi` 超过旧版 18 秒总超时、被误报为“SSH 不可用”的问题。

## [6.1.1] - 2026-08-14

### 新增

- 每台 SSH 设备新增“立即刷新”“复制诊断”“观察 2 分钟”和“展开详情”文字按钮。
- SSH 失败增加认证、主机密钥、DNS、路由、拒绝连接与超时分类，并提供恢复建议和原始证据。

### 修复

- 修复仅有微小图标且没有操作反馈、用户无法判断按钮用途的问题。
- 折叠设备卡片恢复单卡显存可视化，详情改为按需展开。

## [6.1.0] - 2026-08-14

### 新增

- 远程设备支持“仅 Slurm/调度器任务”和“显示脱敏启动命令”两种逐台隐私模式。
- 支持两分钟高频观察、错峰轮询、按设备刷新间隔、诊断证据复制。
- 时间线首次启动建立账本，并接入番茄钟、手动网络诊断和本机状态事件。
- 社区重置信号显示当前计划、历史事件置信度、上游生成时间与本机获取时间。

### 修复

- 修复同一 GPU PID 跨卡出现时，使用重复字典键导致 Swift 运行时崩溃的问题。
- Codex 探测不再把“非交互 SSH PATH 未发现”直接称为“未安装”。
- 展开浮窗可成为键盘窗口，远程证据与脱敏命令支持选择复制。

### 安全

- 启动命令隐藏 Token、API Key、密码和用户目录；默认仍为最小披露模式。

## [6.0.0] - 2026-08-12

### 新增

- 新增本地事件时间线，统一记录网络、远程设备、额度、热风险、Clash 和番茄钟的发生、证据、恢复与持续时间。
- 新增多 SSH 设备健康中心，读取负载、内存、磁盘、Codex 端点、NVIDIA GPU、显存、可选进程归属和 Slurm 任务。
- 新增 MacBook Air 热风险分级，结合温度趋势、持续负载和 macOS 热状态。
- 新增 Mihomo 本地控制器主动同步、飞书群机器人告警、通用 API rate-limit 连接器。
- 新增简中、繁中、英语、日语、韩语基础本地化和不含秘密的配置导入/导出。

### 安全

- 飞书 Webhook、签名密钥、API Key 和 Clash Secret 存入 Keychain，不进入配置导出和事件账本。
- SSH 探测使用固定只读命令，不保存远程密码或私钥。

## [5.1.4]

### 修复

- 主题由会撑宽页面的分段控件改为色卡网格与紧凑菜单，修复设置页左右裁切。

## [5.1.3]

### 新增

- 主题扩充为云雾、海盐蓝、薰衣草、抹茶、森林、晚霞、暖沙、午夜蓝、石墨和自定义。
- 新增主题深浅滑杆与深色背景文字方案。

### 修复

- Clash 刷新至少保留短暂旋转反馈，避免点击后看起来没有响应。

## [5.1.2]

### 修复

- 双击展开后自动限制到可用屏幕范围；展开内容改为滚动布局，顶部控制不再被裁切。

## [5.1.1]

### 新增

- 新增五套初始主题和可保存的自定义背景色。
- 天气、Codex、Clash、社区信号和公网 IP 刷新按钮统一加载动画与防重复点击。
- 工作日改为独立可点击按钮并即时保存。

## [5.1.0]

### 变更

- 紧凑浮窗重构为主状态加次级胶囊，新增 330px 极简和 390px 平衡密度。
- 网络异常、额度低和番茄钟进行中可接管主状态；正常状态降低视觉噪声。

### 新增

- 诊断报告支持一键复制；各数据卡显示来源与更新时间。

## [5.0.5]

### 修复

- 菜单栏入口改为固定宽度 `● PD`，并在启动后延迟重建两次。
- 新增“修复菜单栏入口”。

## [5.0.4]

### 修复

- Codex 下次刷新显示完整日期和时间。
- 修复 Dock/菜单栏模式切换后状态项对象存在但图标消失。
- 明确 Cursor 个人额度没有稳定官方读取接口。

## [5.0.3]

### 修复

- 菜单栏入口增加 `PD` 文字兜底；“仅置顶当前桌面”改名为“只在当前桌面显示”。

## [5.0.2]

### 修复

- 排除应用前增加二次确认；排行榜空状态可恢复全部排除应用。

## [5.0.1]

### 修复

- 展开浮窗可获得键盘焦点，首次修复天气城市无法输入；紧凑态保持不主动抢焦点。

## [5.0.0]

### 新增

- 新增所有普通前台应用的今日、7 天和 30 天有效活跃排行、应用排除与恢复。
- 新增多事件键鼠空闲判断，排除锁屏、睡眠和长时间停顿。
- 新增 Codex 官方额度窗口和 reset credits、Codex Runway 第三方重置信号。
- 新增手动天气城市、温度/体感温度、日出日落、月相和中国法定节假日。

## [0.x–4.x] - 根据产品历史重建

以下为早期迭代里程碑，缺少可验证的独立构建号，因此不伪造成精确版本记录：

- 建立 macOS 轻量浮窗原型，显示 CPU、内存、网络速度、延迟、温度和公网 IP/地区。
- 增加代理/VPN 场景识别，天气城市改为用户手动选择，不跟随代理出口 IP。
- 增加菜单栏、程序坞、始终置顶、当前桌面、透明度、右键退出与双击展开/收起。
- 增加 Clash 本地订阅流量、Codex/Cursor 额度可行性说明、番茄钟、工作/午休/加班与周末状态。
- 逐步确立“面向 AI 开发者的轻量工作状态浮窗”产品定位，而不是复刻通用系统监控工具。
