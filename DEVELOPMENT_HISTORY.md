# PulseDock 完整开发历史

本文件是项目的长期开发档案，记录“为什么做、做过什么、后来为什么调整”。精确的逐版本变更见 `CHANGELOG.md`；尚未交付的需求见 `BACKLOG.md`。

## 产品起点：轻量 macOS 状态浮窗

最初目标是在 macOS 上做一个高级、轻便、始终可见的浮窗，解决 Codex 经常断网时缺少证据的问题。首批能力包括 CPU、GPU、内存、温度、上下行网速、延迟、代理状态、公网 IP 与归属地。产品从一开始就要求可退出、可收起到菜单栏或 Dock、右键置顶、透明度调节，以及双击展开/收起。

## 第一阶段：从系统监控转向 AI 开发者工作状态

- 明确定位为“面向 AI 开发者的轻量工作状态浮窗”，不复制 iStat Menus 的全量硬件面板。
- 增加 Codex 额度与刷新时间、Clash 本地订阅流量、代理出口 IP、AI 网络健康。
- 网络诊断逐步拆分为 DNS、TCP、TLS、代理、规则、真实 Codex 端点与服务侧状态。
- 加入番茄钟、工作时间、午休、工作日、周末/加班、中国法定节假日与跨日状态清理。
- 天气位置与代理出口彻底解耦；天气按用户手选城市或主动授权的系统定位，结合日出日落与月相改变外观。

## 第二阶段：生产力与浮窗交互

- 应用活跃度由模糊分数改为可解释的前台时长和有效使用时长；加入今日、7 日、30 日排行榜。
- 锁屏、睡眠、合盖、空闲和长时间暂停不计入有效时长；浏览器只统计应用，不读取网页。
- 紧凑浮窗改为异常优先，显示今日第一应用、专注、额度、Clash 和天气。
- 持续修复菜单栏入口、Dock 恢复、输入框焦点、拖动、双击、窗口越界和展开裁切。
- 增加主题色、背景深浅、透明度、当前桌面/所有桌面策略和运行时多语言基础。

## 第三阶段：可信状态与本地事件时间线

- 建立本地事件账本，把网络、远程设备、额度、温度、Clash、番茄钟统一记录为发生、证据、恢复和持续时间。
- 时间线只保留最近 30 天/500 条，并采用懒加载，避免切页冻结。
- 所有第三方或推断数据开始展示来源、更新时间、置信度和降级状态。
- 社区 Codex 全局重置信号与个人官方额度严格分离，不再把社区预测写成官方承诺。

## 第四阶段：多 SSH 与 GPU 工作台

- 多设备复用本机 `~/.ssh/config` 与 ssh-agent，采用固定只读命令采集负载、内存、磁盘、GPU 和 Codex 可达性。
- 显存成为主要可视化；计算利用率、温度、功耗进入紧凑 GPU 矩阵。
- 任务识别支持 Slurm/调度器和用户显式开启的脱敏启动命令；不读取训练日志，不猜测任务内容。
- SSH 连接和详细采集分层，保留上次成功显存但不保留过期任务；手动刷新、观察模式和可复制诊断逐步加入。
- 规划局域网/VPN 条件、预期离线、告警宽限与会话级 Codex 代理，禁止默认污染远端全局环境。

## 第五阶段：额度、Clash 与外部通知

- Codex 使用本机官方 app-server；Cursor 采用只读本地会话的实验性内部接口；GLM Coding Plan 使用专用只读额度端点；DeepSeek 普通 API 仅在官方开放余额时展示。
- Clash 首选本机订阅元数据；Mihomo 控制器仅用于主动刷新 provider，并区分端口未监听、Secret 错误、HTTP 错误和无可刷新 provider。
- 飞书群机器人用于异常首次发生和恢复通知，带冷却与去重；Webhook 和签名密钥不进入日志、导出或时间线。
- 凭据从多个 Keychain 项逐步合并为一个统一保险库，目标是每次运行最多一次主动解锁。

## 6.4 系列：凭据架构与环境音

### 6.4.0

统一凭据保险库、Cursor 刷新修复、Clash 来源分层，并试验程序合成白噪音。

### 6.4.1

因合成声音辨识度差且令人疲劳，全部撤回。增加旧凭据迁移和 Mihomo 候选端口诊断，但自动遍历旧 Keychain 项仍可能触发系统密码弹窗队列。

### 6.4.2

- 解锁只读取一条统一保险库；旧凭据迁移改为显式操作，且全程禁止认证 UI。
- Security 查询同时使用非交互 `LAContext` 与失败策略；启动、轮询、切页仍为零 Keychain 访问。
- 加入 LaunchServices 和运行时双重单实例保护，避免两个 PulseDock 同时争用钥匙串。
- 环境音改为真实 CC0 录音网络流：窗边雨、微风、树叶、远雷、水下空间与棕色噪音；默认不播放、不落盘、不记录历史。
- 新增环境音来源/许可、音量、暂停、停止和 15/30/60 分钟定时。
- 建立产品规范、架构与数据源目录。

## 6.5.0：可信 Codex 与专注声音

- 研究 Codex Runway 后确认其 AGPL-3.0 授权边界：PulseDock 只参考信息层级，基于 OpenAI 官方 `codex app-server` clean-room 实现，不复制源码、OAuth、UI、资产或文案。
- Codex 从单一百分比升级为多个语义化窗口，并把官方 Token 活动作为独立口径呈现；社区全局重置继续处于第三方层，不能修改个人额度。
- 声音域从“几条环境录音”演进为环境音、工作音乐、网络广播三个模型。首批网上内容通过 Radio Browser 查询，只接受 HTTPS 和可解码流；磁带机与网络收音机形成不同交互。

## 6.7.0：声音信息架构与可解释状态

- 声音三类页面统一为 2×3 六卡的发现、搜索、网络模式和分页结构；环境音不再是被简化的旁路页面。
- 浏览分类与真实播放队列拆分，避免在听广播时切到环境音页面而改掉广播循环模式或使用错误皮肤。
- 环境音采用程序化低刷新动态声景，音乐和广播分别保留磁带机、收音机，避免下载动态视频或维持高功耗渲染。
- Clash 用量从单一“剩余值”升级为已用/剩余/总计并列显示，消除与 Clash Verge 的表述差异。
- SSH/GPU 主机健康与 Codex HTTP 端点探测分层；未认证端点没有 HTTP 状态不再被包装成整机“部分异常”。

## 6.7.1：实机反馈后的声音交互纠偏

- 承认 6.7.0 只完成声音布局骨架，未完整交付分页、发现分类和控制语义；补丁版按截图重新验收。
- 分页从两个箭头升级为邻近页、±1/±2 与指定页跳转，并明确采用按页 offset 懒加载。
- 输入问题定位到游离 field editor：修正为真实文本框先成为响应者，再配置其已附着编辑器。
- 音乐、广播发现分类实质扩充；环境音发现从静态文字改为可操作筛选。
- 定时和播放模式恢复用户最初要求的图标交互；环境动画按声音类型绘制，避免一套粒子替代所有声景。
- 坚持轻量与隐私：一个播放器、默认静音、点击才连接、不自动恢复、不抓账号、不存历史、不建立离线资料库。
- 远程设备新增网络范围、置顶与排序。局域网/VPN 不满足时属于预期离线；网络切换宽限、三次失败阈值和三台并发上限减少飞书噪音与本机负载。
- 交互层修复 ScrollView 背景命中根因，完整态普通区域重新支持双击和拖动；定位加入用户选择 generation，确保手动城市最终优先。
- SSH 解析从“避免重复字典键”继续加固为限长、实时排空、数值范围、饱和求和和缺 marker 降级，形成面向不可信远端文本的输入边界。

## 6.7.2：从“网络类型”升级为“目标网络范围”

- 实机反馈证明，仅知道 Mac 正在使用 Wi-Fi 并不能说明它位于 G1-002 所属局域网；旧逻辑会在任何 Wi-Fi 下探测局域网机器，制造首页红色状态与无效告警。
- 新逻辑使用 `ssh -G` 获取别名的有效 `HostName`，解析私有 IPv4，并与活动物理接口的地址和掩码比较。目标不属于当前子网时是“预期暂停”，而非 SSH 故障。
- 公网设备与局域网设备在卡片上使用不同范围标识；状态与范围成为两个独立维度，避免“公网健康”和“局域网暂停”在视觉上无法解释。
- 代理出口 IP 不参与局域网判断。这样 Clash 切换海外节点不会让天气位置或 SSH 局域网范围随之漂移。
- 范围判断在探测前和探测结果提交前各执行一次；同类网络之间切换也主动刷新，防止迟到 SSH 结果污染当前网络状态。

## 6.8.0：让监控器先证明自己不卡

- 将高频系统指标采样从 `@MainActor` 中抽离，用专用串行 utility 队列生成不可变快照，主线程只负责一次发布。
- 新增主线程 ping 滚动统计。诊断不再只是“网络是否正常”，也能回答“软件刚才是否真的卡了、卡了多久”。
- 交互层取消必然延迟单击的互斥双击协议，并把首击激活、真实 field editor 和 IME marked text 收敛到一条 AppKit 响应链。
- 洞察页从堆卡转向信息密度：Token 图表使用真实日期和稀疏刻度，Cursor 只保留一个权威配置入口，工作日摘要按需展开为官方节假日月历。
- 环境音从六个样例扩充为 24 个可审计 CC0 条目。条数不再是目标本身；唯一来源 ID、许可、作者、时长、核验日期和实际分类关系一起进入发布门禁。

## 6.9.1：把发现筛选还原为筛选控件

- 6.9.0 虽完成声景和播放器重构，却遗漏了环境音发现区仍使用两列弹性网格的旧布局，造成每个类型被拉成半屏灰色条。该问题说明视觉回归不能只验证播放器。
- 6.9.1 将环境分类切换为内容宽度的标签流：左对齐、自动换行、统一选中色和键盘可访问性；不再为了对齐而牺牲信息层级。

## 6.12.3：让设置控件符合 macOS 的焦点语义

- 固定 Picker 替换为作为 first responder 的原生按键录制控件；录制期间旧热键临时注销，候选值只在 Carbon 注册成功后成为有效设置。
- 浮窗的共享 field editor 原先在点击 SwiftUI 背景后仍保持 first responder。现在非输入目标的点击会先结束编辑，蓝色焦点环与光标不再伪装成“一直待输入”。

## 6.12.2：不再把局域网错等于同一子网

- G1-002 的现场证据是 Mac 位于 `192.168.31.0/24`，机器人位于 `10.33.48.138`，系统存在经 `en0` 和 `192.168.31.1` 的专用主机路由。旧的子网位与判断会在 SSH 之前直接拦截。
- 新逻辑把同子网、物理网卡专用路由和有界 TCP 握手按证据强度组合，同时拒绝默认路由与 `utun` 等虚拟网卡。

## 6.12.1：让截图自带版本证据

- 展开面板品牌区增加低对比度版本胶囊，并从 Info.plist 动态读取版本；错误截图不再需要另外确认用户正在运行哪个安装包。

## 6.12.0：让召唤、位置与网络失败可预测

- 窗口状态从菜单与鼠标扩展到两组 Carbon 全局快捷键，显示/隐藏和展开/收起各自拥有单一职责；设置变化会重新注册，冲突不会静默覆盖。
- 天气位置第一次具备持久模式：固定地点保护手动选择，自动跟随使用低精度单次定位、30 分钟/2 小时分级周期和 2 公里行政区防抖。
- 音频播放器在 AVPlayer 前增加只读取首个响应字节的有界预检，并把失败阶段映射为可见类别；同一网络会话内失败来源冷却五分钟，成功来源重新排到前面。
- AI 路径从一次测速升级为三轮中位数与范围证据，性能偏慢与不可达继续保持不同状态。
- 中文目录没有为追求数量而接入不可审计网页。现有公共目录使用地区、语言别名和有限后续页回填，并把本机已收藏且元数据仍匹配的来源作为补充，界面显示来源组成。
- AppKit 内容命中压力回归增加 100 轮连续双击，防止设置、播放和分页操作再次被窗口手势抢占。

## 6.11.0：让中文目录与控制语义真正落地

- 目录检索不再把一个 `language=chinese` 标签当成中文内容的全部。港澳台及海外地区会在首批不足一页时，保留相同国家限定补查 `mandarin` 和 `cantonese`，并继续读取有限后续页，再做 HTTPS、编码和本地语义复核。
- 播放控制回归用户熟悉的三态：顺序循环使用 `repeat`、随机使用 `shuffle`、单条循环使用 `repeat.1`。历史固定电台配置保留兼容，但视觉上不再暗示“置顶”。
- 布局将所有可伸缩空间置于音量和右侧操作之间，因此定时和模式始终在尾部成组；定时胶囊以固定内距容纳图标与分钟，避免文字贴边。
- AppKit 双击守卫升级为内容画布优先：只允许真正外圈留白与明确拖动把手触发窗口状态改变，避免任何 SwiftUI 交互控件因命中链不完整而误缩小。

## 6.10.0：让目录、诊断与交互说真话

- 将“本地中文”改为严格大陆地理语义，并把港澳台、全球中文单列；工作音乐不再以“同一个广播目录、换个图标”实现。
- 声音操作从重复下拉和易被双击抢走的组合，收敛为单击切换、右键补充选项与统一的交互保护期。
- 反馈表明“AI 较慢”和“AI 不可达”必须拆分；Token 图和排行榜也转向默认只展示当前决策真正需要的信息。

## 6.9.0：从“换图标”到有语义的环境声景

- 实机截图表明，早期环境播放器虽然按雨、风、水下分支绘制，但咖啡馆、壁炉、列车和阅读等大部分场景仍共同落到棕色频谱柱模板；这造成类型辨识度低、视觉模板化且与真实环境声不匹配。
- 环境录音现在在目录层拥有显式 `sceneStyle`，视觉不再依赖标题、语言、tag 或图标猜测。相同的绘制引擎可复用，但“近景柴火”和“暖炉房间”、“普通雨景”和“城市晚雨”等必须具有不同构图与运动语义。
- 播放器控制区也从弹性横排重构为固定分区，避免音量图标宽度与 Slider 伸缩导致定时器、循环控制和整块播放器跳动。静音成为真实 0 音量，取消近似静音的下限。
- 输入问题继续下沉到 AppKit 命中和 field editor 生命周期：编辑器本身被命中时仍视为文本控件，组合输入或已附着编辑器期间拒绝状态回写，防止中文输入和插入光标偶发丢失。

## 6.6.0：可靠编辑、智能直连与可停留设置

- 声音搜索从普通 SwiftUI 字段改为原生 AppKit 编辑桥，显式保护中文输入法 marked text、首击焦点和标准编辑快捷键。
- 目录端分页替代“只显示固定 12 条”；收藏保存最小公开元数据，音乐与广播按各自语义提供播放模式。
- 面对 Clash 全局代理导致部分电台 TLS/缓冲失败，加入最小权限的会话级本机音频中转：只处理用户点选的公共 HTTPS MP3/AAC，不改系统或 Clash 配置。
- GLM 连接器加入单飞、结构演进解析、业务错误与旧快照保留，避免一次上游变化把额度清成未知。
- Clash Verge 的真实控制器可能只暴露 Unix Socket；自动发现据此扩展，同时保持固定路径白名单。
- 设备的嵌套菜单改为可交互 popover，解决鼠标进入“网络范围”子菜单时消失的问题。

## 始终不变的边界

PulseDock 不读取网页、窗口标题、聊天、剪贴板、训练日志、项目文件、SSH 私钥或远端密码；不静默修改 Clash、远端 shell、systemd、IDE 启动器或代理全局配置。来源不可靠时必须显示未知或降级，而不是伪造精确数据。
